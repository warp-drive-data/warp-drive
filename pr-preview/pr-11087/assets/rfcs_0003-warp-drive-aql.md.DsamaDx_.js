import{Ct as e,at as t,it as n,ot as r,st as i,t as a,tt as o,yt as s}from"./chunks/framework.ILid7KTz.js";var c=JSON.parse(`{"title":"AQL: An Abstract Query Language and Persisted-Query Extension for JSON:API","description":"","frontmatter":{"title":"AQL: An Abstract Query Language and Persisted-Query Extension for JSON:API","warp-drive-rfc":3,"emberjs-rfc":null,"emberjs-pr":null,"emberjs-branch":null,"sync-hash":null,"stage":"proposed","start-date":"2026-09-22T00:00:00.000Z","release-date":null,"release-versions":null,"teams":["data"],"prs":{"accepted":null},"project-link":null,"suite":null},"headers":[],"relativePath":"rfcs/0003-warp-drive-aql.md","filePath":"rfcs/0003-warp-drive-aql.md"}`),l={name:`rfcs/0003-warp-drive-aql.md`},u={id:`aql-an-abstract-query-language-and-persisted-query-extension-for-json-api`,tabindex:`-1`};function d(a,c,l,d,f,p){let m=e(`StatusBadge`);return s(),n(`div`,null,[o(`h1`,u,[c[0]||=r(`AQL: An Abstract Query Language and Persisted-Query Extension for JSON:API `,-1),i(m,{variant:`proposed`,text:`Proposed`}),c[1]||=r(),c[2]||=o(`a`,{class:`header-anchor`,href:`#aql-an-abstract-query-language-and-persisted-query-extension-for-json-api`,"aria-label":`Permalink to “AQL: An Abstract Query Language and Persisted-Query Extension for JSON:API”`},`​`,-1)]),c[3]||=t(`<h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to “Summary”">​</a></h2><p>AQL is a terse <code>.aql</code> text syntax that compiles to the persisted-query envelope defined by the (proposed) JSON:API <a href="https://jsonapi.org/format/1.2/#extension-rules" target="_blank" rel="noreferrer"><code>QUERY</code> extension</a> — <code>q:id</code> / <code>q:type</code> / <code>q:search</code>, extended here with <code>q:defaults</code>, <code>q:headers</code>, and <code>q:entries</code>. It gives WarpDrive apps (and, by design, any JSON:API-flavored backend) a client-authorable query language covering field/relationship selection, a filter operator vocabulary aligned with the <code>filter[field][operator]=value</code> convention already common across production JSON:API APIs, request headers, persisted-query variables, and batching several unrelated root queries into one document. This RFC specifies the language and its compiled output as a design proposal, ahead of any implementation — see <a href="#tooling">Tooling</a> for the parser, editor, and linting work this RFC&#39;s acceptance would unblock.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to “Motivation”">​</a></h2><h3 id="the-problem" tabindex="-1">The problem <a class="header-anchor" href="#the-problem" aria-label="Permalink to “The problem”">​</a></h3><ol><li><strong>Hand-authoring JSON:API query bodies is verbose and typo-prone.</strong> Sparse fieldsets and <code>include</code> paths are easy to get subtly wrong (a misspelled field name, a forgotten nested include) with no compile-time feedback, and the JSON is noisy to write by hand for anything beyond a trivial query.</li><li><strong>Base JSON:API has no filter syntax, and every backend invents its own.</strong> This is by design in the base spec (filtering is left to server implementations), but it means there&#39;s no portable way to <em>express</em> a filter that a client-side tool — a query language, a schema-aware editor, a codegen step — can reason about across backends, even though a large fraction of real JSON:API-flavored backends converge on some variant of <code>?filter[field][operator]=value</code>.</li><li><strong>Persisted queries need somewhere to put variables and request metadata that isn&#39;t query content.</strong> A reusable, cacheable query needs a way to declare &quot;this value is supplied per call&quot; (variables) and &quot;this header is required&quot; — neither of which is JSON:API document content, so neither has anywhere to live in a plain <code>q:search</code> body.</li><li><strong>Screens routinely need several unrelated resources at once.</strong> A dashboard&#39;s &quot;current user + notifications + billing status&quot; is three independent root queries, not a graph traversal from any one of them — the kind of batching GraphQL handles natively that JSON:API&#39;s one-collection-URL-per-request model has no answer for.</li><li><strong>Real backends rarely conform 100%.</strong> Different filter param names, no <code>include</code> support, no batch endpoint, non-JSON:API response shapes — all common, all needing translation somewhere. Today that translation is ad hoc, scattered glue code with no name and no shared shape.</li></ol><h3 id="why-a-language-not-just-a-json-builder-function" tabindex="-1">Why a language, not just a JSON builder function <a class="header-anchor" href="#why-a-language-not-just-a-json-builder-function" aria-label="Permalink to “Why a language, not just a JSON builder function”">​</a></h3><p>A terse text syntax with its own grammar — rather than, say, a fluent JS builder API — is what makes AQL schema-aware and toolable the way a query needs to be to catch mistakes early: a <code>.aql</code> file can be statically checked against a <code>Schemas</code> map (unknown field and relationship names are a compile error, not a runtime 404 or a silently-empty response), it&#39;s diffable and reviewable in a PR like any other source file, and it&#39;s the natural shape for the persisted-query story this RFC also specifies (a stable, hashable unit of source text is what makes a <code>q:id</code> meaningful).</p><h2 id="detailed-design" tabindex="-1">Detailed design <a class="header-anchor" href="#detailed-design" aria-label="Permalink to “Detailed design”">​</a></h2><p>This section is the complete design specification for review purposes — there is no accompanying implementation or separate spec document yet; this RFC is that document. If accepted, the reference implementation should ship a full, executable-grammar reference (EBNF-level detail per section, kept in sync with the parser rather than with this RFC&#39;s prose) alongside the parser itself — see <a href="#tooling">Tooling</a>.</p><h3 id="pipeline" tabindex="-1">Pipeline <a class="header-anchor" href="#pipeline" aria-label="Permalink to “Pipeline”">​</a></h3><div class="language-txt"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>.aql text  →  parse  →  compiled envelope (canonical IR)  →  [adapter]  →  actual HTTP request</span></span></code></pre></div><p><code>.aql</code> text and the compiled envelope it produces are the two artifacts this RFC specifies. What sits downstream of the envelope — a request builder, an adapter for a non-conforming backend — is discussed as design guidance (see <a href="#adapting-non-conforming-apis">Adapting non-conforming APIs</a> below) but is explicitly out of scope for what this RFC asks WarpDrive to ship.</p><h3 id="documents-single-entry-and-multi-entry" tabindex="-1">Documents: single-entry and multi-entry <a class="header-anchor" href="#documents-single-entry-and-multi-entry" aria-label="Permalink to “Documents: single-entry and multi-entry”">​</a></h3><p>Every <code>.aql</code> document is exactly one <code>QUERY</code> block, in one of two mutually exclusive shapes, distinguished by the first token after <code>QUERY</code>:</p><div class="language-aql"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">aql</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>QUERY &lt;type&gt; { &lt;sections&gt; }              # single-entry: one primary type, one graph</span></span></code></pre></div><div class="language-aql"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">aql</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>QUERY {</span></span>
<span class="line"><span>  entry &lt;name&gt;: &lt;type&gt; { &lt;sections&gt; }    # multi-entry: several independent roots</span></span>
<span class="line"><span>  ...</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>A single-entry document&#39;s sections (<code>data</code>/<code>filter</code>/<code>page</code>/<code>headers</code>) are unambiguous because there&#39;s exactly one primary type they apply to. The moment there&#39;s more than one root, every section needs to know <em>which</em> root it belongs to, which is exactly what <code>entry &lt;name&gt;: &lt;type&gt; {}</code> provides — multi-entry isn&#39;t a variation on single-entry&#39;s grammar, it&#39;s a distinct document shape. <code>&lt;name&gt;</code> is the multi-entry equivalent of a GraphQL field alias: how the caller refers to that entry&#39;s result in the response, with no meaning to the server beyond being a response key.</p><h3 id="data-—-field-and-relationship-selection" tabindex="-1"><code>data</code> — field and relationship selection <a class="header-anchor" href="#data-—-field-and-relationship-selection" aria-label="Permalink to “data — field and relationship selection”">​</a></h3><div class="language-aql"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">aql</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>data {</span></span>
<span class="line"><span>  name</span></span>
<span class="line"><span>  ceo { name; profileImage }   # a relationship followed by {} both selects and includes it</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>A bare field name selects an attribute (or a relationship&#39;s linkage, without its own fields, if not followed by <code>{}</code>). A relationship followed by <code>{ &lt;selection&gt; }</code> recursively selects and includes it — only fields whose schema <code>kind</code> is <code>resource</code> or <code>collection</code> may take a <code>{}</code> selection; anything else is a compile error. If the same related type is reached through more than one path, its requested fields merge into one entry and its <code>include</code> path is only added once. Compiles onto <code>q:search.fields</code> (type name → field-name array) and <code>q:search.include</code> (a dotted path, or an array of them).</p><h3 id="filter-—-predicates" tabindex="-1"><code>filter</code> — predicates <a class="header-anchor" href="#filter-—-predicates" aria-label="Permalink to “filter — predicates”">​</a></h3><p>The core new design in this RFC. A filter is a predicate — <code>(fieldPath, operator, value)</code> — combined with every other filter predicate in the query via AND:</p><div class="language-aql"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">aql</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>filter {</span></span>
<span class="line"><span>  status = &quot;active&quot;                    # bare \`=\` is shorthand for [eq]</span></span>
<span class="line"><span>  age[gte] = 18</span></span>
<span class="line"><span>  age[lt] = 65</span></span>
<span class="line"><span>  category[in] = [&quot;books&quot;, &quot;film&quot;]</span></span>
<span class="line"><span>  ownerId[isSame] = $user.id</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>compiles to:</p><div class="language-json"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;filter&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;eq&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;active&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;age&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;gte&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">18</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;lt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">65</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;category&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;in&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;books&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;film&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;ownerId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;isSame&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;$user.id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>This is a deliberate choice to make <code>q:search.filter</code> mirror the <code>?filter[field][operator]=value</code> convention already common across production JSON:API-flavored backends, rather than invent a new one — so a server that already parses that convention for <code>GET</code> requests can reuse the same logic for a <code>QUERY</code> request&#39;s body with no translation layer. The full operator vocabulary — <code>eq</code>, <code>ne</code>, <code>in</code>, <code>notIn</code>, <code>like</code>, <code>iLike</code>, <code>notLike</code>, <code>notILike</code>, <code>gt</code>, <code>gte</code>, <code>lt</code>, <code>lte</code>, <code>between</code>, <code>notBetween</code>, <code>isNull</code>, <code>isNotNull</code>, <code>isSame</code> — along with each operator&#39;s arity and expected value shape, is specified in full in the table below.</p><table tabindex="0"><thead><tr><th>Operator</th><th>Arity</th><th>Value shape</th><th>Meaning</th></tr></thead><tbody><tr><td><code>eq</code></td><td>1</td><td>literal</td><td>Equal to (default when <code>[operator]</code> omitted)</td></tr><tr><td><code>ne</code></td><td>1</td><td>literal</td><td>Not equal to</td></tr><tr><td><code>in</code></td><td>≥1</td><td>array literal</td><td>Equal to one of</td></tr><tr><td><code>notIn</code></td><td>≥1</td><td>array literal</td><td>Equal to none of</td></tr><tr><td><code>like</code></td><td>1</td><td>literal (string)</td><td>SQL-style pattern match, case-sensitive</td></tr><tr><td><code>iLike</code></td><td>1</td><td>literal (string)</td><td>SQL-style pattern match, case-insensitive</td></tr><tr><td><code>notLike</code></td><td>1</td><td>literal (string)</td><td>Negated <code>like</code></td></tr><tr><td><code>notILike</code></td><td>1</td><td>literal (string)</td><td>Negated <code>iLike</code></td></tr><tr><td><code>gt</code></td><td>1</td><td>literal (number or date)</td><td>Greater than</td></tr><tr><td><code>gte</code></td><td>1</td><td>literal (number or date)</td><td>Greater than or equal to</td></tr><tr><td><code>lt</code></td><td>1</td><td>literal (number or date)</td><td>Less than</td></tr><tr><td><code>lte</code></td><td>1</td><td>literal (number or date)</td><td>Less than or equal to</td></tr><tr><td><code>between</code></td><td>2</td><td>array literal (2 elements)</td><td>Inclusive range</td></tr><tr><td><code>notBetween</code></td><td>2</td><td>array literal (2 elements)</td><td>Outside an inclusive range</td></tr><tr><td><code>isNull</code></td><td>1</td><td>literal (boolean)</td><td><code>true</code>: field is null; <code>false</code>: is not null</td></tr><tr><td><code>isNotNull</code></td><td>1</td><td>literal (boolean)</td><td>Negation of <code>isNull</code></td></tr><tr><td><code>isSame</code></td><td>1</td><td>reference</td><td>See below</td></tr></tbody></table><p><code>like</code>/<code>iLike</code>/<code>notLike</code>/<code>notILike</code> use <code>%</code> (zero or more characters) and <code>_</code> (exactly one character) as wildcards, escaped as <code>\\%</code> / <code>\\_</code> to match them literally. <code>between</code>/<code>notBetween</code> take their two bounds as an array literal of exactly two elements, in order (<code>[low, high]</code>), inclusive on both ends — AQL does not itself validate that the first element isn&#39;t greater than the second; that&#39;s a server-side concern, like all other value validation. <code>isNull</code>/<code>isNotNull</code> take a boolean, not the value being tested — the field being tested is the field path itself (<code>jobId[isNull] = true</code>, not some encoding of &quot;null&quot; as a value for <code>eq</code>).</p><p>Two things stand out enough to call out here:</p><ul><li><strong><code>isSame</code></strong> compares a field against another field on the same resource, or against a <code>$</code>-namespaced piece of server-resolved request context (<code>$user.id</code>, <code>$session.tenantId</code>) — AQL emits the reference as a plain string and does no comparison itself; resolution semantics are entirely the server&#39;s contract.</li><li><strong>Deep paths</strong> (<code>team.manager.lastName</code>) compile to dotted keys unchanged. Whether a server supports filtering through a relationship, and what it does about join cardinality or nulls along the way, is server-defined and outside AQL&#39;s scope.</li></ul><p>Deliberately left out: OR/grouped boolean logic (there is no OR in AQL&#39;s filter grammar today — see <a href="#unresolved-questions">Unresolved questions</a>), filtering a related collection separately from the primary result set, and a separate <code>search</code> parameter.</p><h3 id="page-—-pagination" tabindex="-1"><code>page</code> — pagination <a class="header-anchor" href="#page-—-pagination" aria-label="Permalink to “page — pagination”">​</a></h3><div class="language-aql"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">aql</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>page { size = 10 }</span></span></code></pre></div><p>Flat <code>key = value</code>, compiled onto <code>q:search.page</code> with no operator — pagination keys (<code>size</code>, <code>limit</code>, <code>offset</code>, <code>number</code>, <code>cursor</code>, ...) are opaque to AQL; a server&#39;s pagination profile (e.g. the <a href="https://jsonapi.org/profiles/ethanresnick/cursor-pagination/" target="_blank" rel="noreferrer">cursor pagination profile</a>) defines which ones are meaningful.</p><h3 id="headers-—-request-headers" tabindex="-1"><code>headers</code> — request headers <a class="header-anchor" href="#headers-—-request-headers" aria-label="Permalink to “headers — request headers”">​</a></h3><div class="language-aql"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">aql</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>headers {</span></span>
<span class="line"><span>  &quot;Idempotency-Key&quot; = @arg requestId</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>compiles to a sibling <code>q:headers</code> key, not into <code>q:search</code>. HTTP headers describe <em>how a request is made</em> (auth, idempotency, tenant scoping, content negotiation), not JSON:API query content a server has any defined place for inside the document it parses — merging them into <code>q:search</code> would ask a JSON:API server to reach into its own request object to find data the JSON:API document format has nowhere to put. A request builder consuming the compiled envelope reads <code>q:headers</code> to set outgoing HTTP headers and never forwards it as document content. Header names are quoted strings (not bare identifiers) because header names routinely contain <code>-</code>; values are a string or an <code>@arg</code> variable — no arrays (a list-like header is still one string) and no <code>isSame</code>-style reference (there&#39;s no server-side resolution step for a literal client-sent header to defer to). A <code>headers{}</code> section may only appear at a document&#39;s outermost level, never inside an <code>entry {}</code> block — a <code>.aql</code> document always compiles to exactly one HTTP request, and one request has exactly one header set no matter how many entries its body describes.</p><h3 id="arg-and-q-defaults-—-persisted-query-variables" tabindex="-1"><code>@arg</code> and <code>q:defaults</code> — persisted-query variables <a class="header-anchor" href="#arg-and-q-defaults-—-persisted-query-variables" aria-label="Permalink to “@arg and q:defaults — persisted-query variables”">​</a></h3><div class="language-aql"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">aql</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>filter { total[gt] = @arg(number) minTotal = 0 }</span></span></code></pre></div><p>compiles the value to its <code>$</code>-prefixed variable form and records the default separately:</p><div class="language-json"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;filter&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;total&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;$gt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;number&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } } }</span></span></code></pre></div><div class="language-json"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;q:defaults&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;filter&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;total&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;gt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } } } }</span></span></code></pre></div><p><code>@arg</code> declares a variable a caller supplies at invocation time via <code>q:args</code>; the optional <code>= default</code> is recorded under a sibling <code>q:defaults</code> object (mirroring <code>q:search</code>&#39;s own shape) so an invocation that omits it still gets a sensible result. <code>q:defaults</code> is deliberately outside the <code>q:search</code> object the base <code>QUERY</code> extension defines, so a server that doesn&#39;t recognize it can ignore it safely rather than choke on an unrecognized member inside the query it does understand.</p><h3 id="multi-entry-graphs" tabindex="-1">Multi-entry graphs <a class="header-anchor" href="#multi-entry-graphs" aria-label="Permalink to “Multi-entry graphs”">​</a></h3><div class="language-json"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;q:id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;sha256&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;q:entries&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;me&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;q:type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;user&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;q:search&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;notifications&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;q:type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;notification&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;q:search&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;q:headers&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>q:entries</code> replaces the top-level <code>q:type</code>/<code>q:search</code> a single-entry envelope has — an envelope has one or the other, never both — with each entry carrying its own <code>q:type</code>/<code>q:search</code>/ <code>q:defaults</code>, exactly as if compiled standalone. <code>q:headers</code>, if present, stays at the very top (see above for why headers can&#39;t be per-entry).</p><p>Two design points this RFC resolves and one it explicitly leaves open:</p><ul><li><strong>Transport.</strong> The base <code>QUERY</code> extension defines requests as issued &quot;against the same URL as if a <code>GET</code> had been made,&quot; which presupposes a URL for <em>a</em> resource collection — a multi-entry query has no such URL, so it needs a dedicated, resource-agnostic endpoint (conceptually closer to a single GraphQL endpoint than a REST collection URL). This RFC doesn&#39;t mandate a specific path, the same way base JSON:API doesn&#39;t mandate specific resource paths beyond convention.</li><li><strong>Response shape.</strong> A multi-entry response wraps one complete, ordinary JSON:API document per entry under an <code>entries</code> key (<code>{ &quot;entries&quot;: { &quot;&lt;name&gt;&quot;: { &quot;data&quot;: ..., &quot;included&quot;: ... } } }</code>). A server MAY additionally hoist resources shared across entries&#39; <code>included</code> arrays into a top-level <code>included</code>, mirroring how a single document already deduplicates included resources — but that&#39;s an optimization, not a requirement.</li><li><strong>Left open</strong>: partial failure semantics (does one bad entry fail the whole request, or does it get an error object in its own slot?), cross-entry dependencies (there is no syntax for &quot;fetch <code>notifications</code> filtered by <code>me</code>&#39;s id&quot; — a meaningfully harder feature than batching independent queries), and <code>q:args</code> name-conflict resolution across entries that happen to declare the same variable name. See <a href="#unresolved-questions">Unresolved questions</a>.</li></ul><h3 id="adapting-non-conforming-apis" tabindex="-1">Adapting non-conforming APIs <a class="header-anchor" href="#adapting-non-conforming-apis" aria-label="Permalink to “Adapting non-conforming APIs”">​</a></h3><p>Most real backends won&#39;t implement the <code>QUERY</code> extension as specified above. The compiled envelope is designed to be a stable contract an app-specific <strong>adapter</strong> (the same &quot;builder&quot; concept WarpDrive&#39;s own request builders already use, applied here to AQL&#39;s compiled output) translates into that backend&#39;s actual wire format — remapping <code>filter[field][operator]</code> into a different param convention, fanning <code>include</code> out into N follow-up requests against a backend with no compound-document support, or fanning a multi-entry document out into one request per entry against a backend with no batch endpoint. This keeps one non-conforming backend&#39;s quirks contained to its own adapter instead of leaking into the language or the compiled shape every backend has to share. This RFC specifies the <em>pattern</em>, illustrated below; it does not ask WarpDrive to ship any adapter implementation.</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// illustrative, not a real API — nothing here exists yet</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> remapFilters</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">filter</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt; </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> undefined</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">filter) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> undefined</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> where</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {};</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">fieldPath</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ops</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Object.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">entries</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(filter)) {</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">operator</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Object.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">entries</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ops)) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      where[fieldPath] </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> \`\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">operator</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}:\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Array</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isArray</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">(</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">value</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> value</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">join</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;,&#39;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> value</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> where;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>A backend whose filter params look like <code>where[field]=operator:value</code> instead of <code>filter[field][operator]=value</code> needs nothing more than a remap like this one in its adapter — the compiled envelope itself never changes to accommodate it.</p><h3 id="current-implementation-status" tabindex="-1">Current implementation status <a class="header-anchor" href="#current-implementation-status" aria-label="Permalink to “Current implementation status”">​</a></h3><p>Nothing in this RFC is implemented anywhere in WarpDrive today. <a href="https://github.com/warp-drive-data/warp-drive/pull/9073" target="_blank" rel="noreferrer">PR #9073</a> (&quot;spec: JSON:API Graphs&quot;) first proposed the <code>QUERY</code> extension and an early version of AQL as its authoring syntax, alongside an unrelated &quot;complex relationships&quot; profile for linkage nested inside <code>attributes</code>. A later exploration (<a href="https://github.com/warp-drive-data/warp-drive/pull/11087" target="_blank" rel="noreferrer">PR #11087</a>) prototyped a parser covering <code>data</code>/<code>page</code>/<code>@arg</code> and a VS Code syntax highlighter; that prototype silently dropped <code>filter</code>/<code>page</code> output in places, had no string-escape handling, and lost track of open contexts on certain inputs. Working through those bugs is what surfaced the design gaps this RFC resolves — the prototype&#39;s <code>filter</code> covered only flat equality (<code>q:search.filter.key = value</code>, no operator), and it had no <code>headers</code> or multi-entry support at all. None of that prototype is proposed for merge alongside this RFC. If this RFC is accepted, <a href="#tooling">Tooling</a> below is the implementation plan.</p><h2 id="how-we-teach-this" tabindex="-1">How we teach this <a class="header-anchor" href="#how-we-teach-this" aria-label="Permalink to “How we teach this”">​</a></h2><p>If implemented, AQL should be documented at three levels, consistent with how WarpDrive documents its other experimental packages:</p><ul><li><strong>Normative reference</strong>: a <code>spec/</code> directory alongside the reference parser (e.g. <code>warp-drive-packages/experiments/src/aql/spec/*.md</code>), one document per grammar section (<code>filter</code>, <code>headers</code>, multi-entry, adapting non-conforming APIs), for contributors and anyone implementing a server or an adapter against the compiled envelope. This should be kept in sync with the parser, not with this RFC — once an implementation exists, this RFC is a historical design record, not the place future grammar changes get made.</li><li><strong>Package README</strong>: a quick-start for someone reaching for <code>@warp-drive/experiments/aql</code> directly.</li><li><strong>The Manual</strong>: reader-facing pages on the docs site&#39;s Experiments section, for app authors browsing WarpDrive&#39;s documentation rather than its source.</li></ul><p>AQL extends concepts JSON:API users already have (sparse fieldsets, <code>include</code>, filtering) with a terser syntax and an operator vocabulary modeled on a convention already common in production JSON:API-flavored backends — someone who has used such a backend&#39;s <code>filter[field][operator]</code> convention should find AQL&#39;s filter syntax familiar rather than a new mental model to learn. It should be positioned, and taught, as an <strong>Experiment</strong> (the same &quot;use at your own risk, PRE-RFC-within-WarpDrive-core&quot; framing every other page under <code>guides/the-manual/experiments/</code> carries) until a request-builder integration exists — this RFC settles the design that integration would be built against, not the integration itself.</p><h2 id="tooling" tabindex="-1">Tooling <a class="header-anchor" href="#tooling" aria-label="Permalink to “Tooling”">​</a></h2><p>A query language earns its keep on the strength of its tooling, not just its grammar. This section inventories every tool AQL adoption would eventually want, states a plan or explicit non-plan for each, and is honest about which ones this RFC does not expect WarpDrive to build well — or at all. None of this is proposed for merge alongside this RFC; it&#39;s the roadmap accepting this RFC would open up, roughly in the order it becomes buildable.</p><table tabindex="0"><thead><tr><th>Tool</th><th>Plan</th><th>Depends on</th></tr></thead><tbody><tr><td>Reference parser</td><td>Build in <code>@warp-drive/experiments/aql</code>; everything else below depends on it</td><td>This RFC</td></tr><tr><td>VS Code syntax highlighting</td><td>TextMate grammar for <code>.aql</code> files and embedded <code>aql\`...\`</code> template literals</td><td>Reference parser&#39;s grammar</td></tr><tr><td>Other TextMate-based editors (Sublime Text, JetBrains via bundle import)</td><td>Ship the same grammar as a standalone TextMate bundle</td><td>VS Code grammar</td></tr><tr><td>Neovim / tree-sitter</td><td>Stretch goal; not committed</td><td>A from-scratch tree-sitter grammar</td></tr><tr><td>Language server (diagnostics, completion, go-to-definition)</td><td>Planned follow-up, not initial rollout</td><td>Reference parser + stable grammar</td></tr><tr><td>Linting</td><td>Expose as diagnostics from the language server; thin CLI wrapper for CI</td><td>Language server</td></tr><tr><td>Formatting</td><td>Deferred</td><td>Persisted-query-id normalization (see <a href="#unresolved-questions">Unresolved questions</a>)</td></tr><tr><td>Embedded template-literal tooling</td><td>Mirror the <code>graphql-tag</code>/lit-html/styled-components precedent</td><td>VS Code grammar, language server</td></tr><tr><td>Persisted-query build-time extraction</td><td>Necessary for production use; not initial rollout</td><td>Stable grammar + request-builder integration</td></tr><tr><td>Type generation / codegen</td><td>Aspirational, likely lowest priority</td><td>Language server or reference parser</td></tr><tr><td>Playground / REPL</td><td>Nice-to-have for docs and onboarding</td><td>Reference parser</td></tr><tr><td>CI validation CLI</td><td>Realistic near-term deliverable</td><td>Reference parser + stable grammar</td></tr></tbody></table><h3 id="reference-parser" tabindex="-1">Reference parser <a class="header-anchor" href="#reference-parser" aria-label="Permalink to “Reference parser”">​</a></h3><p>The foundation everything else in this table depends on: a TypeScript lexer and recursive-descent parser compiling <code>.aql</code> text to the envelope this RFC specifies, taking a <code>Schemas</code> map (WarpDrive&#39;s existing schema representation) so unknown field/relationship names are a compile error. Ship it in <code>@warp-drive/experiments/aql</code>, kept experimental until a request-builder integration exists to consume its output; graduate it out of experiments once both this RFC&#39;s grammar and that integration have stabilized.</p><h3 id="syntax-highlighting" tabindex="-1">Syntax highlighting <a class="header-anchor" href="#syntax-highlighting" aria-label="Permalink to “Syntax highlighting”">​</a></h3><p>VS Code is the flagship target: a TextMate grammar for standalone <code>.aql</code> files, plus an injection grammar for <code>aql\`...\`</code> tagged template literals embedded in JS/TS/JSX/TSX/Svelte source, the same pattern <code>graphql-tag</code>, lit-html, and styled-components tooling all use for embedding a foreign language inside a template literal. Publishing that grammar as a standalone TextMate bundle gets tokenization (not diagnostics) for free in any other TextMate-consuming editor — Sublime Text, JetBrains IDEs via bundle import.</p><p><strong>Callout</strong>: Neovim and other tree-sitter-based editors need a <em>separate</em> grammar — tree-sitter and TextMate are different formalisms, so nothing here is reusable. That&#39;s real, standalone work this RFC does not commit WarpDrive to; it&#39;s a reasonable target for community contribution once the grammar itself has stopped changing, but not part of the initial rollout.</p><h3 id="language-server-linting-and-completion" tabindex="-1">Language server, linting, and completion <a class="header-anchor" href="#language-server-linting-and-completion" aria-label="Permalink to “Language server, linting, and completion”">​</a></h3><p>A schema-aware &quot;unknown field,&quot; &quot;duplicate filter assertion,&quot; &quot;autocomplete a relationship name from <code>Schemas</code>&quot; experience needs semantic analysis, not just tokenization. The right way to build that once, rather than N times, is a <strong>Language Server Protocol (LSP)</strong> implementation over the reference parser and its <code>Schemas</code> input — one server, thin clients for VS Code, Neovim&#39;s built-in LSP client, JetBrains via the LSP4IJ plugin, and so on. Linting (duplicate <code>(fieldPath, operator)</code> assertions, empty array literals, reserved header names, <code>@arg</code> types that don&#39;t match a field&#39;s schema type) should be diagnostics from that same analysis layer, with a thin CLI wrapper (<code>aql-lint some/**/*.aql</code>) for CI, rather than a second, separately-maintained implementation of the same schema-aware checks.</p><p><strong>Callout</strong>: this is the single biggest lift in this entire table, and this RFC explicitly does not commit to it as part of an initial rollout. Building rich editor tooling against a language half of which is still <code>[spec-only]</code> (see <a href="#detailed-design">Detailed design</a>) would mean building it twice — once now, once after the grammar actually settles. It&#39;s follow-up work, gated on the grammar stabilizing and a real implementation existing to analyze.</p><h3 id="formatting" tabindex="-1">Formatting <a class="header-anchor" href="#formatting" aria-label="Permalink to “Formatting”">​</a></h3><p>An opinionated formatter (prettier-style) for consistent <code>.aql</code> style is a reasonable long-term want.</p><p><strong>Callout</strong>: deliberately deferred, and not just for lack of time — a formatter that reformats whitespace or comments would, under this RFC&#39;s <code>q:id = sha256(source text)</code> design, silently change a persisted query&#39;s id. Building a formatter before resolving that normalization question (see <a href="#unresolved-questions">Unresolved questions</a>) would ship a tool whose entire job is to introduce the exact footgun a formatter should prevent.</p><h3 id="persisted-query-build-time-extraction" tabindex="-1">Persisted-query build-time extraction <a class="header-anchor" href="#persisted-query-build-time-extraction" aria-label="Permalink to “Persisted-query build-time extraction”">​</a></h3><p>For the persisted-query story to be useful in production, an app needs a build step that walks its source for <code>.aql</code> documents and <code>aql\`...\`</code> template literals, compiles each one, and emits a manifest (<code>q:id</code> → compiled query) a server can pre-register — the same shape of tool as the Relay compiler or <code>graphql-codegen</code>&#39;s persisted-query extraction.</p><p><strong>Callout</strong>: real and necessary for the feature to pay off in production, but explicitly out of scope for an initial rollout — it depends on this RFC&#39;s grammar being stable and on a request-builder integration existing to define what a &quot;usage site&quot; even is.</p><h3 id="type-generation-codegen" tabindex="-1">Type generation / codegen <a class="header-anchor" href="#type-generation-codegen" aria-label="Permalink to “Type generation / codegen”">​</a></h3><p>Generating TypeScript types for a <code>.aql</code> document&#39;s result shape from a <code>Schemas</code> map, analogous to <code>graphql-codegen</code>, would close the loop between the query language and WarpDrive&#39;s typed records.</p><p><strong>Callout</strong>: aspirational and, realistically, the last item on this list WarpDrive would build, if ever — its schema-driven reactive records already provide much of this value at the record level without per-query codegen, which lowers the payoff relative to everything else in this table.</p><h3 id="playground-repl-and-ci-validation" tabindex="-1">Playground / REPL and CI validation <a class="header-anchor" href="#playground-repl-and-ci-validation" aria-label="Permalink to “Playground / REPL and CI validation”">​</a></h3><p>A GraphiQL-style interactive tool — author <code>.aql</code>, see the compiled envelope and, optionally, a live response — is a natural fit for documentation and onboarding once a reference implementation exists, but doesn&#39;t gate or get gated by anything else here. A small CI-oriented CLI (<code>aql check some/**/*.aql --schema ./schema.json</code>) that validates a repo&#39;s <code>.aql</code> files against a schema and exits non-zero on the same errors <code>parseAQL</code> throws is a realistic near-term deliverable — a thin wrapper over the reference parser, buildable as soon as the grammar and <code>Schemas</code> format in this RFC stabilize.</p><h2 id="drawbacks" tabindex="-1">Drawbacks <a class="header-anchor" href="#drawbacks" aria-label="Permalink to “Drawbacks”">​</a></h2><ul><li>This adds a whole new DSL — grammar, parser, VS Code tooling — to maintain and teach, on top of plain JSON:API&#39;s own learning curve.</li><li>The filter operator vocabulary is a specific, opinionated choice (closer to SQL/Sequelize&#39;s operator set than, say, OData&#39;s or a GraphQL resolver&#39;s own filter conventions). Backends with a fundamentally different filter philosophy gain nothing from the vocabulary matching theirs and still need a full adapter, same as any other non-conforming backend.</li><li>This entire RFC is specified ahead of any implementation — <code>data</code>/<code>filter</code>/<code>page</code>/<code>@arg</code> were at least prototyped during design exploration (see <a href="#current-implementation-status">Current implementation status</a>); <code>headers</code> and multi-entry never were. Shipping a spec this far ahead of code risks it drifting from whatever an eventual implementation actually needs, or the gap simply never getting closed.</li><li><code>@arg</code> living inline inside <code>filter</code>/<code>page</code>/<code>headers</code> values, rather than in one centralized declarations block, scatters a persisted query&#39;s variable declarations through the document — arguably harder to see &quot;what does this query need at call time&quot; at a glance than a Relay-style upfront variable list would be.</li><li>A persisted query&#39;s <code>q:id</code> is a straight sha256 of its source text, so a whitespace- or comment-only edit changes the id with no normalization step defined to prevent that.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to “Alternatives”">​</a></h2><ul><li><strong>GraphQL.</strong> Solves multi-entry batching, field selection, and (via resolvers) arbitrary filtering natively — but abandons JSON:API&#39;s REST/URL-addressable-resource model entirely. Not a drop-in for an existing JSON:API backend, which is specifically the population AQL targets.</li><li><strong>Status quo: hand-written JSON query bodies.</strong> What building a <code>QUERY</code> request body looks like today, with no terse authoring syntax, no schema-checked field/relationship names, and no persisted-query story.</li><li><strong>OData <code>$filter</code> expressions.</strong> A mature, standardized alternative filter expression syntax. Considered and rejected specifically for AQL&#39;s filter shape, to stay JSON:API-native and match the URL-bracket convention already common among JSON:API-flavored backends rather than adopting an unrelated ecosystem&#39;s expression language wholesale.</li><li><strong>A separate persisted-query registration step</strong> (Relay-style, registering queries out of band rather than deriving <code>q:id</code> from source text inline). Rejected for now to keep a query and its variable declarations as one artifact; worth revisiting if the multi-entry <code>q:args</code> name-conflict problem turns out to need a registry regardless.</li></ul><h2 id="unresolved-questions" tabindex="-1">Unresolved questions <a class="header-anchor" href="#unresolved-questions" aria-label="Permalink to “Unresolved questions”">​</a></h2><ul><li><strong>OR / grouped boolean filter logic.</strong> No syntax exists; any future design should be additive (a new, distinct block or key) rather than changing what a bare <code>filter{}</code> block means today.</li><li><strong>Filtering a related collection separately from the primary result set.</strong> There&#39;s no syntax for &quot;filter the <code>comments</code> on this <code>post</code>, as opposed to filtering which <code>posts</code> come back&quot; — disambiguating those two intents needs its own design.</li><li><strong>A <code>search</code> (full-text/opaque) parameter</strong>, as a sibling top-level statement rather than something inside <code>filter{}</code>, since it isn&#39;t a <code>(fieldPath, operator, value)</code> triple.</li><li><strong>Multi-entry partial failure semantics</strong> — whole-request failure vs. a per-entry error slot.</li><li><strong>Multi-entry cross-entry dependencies</strong> — out of scope as &quot;a meaningfully harder feature than batching independent queries,&quot; but not permanently ruled out.</li><li><strong><code>q:args</code> name-conflict resolution across multi-entry entries</strong> that declare the same variable name for logically different things — full-path disambiguation (<code>q:args[entries.&lt;name&gt;.filter.&lt;field&gt;.&lt;operator&gt;]</code>) is the obvious answer but isn&#39;t specified.</li><li><strong>Persisted-query id normalization</strong> — should whitespace/comment-only differences between two <code>.aql</code> files that are otherwise identical hash to the same <code>q:id</code>?</li></ul>`,86)])}var f=a(l,[[`render`,d]]);export{c as __pageData,f as default};
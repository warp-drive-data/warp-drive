---
url: >-
  https://canary.warp-drive.io/pr-preview/pr-11303/api/@warp-drive/legacy/adapter/variables/BuildURLMixin.md
description: >-
  Legacy `Mixin` that adds `buildURL` and the `urlFor*` hooks to an adapter so
  it can turn a model name, id, snapshot, and request type into a request URL.
---

&#x20;

# &#x20;BuildURLMixin

```ts
BuildURLMixin: Mixin;
```

Defined in: [warp-drive-packages/legacy/src/adapter/-private/build-url-mixin.ts:29](https://github.com/warp-drive-data/warp-drive/blob/6a0f52f3db568b4389b2d95a71d94f0ddfce2d26/warp-drive-packages/legacy/src/adapter/-private/build-url-mixin.ts#L29)

The `Mixin` implementation of BuildURLMixin, ready to be applied
to an `EmberObject` subclass such as an `Adapter`.

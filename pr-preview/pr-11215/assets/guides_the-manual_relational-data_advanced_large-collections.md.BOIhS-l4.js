import{at as e,it as t,t as n,yt as r}from"./chunks/framework.BXnsBMxG.js";var i=JSON.parse(`{"title":"Large Collections","description":"","frontmatter":{},"headers":[],"relativePath":"guides/the-manual/relational-data/advanced/large-collections.md","filePath":"guides/the-manual/relational-data/advanced/large-collections.md"}`),a={name:`guides/the-manual/relational-data/advanced/large-collections.md`};function o(n,i,a,o,s,c){return r(),t(`div`,null,[...i[0]||=[e(`<h1 id="large-collections" tabindex="-1">Large Collections <a class="header-anchor" href="#large-collections" aria-label="Permalink to “Large Collections”">​</a></h1><p>A <code>collection</code> relationship holds the complete membership of a relationship and materializes a record for every member when accessed. That is exactly right for a post&#39;s tags or a user&#39;s addresses. It is the wrong tool for a post&#39;s comments, a user&#39;s activity history, or anything a user would expect to scroll, search or sort.</p><p>This page explains the boundary, how WarpDrive can enforce it for you, and what to do instead.</p><h2 id="why-not-load-everything-through-the-relationship" tabindex="-1">Why Not Load Everything Through The Relationship? <a class="header-anchor" href="#why-not-load-everything-through-the-relationship" aria-label="Permalink to “Why Not Load Everything Through The Relationship?”">​</a></h2><ul><li><strong>Every payload replaces the whole membership.</strong> If the API sends a partial list, the relationship silently becomes that partial list.</li><li><strong>Relationships do not paginate, sort or filter.</strong> <code>next</code>/<code>prev</code> links on a relationship are surfaced but never followed into <code>data</code>.</li><li><strong>Cost scales with the parent.</strong> Loading a user means loading every identifier in every collection on that user, and every inverse edge for each of them.</li><li><strong>The list is a query, not a fact.</strong> &quot;Comments on this post, newest first, approved only, page 3&quot; is a request; &quot;this comment belongs to this post&quot; is a relationship.</li></ul><h2 id="the-rule-of-thumb" tabindex="-1">The Rule Of Thumb <a class="header-anchor" href="#the-rule-of-thumb" aria-label="Permalink to “The Rule Of Thumb”">​</a></h2><p>Ask: <em>would a user ever want to page, sort or filter this list?</em> If yes, load it with a <a href="./pagination">top-level request</a> and keep the relationship for bookkeeping (usually via the <a href="./../features/inverses">inverse</a> on the many side, e.g. <code>comment.post</code>). If no, and the list is bounded by the domain (a handful to a few dozen members), a <code>collection</code> relationship is a good fit.</p><h2 id="enforcing-it-maxcollectionrelationshipsize" tabindex="-1">Enforcing It: <code>maxCollectionRelationshipSize</code> <a class="header-anchor" href="#enforcing-it-maxcollectionrelationshipsize" aria-label="Permalink to “Enforcing It: maxCollectionRelationshipSize”">​</a></h2><p>WarpDrive can tell you when a <code>collection</code> relationship receives more members than you intended. Configure a limit on the store:</p><div class="vp-code-block-title"><div class="vp-code-block-title-bar"><span class="vp-code-block-title-text" data-title="services/store.ts">services/store.ts</span></div><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { useRecommendedStore } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@warp-drive/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { JSONAPICache } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@warp-drive/json-api&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Store</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> useRecommendedStore</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  cache: JSONAPICache,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  maxCollectionRelationshipSize: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div></div><p>Or on a custom Store subclass:</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AppStore</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> extends</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Store</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  maxCollectionRelationshipSize</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>When any remote payload for a <code>collection</code> field carries a <code>data</code> array longer than the limit, WarpDrive throws an error <strong>asynchronously</strong> (from a microtask) after the payload has been applied:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>Error: The collection relationship post.comments received 250 related resources, exceeding the</span></span>
<span class="line"><span>configured maxCollectionRelationshipSize of 100. Large collections should be loaded with a</span></span>
<span class="line"><span>top-level (paginated) request instead of through the relationship.</span></span></code></pre></div><p>Throwing asynchronously means the cache is never left half-updated and the request that carried the payload still resolves, while the error still reaches <code>window.onerror</code>, your error reporter, and fails tests. Treat it as a signal that the API response or the schema should change — usually by removing the relationship&#39;s <code>data</code> from the parent payload and exposing the list as its own endpoint.</p><h3 id="what-the-check-is-and-isn-t" tabindex="-1">What The Check Is (And Isn&#39;t) <a class="header-anchor" href="#what-the-check-is-and-isn-t" aria-label="Permalink to “What The Check Is (And Isn&#39;t)”">​</a></h3><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td>Counts</td><td>the length of <code>data</code> in <strong>one</strong> relationship payload</td></tr><tr><td>Applies to</td><td><code>collection</code> fields only — not legacy <code>hasMany</code>, not <code>resource</code></td></tr><tr><td>Triggers on</td><td>remote updates (payloads pushed into the cache)</td></tr><tr><td>Does not trigger on</td><td>local mutations (<code>data.push(...)</code>), or payloads without a <code>data</code> key</td></tr><tr><td>Rejects the payload?</td><td>no — the data is applied, then the error is thrown</td></tr><tr><td>Default</td><td>off (<code>null</code>)</td></tr></tbody></table><p>The limit is not a byte size and does not consider how many members the relationship already had; it is a per-payload cardinality check. Because inverses are updated from each side&#39;s own payload, a large list will typically trip the check on the parent&#39;s payload even if the many side is loaded in pages.</p><h3 id="choosing-a-value" tabindex="-1">Choosing A Value <a class="header-anchor" href="#choosing-a-value" aria-label="Permalink to “Choosing A Value”">​</a></h3><p>Set the limit to the page size you would use if the list were paginated in your UI. Most apps land between 50 and 200. A limit that is far higher than any real relationship is harmless; a limit that is too low turns legitimate relationships into noise. Start higher and tighten as you learn which relationships in your API are actually bounded.</p><h2 id="valid-relationship-payloads-summarized" tabindex="-1">Valid Relationship Payloads, Summarized <a class="header-anchor" href="#valid-relationship-payloads-summarized" aria-label="Permalink to “Valid Relationship Payloads, Summarized”">​</a></h2><p>For reference, these are the payload shapes for a <code>collection</code> relationship and how WarpDrive treats each (the normative list is in the <a href="./../spec">specification</a>):</p><table tabindex="0"><thead><tr><th>Payload</th><th><code>async: false</code></th><th><code>async: true</code></th></tr></thead><tbody><tr><td><code>{ data: [ids...] }</code> + members included</td><td>✅ complete membership</td><td>❌ <code>links.related</code> required</td></tr><tr><td><code>{ links: { related }, data: [ids...] }</code> + members included</td><td>⚠️ links are unnecessary (warning)</td><td>✅ complete membership, re-fetchable</td></tr><tr><td><code>{ links: { related } }</code></td><td>❌ <code>data</code> required</td><td>✅ membership unknown; <code>doc.data</code> is <code>undefined</code> until fetched</td></tr><tr><td><code>{ data: [] }</code></td><td>✅ known to be empty</td><td>❌ <code>links.related</code> required</td></tr><tr><td><code>{ meta: {...} }</code> alone</td><td>❌ <code>data</code> required</td><td>❌ <code>links.related</code> required</td></tr><tr><td><code>{ data: [ids...] }</code> with a member <strong>not</strong> included</td><td>❌ reading <code>doc.data</code> throws</td><td>❌ reading <code>doc.data</code> throws</td></tr><tr><td><code>{ ..., links: { next, prev } }</code></td><td>pagination links are exposed on <code>doc.links</code> but never followed into <code>data</code></td><td>same</td></tr></tbody></table><p>For a <code>resource</code> relationship replace the array with a single identifier or <code>null</code>. The same rules apply, minus the size check.</p>`,24)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};
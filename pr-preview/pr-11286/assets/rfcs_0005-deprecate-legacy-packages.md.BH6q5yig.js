import{Ct as e,at as t,it as n,ot as r,st as i,t as a,tt as o,yt as s}from"./chunks/framework.Baq27OmJ.js";var c=JSON.parse(`{"title":"Deprecating the Legacy ember-data Packages and @warp-drive/core-types","description":"Proposes deprecating the ember-data package, every @ember-data/* package, and @warp-drive/core-types now, in favor of their already-shipping @warp-drive/* successors, with removal from publishing targeted for 6.0.","frontmatter":{"title":"Deprecating the Legacy ember-data Packages and @warp-drive/core-types","description":"Proposes deprecating the ember-data package, every @ember-data/* package, and @warp-drive/core-types now, in favor of their already-shipping @warp-drive/* successors, with removal from publishing targeted for 6.0.","warp-drive-rfc":5,"emberjs-rfc":null,"emberjs-pr":null,"emberjs-branch":null,"sync-hash":null,"stage":"proposed","start-date":"2026-09-25T00:00:00.000Z","release-date":null,"release-versions":null,"teams":["data","learning"],"prs":{"accepted":null},"project-link":null,"suite":null},"headers":[],"relativePath":"rfcs/0005-deprecate-legacy-packages.md","filePath":"rfcs/0005-deprecate-legacy-packages.md"}`),l={name:`rfcs/0005-deprecate-legacy-packages.md`},u={id:`deprecating-the-legacy-ember-data-packages-and-warp-drive-core-types`,tabindex:`-1`};function d(a,c,l,d,f,p){let m=e(`StatusBadge`);return s(),n(`div`,null,[o(`h1`,u,[c[0]||=r(`Deprecating the Legacy ember-data Packages and @warp-drive/core-types `,-1),i(m,{variant:`proposed`,text:`Proposed`}),c[1]||=r(),c[2]||=o(`a`,{class:`header-anchor`,href:`#deprecating-the-legacy-ember-data-packages-and-warp-drive-core-types`,"aria-label":`Permalink to “Deprecating the Legacy ember-data Packages and @warp-drive/core-types”`},`​`,-1)]),c[3]||=t(`<h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to “Summary”">​</a></h2><p>The <code>ember-data</code> package, every <code>@ember-data/*</code> package (<code>active-record</code>, <code>adapter</code>, <code>codemods</code>, <code>debug</code>, <code>graph</code>, <code>json-api</code>, <code>legacy-compat</code>, <code>model</code>, <code>request</code>, <code>request-utils</code>, <code>rest</code>, <code>serializer</code>, <code>store</code>, <code>tracking</code>), and <code>@warp-drive/core-types</code> are deprecated starting in the next 5.x minor, with removal from npm publishing at 6.0. Most of these are already, today, a thin re-export shim over a <code>@warp-drive/*</code> package introduced by the package-unification effort (<a href="https://rfcs.emberjs.com/id/1075-warp-drive-package-unification/" target="_blank" rel="noreferrer">emberjs/rfcs#1075</a>) — <code>@warp-drive/core</code>, <code>@warp-drive/legacy</code>, <code>@warp-drive/utilities</code>, <code>@warp-drive/json-api</code>, and <code>@warp-drive/ember</code>. The two exceptions get a successor as part of this RFC rather than reusing an existing shim: <code>@ember-data/debug</code>&#39;s ember-inspector integration is superseded by the WarpDrive DevTools browser extension proposed in <a href="./0003-warp-drive-devtools-extension">RFC 0003</a>, and <code>@ember-data/codemods</code> is renamed to <code>@warp-drive/codemods</code>. For the rest, this RFC does not move any logic; it formalizes the deprecation of the old import paths that RFC 1075 already implied, with concrete deprecation flags/ids and documentation updates, landing now, on the same <code>since &lt;5.x&gt;</code> / <code>until 6.0</code> timeline every other active deprecation in <code>deprecations.ts</code> already uses. The mechanical import rewrite itself needs no new tooling: <code>eslint-plugin-warp-drive</code>&#39;s <code>no-legacy-imports</code> rule already autofixes exactly this rename, and already ships enabled in its <code>recommended</code> config.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to “Motivation”">​</a></h2><p>RFC 1075 unified WarpDrive&#39;s packages so that an app installs <code>@warp-drive/core</code> and a small number of framework/feature packages instead of assembling a dozen <code>@ember-data/*</code> packages by hand. That work is done: every legacy package&#39;s <code>src/index.ts</code> is now nothing but a re-export. For example:</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// packages/store/src/index.ts</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Store </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@warp-drive/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// packages/core-types/src/index.ts</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> *</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@warp-drive/core/types&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>Despite this, the legacy packages are still published, versioned in lockstep with everything else, documented as a first-class install path (<code>guides/configuration/legacy-package-setup</code> still lists <code>@ember-data/store</code>, <code>@ember-data/request</code>, <code>@warp-drive/core-types</code>, etc. as &quot;the current setup&quot;), and tested as if they were independent implementations. Carrying two import paths to the same code indefinitely means:</p><ul><li><strong>Doubled documentation and onboarding surface.</strong> New users following the legacy setup guide learn a five-to-fourteen-package install command for functionality that <code>@warp-drive/core</code> (plus at most <code>@warp-drive/ember</code>) already provides in one or two packages.</li><li><strong>A confusing dependency graph for tooling.</strong> Bundlers, TypeScript, and vite in particular handle the <code>ember-data</code> meta-package&#39;s automatic dependency bundling poorly (already called out in the legacy setup guide&#39;s own &quot;What about the <code>ember-data</code> package?&quot; section) — the fix has existed since RFC 1075 shipped, but nothing tells an app it should stop relying on the meta-package.</li><li><strong>No signal that the shims are temporary.</strong> <code>@warp-drive/core-types</code>&#39;s package description already says <code>(Legacy)</code>, but nothing else — no build-time warning, no npm deprecation notice, no migration guide — tells a consumer that importing from <code>@ember-data/model</code> instead of <code>@warp-drive/legacy/model</code> is a choice with an expiration date.</li></ul><p>The expected outcome: starting in the next 5.x minor, every consumer importing from a legacy package sees a clear, actionable deprecation pointing at the exact <code>@warp-drive/*</code> replacement import, with <code>eslint --fix</code> performing the rewrite mechanically via the already-shipping <code>no-legacy-imports</code> rule; at 6.0, WarpDrive stops publishing new versions of the legacy packages, and the legacy setup guide is replaced entirely by the unified one.</p><h2 id="detailed-design" tabindex="-1">Detailed design <a class="header-anchor" href="#detailed-design" aria-label="Permalink to “Detailed design”">​</a></h2><h3 id="what-s-covered-and-what-it-maps-to" tabindex="-1">What&#39;s covered and what it maps to <a class="header-anchor" href="#what-s-covered-and-what-it-maps-to" aria-label="Permalink to “What&#39;s covered and what it maps to”">​</a></h3><p>Every legacy package&#39;s replacement — a re-export target confirmed by reading its <code>src/index.ts</code> for most of them, or a successor this RFC introduces for the two that don&#39;t have one yet:</p><table tabindex="0"><thead><tr><th>Legacy package</th><th>Deprecated now, removed at 6.0</th><th>Replacement</th></tr></thead><tbody><tr><td><code>ember-data</code></td><td>Yes</td><td><code>@warp-drive/core</code> (+ <code>@warp-drive/ember</code> for Ember apps, <code>@warp-drive/legacy</code> for Model/Adapter/Serializer)</td></tr><tr><td><code>@warp-drive/core-types</code></td><td>Yes</td><td><code>@warp-drive/core/types</code></td></tr><tr><td><code>@ember-data/store</code></td><td>Yes</td><td><code>@warp-drive/core</code></td></tr><tr><td><code>@ember-data/graph</code></td><td>Yes</td><td><code>@warp-drive/core</code> (<code>/graph</code>)</td></tr><tr><td><code>@ember-data/request</code></td><td>Yes</td><td><code>@warp-drive/core/request</code></td></tr><tr><td><code>@ember-data/request-utils</code></td><td>Yes</td><td><code>@warp-drive/utilities</code> (<code>/handlers</code>, <code>/string</code>)</td></tr><tr><td><code>@ember-data/json-api</code></td><td>Yes</td><td><code>@warp-drive/json-api</code></td></tr><tr><td><code>@ember-data/model</code></td><td>Yes</td><td><code>@warp-drive/legacy/model</code></td></tr><tr><td><code>@ember-data/adapter</code></td><td>Yes</td><td><code>@warp-drive/legacy/adapter</code></td></tr><tr><td><code>@ember-data/serializer</code></td><td>Yes</td><td><code>@warp-drive/legacy/serializer</code></td></tr><tr><td><code>@ember-data/legacy-compat</code></td><td>Yes</td><td><code>@warp-drive/legacy/compat</code></td></tr><tr><td><code>@ember-data/rest</code></td><td>Yes</td><td><code>@warp-drive/utilities/rest</code> + <code>@warp-drive/legacy</code> (<code>/adapter/rest</code>, <code>/serializer/rest</code>)</td></tr><tr><td><code>@ember-data/active-record</code></td><td>Yes</td><td><code>@warp-drive/utilities/active-record</code></td></tr><tr><td><code>@ember-data/tracking</code></td><td>Already deprecated (<code>DEPRECATE_TRACKING_PACKAGE</code>, since 5.5, until 6.0)</td><td><code>@warp-drive/ember/install</code></td></tr><tr><td><code>@ember-data/debug</code></td><td>Yes</td><td>the WarpDrive DevTools browser extension (<a href="./0003-warp-drive-devtools-extension">RFC 0003</a>) — not a code change, see &quot;Packages without an import-path replacement&quot;</td></tr><tr><td><code>@ember-data/codemods</code></td><td>Yes</td><td><code>@warp-drive/codemods</code> (new package; a rename, not an absorption — see &quot;Packages without an import-path replacement&quot;)</td></tr></tbody></table><p><code>@ember-data/tracking</code> already has a resolved deprecation story under <code>DISABLE_7X_DEPRECATIONS</code>&#39;s sibling flags and is unaffected by this RFC beyond being folded into the same messaging pass.</p><h3 id="deprecation-flags" tabindex="-1">Deprecation flags <a class="header-anchor" href="#deprecation-flags" aria-label="Permalink to “Deprecation flags”">​</a></h3><p>Three new flags are added to <code>warp-drive-packages/build-config/src/deprecations.ts</code>, following the existing shape (<code>DEPRECATE_TRACKING_PACKAGE</code>, <code>DEPRECATE_EMBER_INFLECTOR</code>, etc.):</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">/**</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * &lt;Badge type=&quot;warning&quot; text=&quot;warp-drive:deprecate-core-types-package&quot; /&gt;</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> *</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * Deprecates \`@warp-drive/core-types\`, which has been a pure type re-export</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * of \`@warp-drive/core/types\` since its unification. Import types directly</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * from \`@warp-drive/core/types\` instead.</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> *</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">@since</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> 5.11</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">@until</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> 6.0</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">@public</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> DEPRECATE_CORE_TYPES_PACKAGE</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">/**</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * &lt;Badge type=&quot;warning&quot; text=&quot;warp-drive:deprecate-ember-data-packages&quot; /&gt;</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> *</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * Deprecates the \`ember-data\` package and every \`@ember-data/*\` package</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * (\`active-record\`, \`adapter\`, \`codemods\`, \`graph\`, \`json-api\`,</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * \`legacy-compat\`, \`model\`, \`request\`, \`request-utils\`, \`rest\`,</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * \`serializer\`, \`store\`). Each is either a re-export shim over a</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * \`@warp-drive/*\` package, or (for \`codemods\`) renamed outright to one;</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * import from or install the \`@warp-drive/*\` package directly instead.</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> *</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">@since</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> 5.11</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">@until</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> 6.0</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">@public</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> DEPRECATE_EMBER_DATA_PACKAGES</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">/**</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * &lt;Badge type=&quot;warning&quot; text=&quot;warp-drive:deprecate-ember-data-debug-package&quot; /&gt;</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> *</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * Deprecates \`@ember-data/debug\`&#39;s ember-inspector integration in favor of</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * the WarpDrive DevTools browser extension. Unlike the other flags in this</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * group, there is no import to rewrite: uninstall \`@ember-data/debug\` and</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * install the browser extension instead.</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> *</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">@since</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> 5.11</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">@until</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> 6.0</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> * </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">@public</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"> */</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> DEPRECATE_EMBER_DATA_DEBUG_PACKAGE</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p><code>DEPRECATE_EMBER_DATA_DEBUG_PACKAGE</code> is split out from the rest because its resolution isn&#39;t an import rewrite — it&#39;s a tool swap, so it gets its own message and isn&#39;t a candidate for the <code>eslint-plugin-warp-drive</code> autofix described below. The other two flags stay coarse-grained (one for <code>core-types</code>, one for the whole <code>ember-data</code> import-rewrite family) rather than one flag per package, matching how the legacy setup guide already treats the family as a single install decision (&quot;What about the <code>ember-data</code> package?&quot;). A dozen near-identical per-package flags would multiply the deprecation surface without giving apps a meaningfully different way to resolve them — the fix is the same import rewrite in every case.</p><h3 id="runtime-warning" tabindex="-1">Runtime warning <a class="header-anchor" href="#runtime-warning" aria-label="Permalink to “Runtime warning”">​</a></h3><p>Each legacy package&#39;s shim entrypoint gains a one-time <code>deprecate()</code> call, guarded by its flag, following the same shape as <code>packages/store/src/index.ts</code>&#39;s existing <code>DEPRECATE_TRACKING_PACKAGE</code> check:</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">DEPRECATE_EMBER_DATA_PACKAGES</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  deprecate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    \`Importing from &#39;@ember-data/model&#39; is deprecated. Import from &#39;@warp-drive/legacy/model&#39; \`</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> +</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      \`instead. Enable eslint-plugin-warp-drive&#39;s recommended config and run </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\\`</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">eslint --fix</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\\`</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> \`</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> +</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      \`to update your imports automatically.\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;warp-drive.deprecate-ember-data-packages&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      until: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;6.0.0&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      for: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;warp-drive&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      since: { enabled: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;5.11.0&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, available: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;5.11.0&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      url: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;https://deprecations.emberjs.com/id/warp-drive.deprecate-ember-data-packages&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  );</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>@warp-drive/core-types</code>&#39;s shim gets the equivalent call under <code>DEPRECATE_CORE_TYPES_PACKAGE</code> with <code>id: &#39;warp-drive.deprecate-core-types-package&#39;</code>. <code>@ember-data/debug</code> gets its own message under <code>DEPRECATE_EMBER_DATA_DEBUG_PACKAGE</code>, pointing at the extension instead of an import:</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">DEPRECATE_EMBER_DATA_DEBUG_PACKAGE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  deprecate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    \`&#39;@ember-data/debug&#39; is deprecated. Uninstall it and install the WarpDrive DevTools \`</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> +</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      \`browser extension instead: https://docs.warp-drive.io/guides/devtools\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;warp-drive.deprecate-ember-data-debug-package&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      until: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;6.0.0&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      for: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;warp-drive&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      since: { enabled: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;5.11.0&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, available: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;5.11.0&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      url: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;https://deprecations.emberjs.com/id/warp-drive.deprecate-ember-data-debug-package&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  );</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>All three flags default to <code>true</code> (the deprecated behavior is active) exactly like every other flag in <code>deprecations.ts</code>, so opting out early — silencing the warning as soon as it ships — is not offered; unlike a behavior deprecation, there&#39;s no &quot;not yet migrated&quot; code path to keep alive here, only an import path (or, for <code>debug</code>, a tool) to change.</p><h3 id="npm-level-deprecation" tabindex="-1">npm-level deprecation <a class="header-anchor" href="#npm-level-deprecation" aria-label="Permalink to “npm-level deprecation”">​</a></h3><p>Every legacy package&#39;s <code>package.json</code> <code>description</code> is updated to lead with <code>(Legacy)</code> (already true for <code>@warp-drive/core-types</code>; not yet true for the <code>@ember-data/*</code> family or <code>ember-data</code> itself), and at the same 5.x minor that ships the flags above, each package&#39;s npm registry entry gets an <code>npm deprecate</code> message pointing at the migration guide (see &quot;How we teach this&quot;). This is metadata only — <code>npm install</code> continues to work unchanged for every version published through the last 5.x release; the notice surfaces immediately in <code>npm install</code> output and on the npmjs.com package page, well ahead of the 6.0 removal.</p><h3 id="automated-migration" tabindex="-1">Automated migration <a class="header-anchor" href="#automated-migration" aria-label="Permalink to “Automated migration”">​</a></h3><p>No new tooling needs to be built for the mechanical import rewrite. <code>eslint-plugin-warp-drive</code> already ships <code>no-legacy-imports</code> (<code>warp-drive/no-legacy-imports</code>), an autofixable rule enabled by default in its <code>recommended</code> config, driven by a generated, export-level mapping table (<code>public-exports-mapping-5.5.enriched.json</code>) that already covers every re-export-shim package in the table above — including the <code>rest</code>/<code>active-record</code> split, since the mapping is per-export, not per-module (e.g. <code>import { findRecord } from &#39;@ember-data/rest/request&#39;</code> already rewrites to <code>import { findRecord } from &#39;@warp-drive/utilities/rest&#39;</code>). Running <code>eslint --fix</code> with <code>recommended</code> enabled is the actual migration path this RFC relies on for those packages. The mapping table gains one more entry for this RFC: <code>@ember-data/codemods</code>&#39;s exports routed to <code>@warp-drive/codemods</code>, the same as any other renamed module specifier.</p><p>Two gaps this doesn&#39;t cover:</p><ul><li>The rule only rewrites static <code>import</code> declarations. Namespace imports, <code>export * from</code> re-exports, CommonJS <code>require</code>, and dynamic <code>import()</code> are flagged without an autofix and need manual migration — worth stating plainly in the migration guide rather than implying <code>eslint --fix</code> alone finishes the job for every app. An app must also already have <code>eslint-plugin-warp-drive</code>&#39;s <code>recommended</code> config enabled to get the autofix at all; the migration guide should call out enabling it as step one for apps that haven&#39;t adopted it yet.</li><li><code>@ember-data/codemods</code>&#39;s CLI invocation (<code>npx @ember-data/codemods ...</code>) isn&#39;t a JS import and isn&#39;t touched by the lint rule; switching it to <code>npx @warp-drive/codemods ...</code> is a manual, one-line change called out in the deprecation message and migration guide.</li><li><code>@ember-data/debug</code> has no autofix at all, covered next.</li></ul><h3 id="packages-without-an-import-path-replacement" tabindex="-1">Packages without an import-path replacement <a class="header-anchor" href="#packages-without-an-import-path-replacement" aria-label="Permalink to “Packages without an import-path replacement”">​</a></h3><p>Two packages&#39; replacements aren&#39;t &quot;import from a different specifier,&quot; so they don&#39;t fit the re-export-shim story the rest of this RFC relies on:</p><ul><li><strong><code>@ember-data/debug</code></strong> provides the Ember Inspector data adapter. Its replacement is the WarpDrive DevTools browser extension proposed in <a href="./0003-warp-drive-devtools-extension">RFC 0003</a> — a different tool entirely, installed in the browser rather than imported in code. RFC 0003 itself, as drafted, describes the two coexisting indefinitely (&quot;<code>@ember-data/debug</code>&#39;s docs get a note that it remains the ember-inspector integration for <code>Model</code>-based apps&quot;); this RFC instead treats the extension as <code>@ember-data/debug</code>&#39;s full replacement and deprecates the package on the same timeline as everything else here. That means this RFC&#39;s <code>@ember-data/debug</code> deprecation is contingent on RFC 0003 landing — the deprecation message has nothing to point at otherwise.</li><li><strong><code>@ember-data/codemods</code></strong> has no <code>@warp-drive/*</code> successor today; this RFC creates one by renaming it to <code>@warp-drive/codemods</code> — moving <code>packages/codemods</code> under the new name and leaving <code>@ember-data/codemods</code> as a deprecated re-export/CLI shim over it, the same pattern every other package in the table already follows, just newly created rather than pre-existing.</li></ul><h3 id="timeline" tabindex="-1">Timeline <a class="header-anchor" href="#timeline" aria-label="Permalink to “Timeline”">​</a></h3><ol><li><strong>Next 5.x minor:</strong> all three flags ship (default <code>true</code>); the runtime warnings and npm deprecation metadata ship together (the <code>eslint-plugin-warp-drive</code> autofix already exists today, and gains the new <code>@ember-data/codemods</code> mapping entry). <code>@warp-drive/codemods</code> is published for the first time, and <code>ember-data</code>, every <code>@ember-data/*</code> package, and <code>@warp-drive/core-types</code> show a deprecation on install and on first import — <code>@ember-data/debug</code> only once <a href="./0003-warp-drive-devtools-extension">RFC 0003</a>&#39;s extension has landed.</li><li><strong>Remaining 5.x betas/minors:</strong> the warning and the <code>eslint --fix</code> autofix are the primary support surface; no further behavior change. This is the entire migration window — it ends at the next major.</li><li><strong>6.0:</strong> WarpDrive stops publishing new versions of the deprecated packages. Versions already published through the last 5.x release remain installable indefinitely (npm does not support retracting published versions), so apps that never migrate are not broken outright — they simply stop receiving fixes, security patches, and compatibility updates for those packages from 6.0 onward.</li></ol><p>This mirrors the <code>since &lt;5.x&gt;</code> / <code>until 6.0</code> shape already used by every other active flag in <code>deprecations.ts</code> (<code>DEPRECATE_TRACKING_PACKAGE</code>, <code>DEPRECATE_EMBER_INFLECTOR</code>, and the rest) — this RFC&#39;s flags resolve on the same release boundary, rather than opening a new post-6.0 deprecation window the way a behavior flag typically would. The difference from a behavior flag is <em>what</em> &quot;resolving&quot; means: there&#39;s no source inside a still-shipping package to delete at 6.0, because the deprecated thing is the package&#39;s own continued publication, not code inside a package that keeps shipping. The &quot;removal&quot; is that the legacy packages&#39; own releases stop.</p><h2 id="how-we-teach-this" tabindex="-1">How we teach this <a class="header-anchor" href="#how-we-teach-this" aria-label="Permalink to “How we teach this”">​</a></h2><ul><li><code>guides/configuration/legacy-package-setup/index.md</code> gets a deprecation banner at the top (the page already carries a &quot;Boilerplate Sucks&quot; callout pointing at RFC 1075; this RFC extends that callout to state the deprecation is immediate and end-of-publishing lands at 6.0, plainly and with a date once one is set) and its package-list code blocks get inline notes next to each deprecated package naming its replacement.</li><li>A new <code>upgrading/v6/</code> page (alongside the existing <code>upgrading/v5/</code>) documents the full mapping table from &quot;Detailed design&quot; as the canonical migration reference, plus how to enable <code>eslint-plugin-warp-drive</code>&#39;s <code>recommended</code> config and run <code>eslint --fix</code>.</li><li>The runtime deprecation message itself (see &quot;Runtime warning&quot;) is the primary channel most developers see this through — it names the exact replacement import and the exact autofix command, no lookup required.</li><li>This is taught as &quot;finishing package unification,&quot; not as a new idea: RFC 1075 is where users already learned that <code>@warp-drive/core</code> is the way forward. This RFC&#39;s messaging should link back to it rather than introduce new terminology.</li><li><code>@ember-data/debug</code>&#39;s deprecation message and migration-guide entry link to RFC 0003&#39;s devtools extension docs, not to an import-path change — it should read plainly as &quot;switch tools,&quot; not as a variant of the same &quot;change this import&quot; instruction every other row gets.</li></ul><h2 id="drawbacks" tabindex="-1">Drawbacks <a class="header-anchor" href="#drawbacks" aria-label="Permalink to “Drawbacks”">​</a></h2><ul><li><strong>The migration window is short.</strong> Deprecating now with removal at the very next major gives apps only the remainder of the current 5.x line to migrate, rather than a full major&#39;s worth of beta/minor cycles. Every other flag in <code>deprecations.ts</code> gets that same window in principle (<code>since 5.x</code> / <code>until 6.0</code>), but most of them were introduced earlier in the 5.x line than this RFC lands — apps adopting this deprecation late in 5.x genuinely have less time than apps that picked up, say, <code>DEPRECATE_TRACKING_PACKAGE</code> at 5.5. The existing <code>eslint-plugin-warp-drive</code> autofix is not optional polish here; it is load-bearing for apps to make this window — and it only helps apps that have already adopted the plugin&#39;s <code>recommended</code> config, which not every app has.</li><li><strong>Two of the three flags are coarse-grained</strong> rather than one per package, so an app can&#39;t resolve the deprecation for, say, just <code>@ember-data/model</code> while keeping the warning active for <code>@ember-data/rest</code> — it resolves the whole <code>ember-data</code> import-rewrite family at once. This trades precision for a simpler mental model matching how the packages are already documented and installed.</li><li><strong><code>ember-data</code> is the simplest onboarding path today</strong> for tutorials and small test apps that don&#39;t want to reason about which of five-to-fourteen packages to install. Deprecating it raises the bar for a &quot;just try it&quot; first install, unless the unified <code>@warp-drive/core</code> install path is brought to at least the same one-command simplicity before 6.0 ships (tracked by the existing <code>@warp-drive/core</code> setup docs, not by this RFC).</li><li><strong>6.0 is a lifecycle boundary for two unrelated deprecations at once.</strong> This RFC&#39;s packages finish their deprecation and stop publishing at 6.0; <a href="./0002-warp-drive-build-plugin">RFC 0002</a>&#39;s babel build path only <em>begins</em> its formal deprecation at 6.0 (removed later, at 7.0). An app reading both at the 6.0 boundary needs to understand it&#39;s losing the legacy packages outright while merely being warned about the babel path — the two migration guides should say this explicitly rather than let the shared version number imply a shared timeline.</li><li><strong><code>@ember-data/debug</code>&#39;s deprecation depends on a separate, unlanded RFC.</strong> Tying its removal to <a href="./0003-warp-drive-devtools-extension">RFC 0003</a> means this RFC&#39;s timeline for that one package is only as solid as RFC 0003&#39;s own; if the extension slips, <code>@ember-data/debug</code> either slips with it or ships without a real replacement yet. RFC 0003 as currently drafted also expects the two tools to coexist rather than one replacing the other — this RFC overrides that expectation, which RFC 0003 should be updated to reflect rather than have two RFCs disagree.</li><li><strong>No automatic migration for <code>@ember-data/debug</code>.</strong> Unlike every other package here, there&#39;s no <code>eslint --fix</code> for &quot;install a browser extension&quot; — apps get a message and a link, not a mechanical fix, which is a meaningfully worse migration experience than the rest of this RFC.</li><li><strong><code>@warp-drive/codemods</code> is a new package, not an existing shim.</strong> Every other replacement in this RFC already exists and is already tested; <code>@warp-drive/codemods</code> has to be created (renamed from <code>packages/codemods</code>) as part of landing this RFC, which is real work beyond &quot;add a deprecation flag&quot; for this one package.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to “Alternatives”">​</a></h2><ul><li><strong>Do nothing; keep the legacy packages fully supported indefinitely.</strong> Rejected: it commits WarpDrive to documenting, testing, and versioning shim code that does nothing but re-export, forever, with no path to ever simplifying the package graph RFC 1075 was meant to simplify.</li><li><strong>Remove the legacy packages outright, immediately, with no deprecation period.</strong> Rejected: skips the standard deprecate-then-remove cycle this codebase already uses for every other breaking change (see every other flag in <code>deprecations.ts</code>), and would break any app that hasn&#39;t migrated with no warning at all.</li><li><strong>Deprecate at 6.0 and remove at 7.0</strong>, mirroring the timeline this RFC originally proposed and the one <a href="./0002-warp-drive-build-plugin">RFC 0002</a> uses for the babel build path. Superseded in this revision: it opens a new post-6.0 deprecation window instead of resolving on the same <code>since 5.x</code> / <code>until 6.0</code> boundary every other flag in <code>deprecations.ts</code> already uses, and delays finishing RFC 1075&#39;s package unification by a full extra major for packages that have had a working replacement for some time.</li><li><strong>Deprecate only <code>@warp-drive/core-types</code>, leave the <code>@ember-data/*</code> family alone.</strong> Rejected: every <code>@ember-data/*</code> package is the same re-export-shim situation as <code>core-types</code>; singling out <code>core-types</code> leaves the larger and more visible part of the problem (the <code>ember-data</code> meta-package and its dependents) unaddressed.</li><li><strong>Per-package deprecation flags (a dozen-plus flags instead of three).</strong> Considered and rejected for this RFC in favor of the coarser grouping described in &quot;Deprecation flags&quot; — see &quot;Drawbacks&quot; for the trade-off this gives up.</li><li><strong>Build a dedicated <code>legacy-imports</code> codemod in <code>@ember-data/codemods</code>.</strong> Considered, and rejected as redundant once <code>eslint-plugin-warp-drive</code>&#39;s <code>no-legacy-imports</code> rule was found to already perform the identical autofixable rewrite off a generated, export-level mapping table. Building a second tool would mean maintaining two mapping tables for the same rename instead of one.</li></ul><h2 id="unresolved-questions" tabindex="-1">Unresolved questions <a class="header-anchor" href="#unresolved-questions" aria-label="Permalink to “Unresolved questions”">​</a></h2><ul><li>Whether this RFC&#39;s <code>@ember-data/debug</code> deprecation should be a normative amendment to RFC 0003 (updating its &quot;two inspection tools&quot; framing) or stay a standalone claim in this RFC — and what happens to this RFC&#39;s timeline if RFC 0003 isn&#39;t ready by the time 6.0 ships.</li><li>Whether apps that haven&#39;t adopted the RFC 0003 extension by 6.0 lose <code>Model</code>-based ember-inspector support outright, or whether <code>@ember-data/debug</code> needs a short grace period past 6.0 specifically (unlike every other package in this RFC) to avoid that gap.</li><li>Whether the remaining 5.x window before 6.0 is long enough for apps to migrate given the existing <code>eslint-plugin-warp-drive</code> autofix, or whether this RFC&#39;s landing should be gated on 6.0 being at least a certain number of 5.x minors away at the time it merges.</li><li>Whether the <code>no-legacy-imports</code> mapping table (currently named/versioned as <code>public-exports-mapping-5.5.enriched.json</code>) needs a refresh or rename as part of this RFC, and what should extend it to cover namespace imports, re-exports, and <code>require</code>/dynamic <code>import()</code> given those are the one gap in the otherwise-automatic migration path.</li><li>Whether <code>npm deprecate</code> notices should be applied to already-published pre-deprecation versions retroactively, or only to versions published at/after the flags ship.</li><li>Final wording and placement of the <code>upgrading/v6/</code> migration page relative to the existing <code>upgrading/v5/</code> content.</li></ul>`,44)])}var f=a(l,[[`render`,d]]);export{c as __pageData,f as default};
import{Ct as e,at as t,it as n,ot as r,st as i,t as a,tt as o,yt as s}from"./chunks/framework.CgPcrqpH.js";var c=JSON.parse(`{"title":"Pointer and Reference Fields","description":"Proposes four PolarisMode relationship field kinds (pointer, pointer-array, reference, reference-array) for relationships WarpDrive never fetches or validates for delivery, where pointers assert the related resource is loaded and references tolerate its absence.","frontmatter":{"title":"Pointer and Reference Fields","description":"Proposes four PolarisMode relationship field kinds (pointer, pointer-array, reference, reference-array) for relationships WarpDrive never fetches or validates for delivery, where pointers assert the related resource is loaded and references tolerate its absence.","warp-drive-rfc":6,"emberjs-rfc":null,"emberjs-pr":null,"emberjs-branch":null,"sync-hash":null,"stage":"proposed","start-date":"2026-09-25T00:00:00.000Z","release-date":null,"release-versions":null,"teams":["data"],"prs":{"accepted":null},"project-link":null,"suite":null},"headers":[],"relativePath":"rfcs/0006-pointer-and-reference-fields.md","filePath":"rfcs/0006-pointer-and-reference-fields.md"}`),l={name:`rfcs/0006-pointer-and-reference-fields.md`},u={id:`pointer-and-reference-fields`,tabindex:`-1`};function d(a,c,l,d,f,p){let m=e(`StatusBadge`);return s(),n(`div`,null,[o(`h1`,u,[c[0]||=r(`Pointer and Reference Fields `,-1),i(m,{variant:`proposed`,text:`Proposed`}),c[1]||=r(),c[2]||=o(`a`,{class:`header-anchor`,href:`#pointer-and-reference-fields`,"aria-label":`Permalink to “Pointer and Reference Fields”`},`​`,-1)]),c[3]||=t(`<h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to “Summary”">​</a></h2><p>PolarisMode gains four relationship field kinds: <code>pointer</code>, <code>pointer-array</code>, <code>reference</code>, and <code>reference-array</code>. They describe a relationship that WarpDrive never fetches through the relationship itself and whose delivery it does not validate: the related resources may be sideloaded with the parent, arrive by some other request, or never arrive. A <strong>pointer</strong> promises the related resource is loaded whenever the field is read, and WarpDrive asserts that in development builds. A <strong>reference</strong> allows it to be missing: the field yields the related resource when it is loaded and its <code>ResourceKey</code> otherwise. All four are synchronous, have no inverse, and live in the relationship graph with one difference from other sync relationships: a committed delete of a related resource removes it from the relationship, but <code>unloadRecord</code> does not.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to “Motivation”">​</a></h2><p>PolarisMode relationships are strict about what their shape promises: a sync relationship must carry its related resources in the same document, and an async one must carry a link that can fetch them (see <a href="/pr-preview/pr-9539/guides/the-manual/misc/links-mode">LinksMode</a>). That rule cannot express a relationship the developer knows will not be satisfied by the delivering document and does not want WarpDrive to fetch either.</p><p>LegacyMode apps declare such relationships as <code>belongsTo</code>/<code>hasMany</code> with <code>async: false</code> because nothing else exists. They arise when:</p><ul><li>a prior or concurrent request is known to deliver the related records;</li><li>the related resources only ever arrive with the parent and have no endpoint of their own, and the parent should own them outright with no inverse and no fetching;</li><li>the app wants only the id, as the legacy References API allowed; or</li><li>a separate system, such as a recommendation service, can name a resource by type and id but cannot deliver it or link to it.</li></ul><p>Today these degrade silently: a sync relationship returns <code>null</code> or a partial array with no signal that anything is wrong, and the schema gives WarpDrive no way to tell &quot;sideload expected but missing&quot; from &quot;sideload never expected&quot;. The two cases differ in one way, and that difference is the point:</p><ul><li>A <strong>pointer</strong> presumes the related resource is loaded. Its absence is a programming error.</li><li>A <strong>reference</strong> allows the related resource to be missing. Its absence is a normal state, and the identity is useful on its own: to key a placeholder, to fetch, or to compare.</li></ul><p>Both overlap with sync <code>resource</code>/<code>collection</code> by design. While the data is present they behave identically; they differ in what WarpDrive enforces (full linkage at push time versus presence at read time) and in what the app promises. Apps choose the contract that fits.</p><p>Pointers and references are always synchronous and never have an inverse. Without that rule every sync/async and inverse/no-inverse permutation would need a pointer and reference variant; with it, there are four kinds.</p><h3 id="why-distinct-kinds-rather-than-an-option" tabindex="-1">Why distinct kinds rather than an option <a class="header-anchor" href="#why-distinct-kinds-rather-than-an-option" aria-label="Permalink to “Why distinct kinds rather than an option”">​</a></h3><ul><li>The contract is visible where the schema is read, to people and to the schema DSL&#39;s type generation, without consulting option semantics.</li><li>The value types differ: a pointer yields <code>T | null</code>, a reference yields <code>T | ResourceKey | null</code>. Separate kinds let the types say so.</li><li><code>resource</code> and <code>collection</code> stay strict, with no option that weakens them.</li></ul><h2 id="detailed-design" tabindex="-1">Detailed design <a class="header-anchor" href="#detailed-design" aria-label="Permalink to “Detailed design”">​</a></h2><h3 id="terminology" tabindex="-1">Terminology <a class="header-anchor" href="#terminology" aria-label="Permalink to “Terminology”">​</a></h3><ul><li><strong>Pointer</strong>: a relationship whose related resources are promised to be loaded whenever the field is read.</li><li><strong>Reference</strong>: a relationship whose related resources may or may not be loaded; the field yields each as the resource when loaded and as its identity otherwise.</li><li><strong>Related identity</strong>: the <code>{ type, id }</code> the API delivered for an entry, exposed at runtime as a <code>ResourceKey</code>.</li></ul><p>&quot;Reference&quot; collides with LegacyMode&#39;s References API (<code>belongsToReference()</code>, <code>hasManyReference()</code>). PolarisMode removes that API, so the term is free there; see <a href="#how-we-teach-this">How we teach this</a>.</p><h3 id="the-four-field-kinds" tabindex="-1">The four field kinds <a class="header-anchor" href="#the-four-field-kinds" aria-label="Permalink to “The four field kinds”">​</a></h3><p>The array forms follow the existing <code>schema-object</code>/<code>schema-array</code> convention.</p><table tabindex="0"><thead><tr><th>Kind</th><th>Cardinality</th><th>Value on read</th><th>Related resource not in the cache</th></tr></thead><tbody><tr><td><code>pointer</code></td><td>to-one</td><td><code>T | null</code></td><td>Assertion in development; <code>null</code> in production</td></tr><tr><td><code>pointer-array</code></td><td>to-many</td><td><code>readonly T[]</code></td><td>Assertion in development; entry omitted in production</td></tr><tr><td><code>reference</code></td><td>to-one</td><td><code>T | ResourceKey | null</code></td><td>The <code>ResourceKey</code></td></tr><tr><td><code>reference-array</code></td><td>to-many</td><td><code>readonly (T | ResourceKey)[]</code></td><td>That entry is the <code>ResourceKey</code></td></tr></tbody></table><p>All four share one schema shape:</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PointerOrReferenceField</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  kind</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;pointer&#39;</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;pointer-array&#39;</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;reference&#39;</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;reference-array&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  name</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  sourceKey</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">  /** The related resource type, or the trait/abstract type when polymorphic. */</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  type</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  options</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">    polymorphic</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>There is no <code>async</code>, <code>inverse</code>, <code>as</code>, or <code>linksMode</code> option: the kind fixes all of them. A schema that supplies one is rejected by DEBUG-time schema validation. <code>polymorphic</code> behaves as it does for every other relationship kind.</p><p>The kinds are added to <code>PolarisModeFieldSchema</code>, <code>LegacyModeFieldSchema</code>, and <code>CacheableFieldSchema</code>. They are not valid in an <code>ObjectSchema</code>.</p><h3 id="example" tabindex="-1">Example <a class="header-anchor" href="#example" aria-label="Permalink to “Example”">​</a></h3><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">store.schema.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">registerResource</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;post&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  identity: { kind: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;@id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  fields: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { kind: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;field&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;title&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">    // the route loads every user and tag before it loads posts</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { kind: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;pointer&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;author&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;user&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { kind: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;pointer-array&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;tags&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;tag&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">    // a separate commerce API; the product may not be in the cache yet</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { kind: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;reference&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;featuredProduct&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;product&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">    // a recommendation service that returns ids of things implementing \`readable\`</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { kind: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;reference-array&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;suggestedReads&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;readable&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, options: { polymorphic: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>The payload is an ordinary JSON:API relationship object with <code>data</code>; the schema, not the document, makes it a pointer or a reference.</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> post</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> store.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">request</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">findRecord</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;post&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;1&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))).data;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">post.author;           </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// User — asserts in DEBUG if user:7 is not loaded</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">post.tags;             </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// readonly Tag[] — asserts in DEBUG if any tag is missing</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">post.featuredProduct;  </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// Product | ResourceKey | null</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">post.suggestedReads.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">read</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isResourceKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(read) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> read.id </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> read.title));</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// loading a missing reference is an ordinary request</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> product</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> post.featuredProduct;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isResourceKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(product)) {</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> store.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">request</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">findRecord</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(product.type, product.id));</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="unloaded-references" tabindex="-1">Unloaded references <a class="header-anchor" href="#unloaded-references" aria-label="Permalink to “Unloaded references”">​</a></h3><p>A reference yields the related resource when it is loaded and its <code>ResourceKey</code> (the stable <code>{ type, id, lid }</code> identity object the store already uses everywhere) otherwise. The field re-resolves reactively when the resource loads or unloads, so a consumer that re-reads it sees the switch. Nothing is materialized for an unloaded target, so a loaded reference target can be unloaded like any other record; the field simply falls back to the key. <code>isResourceKey</code>, private today, becomes a public export of <code>@warp-drive/core</code> so consumers can discriminate the two. Loading is an ordinary request against the key&#39;s <code>type</code> and <code>id</code>.</p><h3 id="graph-semantics" tabindex="-1">Graph semantics <a class="header-anchor" href="#graph-semantics" aria-label="Permalink to “Graph semantics”">​</a></h3><p>Pointers and references are ordinary graph relationships with implicit inverses, so the graph prunes a related resource from every pointer and reference that held it when its deletion is committed, or when a never-persisted record is discarded. A deleted <code>tag</code> vanishes from <code>post.tags</code>; a deleted <code>product</code> turns <code>post.featuredProduct</code> into <code>null</code>.</p><p>They differ from every other sync relationship in one way. Today, unloading a resource that a sync relationship holds is treated as a client-side delete and the resource is pruned, since a sync relationship cannot refetch it. Pointers and references make no promise that the relationship can deliver the resource, so an unload is an eviction, not a statement about the relationship: the identity stays in the relationship, the way it does today for async inverses. An unloaded pointer target therefore asserts on the next read instead of silently disappearing, and an unloaded reference target keeps its identity available to fetch with.</p><p>Because they are graph-backed, the existing relationship <code>Cache</code> APIs apply unchanged: <code>getRelationship</code>, the related-records mutation operations, <code>changedRelationships</code>, <code>rollbackRelationships</code>, and JSON:API serialization under <code>relationships</code>. <code>meta</code> and <code>links</code> on the relationship object are preserved but never used to fetch.</p><p>Mutation on an editable record accepts:</p><ul><li>for a <code>pointer</code>, a record instance or <code>null</code>; for a <code>pointer-array</code>, record instances. A pointer can only be set to something loaded, because a record instance is proof of loading;</li><li>for a <code>reference</code> or <code>reference-array</code>, a record instance, a bare identity (<code>{ type, id }</code> or a <code>ResourceKey</code>), or <code>null</code>; a bare identity reads back as the resource if loaded, else its key.</li></ul><h3 id="validation" tabindex="-1">Validation <a class="header-anchor" href="#validation" aria-label="Permalink to “Validation”">​</a></h3><p>At push time the only check is that the relationship object carries a <code>data</code> key; <code>null</code> and <code>[]</code> are valid empty values, and a missing or <code>undefined</code> <code>data</code> is rejected, as it is for linksMode relationships. Full linkage is not checked. The polymorphic type check applies as for every relationship kind.</p><p>At read time, a <code>pointer</code> or <code>pointer-array</code> with an unloaded identity fails a DEBUG assertion naming the resource, the field, and each missing identity:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>Assertion Failed: post:1 declares \`author\` as a pointer to user:7, but user:7 is not</span></span>
<span class="line"><span>loaded. A pointer promises its related resource is already loaded whenever the</span></span>
<span class="line"><span>field is read. Load user:7 first, or declare \`author\` as a \`reference\`</span></span>
<span class="line"><span>if it may legitimately be absent.</span></span></code></pre></div><p>In production, where assertions are stripped, a missing pointer target resolves to <code>null</code> or is omitted from the array, the same degraded behaviour a sync <code>linksMode</code> <code>belongsTo</code> has today. The check is read-time by design: whatever satisfies a pointer may land after the document that carries it. References have no read-time assertion.</p><h3 id="reactivity" tabindex="-1">Reactivity <a class="header-anchor" href="#reactivity" aria-label="Permalink to “Reactivity”">​</a></h3><p>Pointers and pointer arrays react to membership changes exactly as the current linksMode <code>belongsTo</code> and <code>hasMany</code> do. A reference field additionally notifies when one of its targets loads or unloads, since its value switches between key and record. Immutable and editable record instances render remote and local relationship state respectively, as for every other field.</p><h3 id="availability-in-legacymode" tabindex="-1">Availability in LegacyMode <a class="header-anchor" href="#availability-in-legacymode" aria-label="Permalink to “Availability in LegacyMode”">​</a></h3><p>The kinds are mode-independent, like <code>schema-array</code>, and are valid in <code>legacy: true</code> schemas. This gives a migration path: an app can re-declare a <code>@hasMany(&#39;tag&#39;, { async: false, inverse: null })</code> that was really a pointer as <code>{ kind: &#39;pointer-array&#39;, name: &#39;tags&#39;, type: &#39;tag&#39; }</code> while still in LegacyMode, get the presence assertion immediately, and sort its sync relationships into pointers and references before switching modes. <code>Model</code> gains no decorator for them; a <code>Model</code>-based app adopts them by moving the resource to a <code>legacy: true</code> schema.</p><h3 id="schema-dsl" tabindex="-1">Schema DSL <a class="header-anchor" href="#schema-dsl" aria-label="Permalink to “Schema DSL”">​</a></h3><p><code>@warp-drive/schema-dsl</code> gains <code>@pointer</code>, <code>@pointerArray</code>, <code>@reference</code>, and <code>@referenceArray</code>, each accepting <code>type</code>, <code>polymorphic</code>, and <code>sourceKey</code>:</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Resource, field, pointer, pointerArray, reference, referenceArray } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@warp-drive/schema-dsl&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@Resource</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Post</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  @field </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">declare</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;"> title</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  @</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pointer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;user&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  declare</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;"> author</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> User</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  @</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pointerArray</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;tag&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  declare</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;"> tags</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> readonly</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Tag</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  @</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">reference</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;product&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  declare</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;"> featuredProduct</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Product</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ResourceKey</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  @</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">referenceArray</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;readable&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, polymorphic: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  declare</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;"> suggestedReads</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> readonly</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Readable</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ResourceKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)[];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The generated read, create, and edit types follow the value table; the edit view of a reference also accepts a bare identity on assignment.</p><h3 id="out-of-scope" tabindex="-1">Out of scope <a class="header-anchor" href="#out-of-scope" aria-label="Permalink to “Out of scope”">​</a></h3><ul><li>The <code>resource</code> and <code>collection</code> fields. Pointers and references are independent of them and can land in either order.</li><li>Paginated collections.</li><li>Any fetching behaviour.</li><li>Deprecating the existing sync <code>linksMode</code> <code>belongsTo</code>/<code>hasMany</code> support in PolarisMode.</li></ul><h2 id="how-we-teach-this" tabindex="-1">How we teach this <a class="header-anchor" href="#how-we-teach-this" aria-label="Permalink to “How we teach this”">​</a></h2><p>The manual&#39;s schema section gains a page on PolarisMode relationships framed around what each kind guarantees:</p><ul><li><strong>WarpDrive delivers it</strong>, sideloaded when sync or fetched by link when async: <code>resource</code> / <code>collection</code>.</li><li><strong>I guarantee it is loaded, however it got here</strong>: <code>pointer</code> / <code>pointer-array</code>.</li><li><strong>It may or may not be loaded; here is who it would be</strong>: <code>reference</code> / <code>reference-array</code>.</li></ul><p>The mnemonic: <em>a pointer is a promise, a reference is a hint.</em> The page shows each kind&#39;s failure mode (the pointer assertion, a reference rendering a placeholder from its <code>id</code>), notes that a sync <code>resource</code> and a <code>pointer</code> overlap on purpose, and states that a PolarisMode <code>reference</code> field is a kind of relationship, not LegacyMode&#39;s References API for inspecting and fetching one. The LinksMode guide&#39;s &quot;What To Expect&quot; section places pointers and references next to the planned <code>resource</code>/<code>collection</code> shapes, and the resource-schema agent skill lists the four kinds. API docs ship with the implementation. A lint rule or codemod that flags <code>async: false, inverse: null</code> legacy relationships as candidates is out of scope.</p><h2 id="drawbacks" tabindex="-1">Drawbacks <a class="header-anchor" href="#drawbacks" aria-label="Permalink to “Drawbacks”">​</a></h2><ul><li><strong>Four more field kinds</strong>, though they map onto two concepts and an existing array convention.</li><li><strong>Two value shapes.</strong> A reference is a record or a <code>ResourceKey</code>, so every read discriminates, and the value changes identity when the target loads, so holders must re-read the field.</li><li><strong>&quot;Reference&quot; is overloaded</strong> while LegacyMode&#39;s References API exists.</li><li><strong>Pointer failures surface at read time</strong>, later than PolarisMode&#39;s other relationship checks. A pointer that is never read never fails, and one read only on an untested path fails silently in production.</li><li><strong>Unload and delete diverge</strong> for these kinds only, a new distinction to learn.</li><li><strong>Third-party caches</strong> that manage relationships without the core graph must implement the same unload and delete rule.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to “Alternatives”">​</a></h2><ul><li><strong>An option on <code>resource</code>/<code>collection</code></strong>, such as <code>options: { loaded: &#39;optional&#39; }</code>. Rejected: it hides the contract in option semantics, weakens the strict kinds, and makes a value type depend on an option.</li><li><strong>Reusing <code>belongsTo</code>/<code>hasMany</code> with a <code>strict</code> flag.</strong> Rejected: it perpetuates fields PolarisMode is moving away from and reduces the pointer/reference distinction to a boolean.</li><li><strong>A <code>{ key, data }</code> wrapper for references</strong>, with <code>data</code> null until loaded. Keeps the value stable across loading, but adds a second value shape and a <code>.data</code> hop on every read.</li><li><strong>Always the resource</strong>, with only <code>$type</code> and <code>id</code> available until loaded. Rejected: an unloaded target would need a materialized record with no data, which <code>unloadRecord</code> could not release without breaking the reference.</li><li><strong>Cardinality as an option</strong> (<code>options: { many: true }</code>). Rejected: every other field kind encodes cardinality in the kind.</li><li><strong>Storing outside the graph.</strong> Considered first, since these fields need no inverse and no fetching. Rejected: it forfeits pruning on delete and hides pointers from <code>getRelationship</code>, <code>changedRelationships</code>, relationship notifications, and the graph explorer in <a href="/pr-preview/pr-9539/rfcs/0003-warp-drive-devtools-extension">RFC 3</a>.</li><li><strong>Treating unload as a client-side delete</strong>, as sync relationships do today. Rejected: it masks the broken promises pointers exist to expose and discards the identity references exist to preserve.</li><li><strong>Doing nothing.</strong> Apps would have to sideload or link every relationship before adopting PolarisMode, which is why relationships block #10408 today.</li></ul><p>Prior art: <a href="https://mobx-state-tree.js.org/concepts/references" target="_blank" rel="noreferrer">MobX-State-Tree</a> draws the same line with <code>types.reference</code> (throws when the target is missing) and <code>types.safeReference</code> (resolves to <code>undefined</code>). Normalized GraphQL caches store every relationship by identity and resolve at read time, but do not distinguish promised from optional presence in the schema.</p><h2 id="unresolved-questions" tabindex="-1">Unresolved questions <a class="header-anchor" href="#unresolved-questions" aria-label="Permalink to “Unresolved questions”">​</a></h2><ul><li><strong>Discriminating the union.</strong> A public <code>isResourceKey</code> guard is proposed; a brand on <code>ResourceKey</code> or a <code>$state</code>-style flag on records could replace or complement it.</li><li><strong>Whether the array kinds expose <code>meta</code> and <code>links</code></strong>, as <code>ManyArray</code> does today.</li><li><strong>Kind names.</strong> <code>-array</code> suffixes versus plurals, or a different base word for either concept.</li><li><strong>A missing pointer target in a production <code>pointer-array</code>.</strong> Omit the entry (proposed) or leave a <code>null</code> hole so the length reflects membership.</li><li><strong>Whether LegacyMode availability should be feature-gated</strong> until the PolarisMode relationship story is complete.</li><li><strong>Sequencing.</strong> #10408 proposes the <code>resource</code>/<code>collection</code> kinds first and these four after; this RFC is independent so the order can follow implementation readiness.</li></ul>`,61)])}var f=a(l,[[`render`,d]]);export{c as __pageData,f as default};
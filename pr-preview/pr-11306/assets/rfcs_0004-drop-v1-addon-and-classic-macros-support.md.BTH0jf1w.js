import{Ct as e,at as t,it as n,ot as r,st as i,t as a,tt as o,yt as s}from"./chunks/framework.Cso29_zS.js";var c=JSON.parse(`{"title":"Dropping V1 Addon and Classic ember-cli Macros Support in 6.0","description":"","frontmatter":{"title":"Dropping V1 Addon and Classic ember-cli Macros Support in 6.0","warp-drive-rfc":4,"emberjs-rfc":null,"emberjs-pr":null,"emberjs-branch":null,"sync-hash":null,"stage":"proposed","start-date":"2026-09-25T00:00:00.000Z","release-date":null,"release-versions":null,"teams":["data","cli"],"prs":{"accepted":null},"project-link":null,"suite":null},"headers":[],"relativePath":"rfcs/0004-drop-v1-addon-and-classic-macros-support.md","filePath":"rfcs/0004-drop-v1-addon-and-classic-macros-support.md"}`),l={name:`rfcs/0004-drop-v1-addon-and-classic-macros-support.md`},u={id:`dropping-v1-addon-and-classic-ember-cli-macros-support-in-6-0`,tabindex:`-1`};function d(a,c,l,d,f,p){let m=e(`StatusBadge`);return s(),n(`div`,null,[o(`h1`,u,[c[0]||=r(`Dropping V1 Addon and Classic ember-cli Macros Support in 6.0 `,-1),i(m,{variant:`proposed`,text:`Proposed`}),c[1]||=r(),c[2]||=o(`a`,{class:`header-anchor`,href:`#dropping-v1-addon-and-classic-ember-cli-macros-support-in-6-0`,"aria-label":`Permalink to “Dropping V1 Addon and Classic ember-cli Macros Support in 6.0”`},`​`,-1)]),c[3]||=t(`<h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to “Summary”">​</a></h2><p>WarpDrive&#39;s 6.0 major drops two pieces of classic Ember infrastructure: the V1 addon shim every <code>@warp-drive/*</code>/<code>@ember-data/*</code>/<code>ember-data</code> package still carries as a compatibility fallback (<code>addonV1Shim</code> from <code>@embroider/addon-shim</code>, and the <code>___legacy_support</code> branch in <code>setConfig</code> that exists only to receive <code>app.options.emberData</code>), and the classic ember-cli macros configuration surface (<code>setConfig(app, __dirname, config)</code> called from <code>ember-cli-build.js</code>, <code>buildMacros()</code> + <code>setConfig(macrosConfig, config)</code> wired by hand into <code>babel.config.mjs</code>, and the accompanying <code>babel-plugin-debug-macros</code> entry). After 6.0, every consumer configures WarpDrive exclusively through the bundler plugin introduced in <a href="/pr-preview/pr-11306/rfcs/0002-warp-drive-build-plugin">RFC 0002</a>, and every WarpDrive package ships as a V2-only addon with no V1 fallback. A non-embroider classic Ember build — one without <code>@embroider/compat</code>&#39;s <code>compatBuild</code> in <code>ember-cli-build.js</code> — can no longer resolve WarpDrive as an ember-cli addon; it can still depend on WarpDrive as a plain npm package via <code>ember-auto-import</code>, with the tradeoffs that entails (see &quot;Ecosystem implications&quot;).</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to “Motivation”">​</a></h2><p>RFC 0002 introduced the build plugin and deprecated the babel-based configuration path, but it deliberately kept two escape hatches alive through the 6.0 boundary to de-risk that proposal on its own: classic ember-cli apps could keep calling <code>setConfig(app, __dirname, config)</code> unchanged, with an automatic babel bridge the addon wires up for them, and WarpDrive&#39;s own packages kept their <code>addonV1Shim</code> fallback so that an app running a fully classic (non-embroider) build could still resolve them. Both of those compatibility paths carry real, ongoing cost, and neither is doing what it was built for anymore:</p><p><strong>1. The V1 shim keeps a resolution path alive that WarpDrive&#39;s own tooling already assumes is absent.</strong> Apps that haven&#39;t adopted embroider at all are still a real, supported part of the Ember ecosystem — this RFC does not claim otherwise, and does not remove WarpDrive&#39;s consumability by them outright. What the shim specifically buys such an app is resolving WarpDrive <strong>as an ember-cli addon</strong> — package discovery, <code>app-js</code>/<code>public-assets</code> merging, the <code>included</code> hook — without embroider&#39;s V2 addon support to interpret those conventions. A classic app could, before and after this RFC, still bring WarpDrive in some other way (for example via <code>ember-auto-import</code> treating it as an external npm dependency — see &quot;Ecosystem implications&quot; below for what that path does and doesn&#39;t get you). What goes away at 6.0 is only the addon-resolution path, not consumability in general. That&#39;s still worth removing: WarpDrive&#39;s own minimum-supported Ember versions already require embroider for anything but the most trivial app (Vite, TypeScript, and strict-mode templates all assume it), so the shim spends real weight — <code>@embroider/addon-shim</code> as a dependency of every published package, plus the <code>ember-addon.version === 1</code> branch in every <code>addon-main.cjs</code> deleting <code>treeForApp</code> — keeping the <em>addon-resolution</em> path alive for a build shape the rest of WarpDrive&#39;s tooling already assumes is absent.</p><p><strong>2. The classic macros path is the thing RFC 0002 was written to replace.</strong> Its own motivation section lists the failure modes directly: a babel pipeline is a hard requirement in ecosystems that don&#39;t have one, the wiring is easy to get wrong (<code>buildMacros</code>, <code>setConfig</code>, <code>...macros()</code>, <code>...Macros.babelMacros</code>, and a <code>babel-plugin-debug-macros</code> entry, each a chance to omit a piece), and multiple copies of <code>@embroider/macros</code> in a dependency tree silently fail to coordinate. Every one of those problems still exists as long as the classic path is reachable at all — a deprecation warning does not remove a footgun, it just labels it. The <code>___legacy_support</code> branch in <code>setConfig</code> (<code>warp-drive-packages/build-config/src/index.ts</code>) exists solely to reconcile <code>app.options.emberData</code> (read by the V1 shim&#39;s <code>included</code> hook) against an app that also calls <code>setConfig</code> directly — a reconciliation step whose only job is supporting the path this RFC removes.</p><p><strong>3. Two supported configuration paths is a permanent maintenance and documentation tax.</strong><code>guides/configuration/index.md</code> currently carries three tabs — Simple Config, Advanced Config, Ember Apps (classic) — for what is conceptually one operation: hand WarpDrive its config object. Every new <code>WarpDriveConfig</code> option, every bug in config resolution, and every support question has to account for three wiring shapes instead of one. RFC 0002 already unified Simple/Advanced Config into the plugin; this RFC finishes the job by retiring the third.</p><p>The expected outcome: one configuration surface (the build plugin, with <code>compatWith</code> and friends), one addon format (V2), <code>@embroider/macros</code> and <code>@embroider/addon-shim</code> off every WarpDrive package&#39;s dependency list, and a support matrix that no longer has to reason about classic (non-embroider) Ember builds at all.</p><h2 id="detailed-design" tabindex="-1">Detailed design <a class="header-anchor" href="#detailed-design" aria-label="Permalink to “Detailed design”">​</a></h2><h3 id="what-v1-addon-support-means-here-precisely" tabindex="-1">What &quot;V1 addon support&quot; means here, precisely <a class="header-anchor" href="#what-v1-addon-support-means-here-precisely" aria-label="Permalink to “What &quot;V1 addon support&quot; means here, precisely”">​</a></h3><p>Every <code>@warp-drive/*</code>, <code>@ember-data/*</code>, and <code>ember-data</code> package ships <code>addon-main.cjs</code> built on:</p><div class="language-js"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;use strict&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">addonShim</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> require</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;@warp-drive/core/addon-shim.cjs&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// re-exports addonV1Shim</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> addon</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> addonShim</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(__dirname);</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> pkg</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> require</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./package.json&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (pkg[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;ember-addon&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">].version </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  delete</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> addon.treeForApp;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>addonV1Shim</code> (from <code>@embroider/addon-shim</code>) is what lets a package written to the V2 addon spec — no <code>index.js</code> addon class, no broccoli trees, just <code>app-js</code>/<code>public-assets</code> in <code>package.json</code> — still resolve correctly for an ember-cli consumer that predates embroider&#39;s V2 addon support. Every WarpDrive package already declares <code>&quot;ember-addon&quot;: { &quot;version&quot;: 2, ... }</code>; the <code>version === 1</code> branch is dead in practice today because nothing in the published packages sets <code>version: 1</code>, but the shim itself, and the <code>included</code> hook it wraps (used to funnel <code>app.options.emberData</code> into <code>setConfig</code> — see next section), stay live and load on every classic-build consumer regardless.</p><p>At 6.0:</p><ul><li><code>addon-main.cjs</code> is replaced by a plain V2 addon manifest with no shim, no <code>included</code> hook, and no runtime branch on <code>ember-addon.version</code>.</li><li><code>@embroider/addon-shim</code> is removed from every WarpDrive package&#39;s dependencies.</li><li>Resolving a WarpDrive package <strong>as an ember-cli addon</strong> requires an embroider-compatible resolution (V2 addon support), whether that&#39;s a Vite build via <code>@embroider/vite</code>, or <code>compatBuild</code> from <code>@embroider/compat</code> in a still-broccoli-based <code>ember-cli-build.js</code>. A fully classic build — no embroider anywhere in the pipeline — cannot resolve WarpDrive packages <em>as addons</em> at 6.0, the same way it already cannot resolve any other V2-only addon in the ecosystem today.</li><li>This is narrower than &quot;cannot use WarpDrive at all&quot; — see &quot;Ecosystem implications&quot; below for the <code>ember-auto-import</code> fallback a fully classic app still has, and what it costs.</li></ul><h3 id="what-classic-ember-cli-macros-config-means-here-precisely" tabindex="-1">What &quot;classic ember-cli macros config&quot; means here, precisely <a class="header-anchor" href="#what-classic-ember-cli-macros-config-means-here-precisely" aria-label="Permalink to “What &quot;classic ember-cli macros config&quot; means here, precisely”">​</a></h3><p>Three call shapes, all accepted by <code>setConfig</code> today (<code>warp-drive-packages/build-config/src/index.ts</code>), are removed:</p><ol><li><p><strong>The 3-argument classic form</strong>, called from <code>ember-cli-build.js</code>:</p><div class="language-js"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(app, __dirname, { compatWith: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;4.12&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, deprecations: { </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">/* ... */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } });</span></span></code></pre></div><p>This is <code>isEmberClassicUsage</code> in <code>setConfig</code>&#39;s implementation — it calls <code>_MacrosConfig.for(context, appRoot)</code> to look up (or create) the app&#39;s embroider macros config by side effect, rather than receiving one directly.</p></li><li><p><strong>The <code>emberData</code> key on an app&#39;s own options object</strong>, passed straight to the <code>EmberApp</code> constructor with no <code>setConfig</code> import at all:</p><div class="language-js"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// ember-cli-build.js</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> app</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> EmberApp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(defaults, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  emberData: { compatWith: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;4.12&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, deprecations: { </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">/* ... */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>This is the oldest of the three shapes and, unlike the other two, isn&#39;t reached by calling <code>setConfig</code> yourself — it&#39;s read by the V1 shim&#39;s <code>included</code> hook (see previous section), which calls <code>setConfig(app, dirname, { ...app.options?.emberData, ___legacy_support: true })</code> on every app regardless of whether that app configured WarpDrive any other way. <code>setConfig</code>&#39;s implementation already throws if this key and a direct <code>setConfig</code> call are both present, and carries a console-warning message for this exact key — written but deliberately never enabled (<code>warp-drive-packages/build-config/src/index.ts</code>, commented out pending a package rearrangement) — that this RFC&#39;s immediate deprecation (see &quot;Deprecating now, removing in 6.0&quot;) supersedes and finally ships, as a proper <code>expectDeprecation()</code>-testable deprecation rather than a plain <code>console.warn</code>.</p></li><li><p><strong>The 2-argument advanced form</strong> aimed at a hand-rolled <code>buildMacros()</code> instance:</p><div class="language-js"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Macros</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> buildMacros</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">configure</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: (</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">config</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> setConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(config, { compatWith: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;5.7&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }) });</span></span></code></pre></div><p>paired with a manually-added <code>babel-plugin-debug-macros</code> entry to convert <code>deprecate</code>/<code>warn</code> calls, and <code>...Macros.babelMacros</code> in the babel plugin list.</p></li></ol><p>At 6.0, <code>setConfig</code> keeps exactly one signature:</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> setConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">config</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> WarpDriveConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>which registers the config directly on the build plugin&#39;s registry (the mechanism RFC 0002 describes under &quot;One config, no matter how many copies&quot;) — no <code>MacrosConfig</code>, no <code>buildMacros</code>, no app/context object, no <code>appRoot</code> string. <code>setConfig</code> becomes a thin wrapper that plugin users don&#39;t even need to call directly; passing the same options object to <code>warpDrive.vite(options)</code> (or the adapter for whichever bundler) does the same thing.</p><p>Calling <code>setConfig</code> with 2 or 3 arguments, or calling <code>buildMacros</code>/<code>babelPlugin</code> from <code>@warp-drive/core/build-config</code>, throws synchronously at build configuration time (not a runtime deprecation warning — by 6.0 these entry points no longer exist in the published type signatures or the compiled output) with a message pointing at the migration guide:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>Error: setConfig() no longer accepts an ember-cli \`app\` object or \`appRoot\` string as of</span></span>
<span class="line"><span>WarpDrive 6.0. Configure WarpDrive through the build plugin instead:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  // vite.config.mjs</span></span>
<span class="line"><span>  import { warpDrive } from &#39;@warp-drive/core/build-plugin&#39;;</span></span>
<span class="line"><span>  plugins: [...ember(), warpDrive.vite({ compatWith: &#39;5.7&#39; })]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Migration guide: https://docs.warp-drive.io/guides/build-plugin-migration</span></span></code></pre></div><p><code>babelPlugin()</code>, the <code>macros()</code> helper, and the documented <code>babel-plugin-debug-macros</code> entry are removed from <code>@warp-drive/core/build-config</code>&#39;s exports entirely; importing them is a module-resolution error, not a runtime warning.</p><h3 id="deprecating-now-removing-in-6-0" tabindex="-1">Deprecating now, removing in 6.0 <a class="header-anchor" href="#deprecating-now-removing-in-6-0" aria-label="Permalink to “Deprecating now, removing in 6.0”">​</a></h3><p>RFC 0002 proposed a staged rollout: the plugin ships silently alongside the classic path, then a build notice one minor later, then a formal deprecation only in the last 5.x minor before 6.0 — with removal itself deferred to 7.0, behind an automatically-injected babel bridge gated by a flag for the full 6.0 beta cycle.</p><p>This RFC collapses that timeline instead of extending it: both deprecations below ship in the very next 5.x minor, alongside the plugin itself, rather than waiting for an interim build notice — and both are removed outright at 6.0, with no bridge and no flag in either major.</p><ol><li><p><strong>Next 5.x minor:</strong> the plugin ships, and both deprecations below fire immediately.</p><ul><li><code>warp-drive.legacy-babel-config</code> — fires on every hit of <code>setConfig</code>&#39;s <code>isEmberClassicUsage</code> (3-arg) branch (covering both the direct <code>setConfig(app, __dirname, config)</code> call and the <code>app.options.emberData</code> key, which reaches the same branch through the V1 shim&#39;s <code>included</code> hook), and on every use of <code>buildMacros()</code> + <code>setConfig(macrosConfig, config)</code>. This is orthogonal to which addon resolution the app uses — an embroider app that still configures WarpDrive this way gets this deprecation, not the next one. Supersedes RFC 0002&#39;s plan of a silent notice followed by a formal deprecation two minors later; the notice and the deprecation collapse into one, and this RFC also finally ships the <code>emberData</code>-key warning that already exists in source but has stayed commented out (see &quot;classic ember-cli macros config&quot; above).</li><li><code>warp-drive.v1-addon-support</code> — fires when WarpDrive is resolved without embroider&#39;s V2 addon support at all, independent of which of the three config shapes the app uses. Pinning down that detection precisely is open — see &quot;Unresolved questions.&quot;</li></ul><p>Both are ordinary WarpDrive deprecations, not build notices: listed alongside the other entries in <code>@warp-drive/build-config/deprecations</code>, testable with <code>assert.expectDeprecation()</code>, and silenceable via the same <code>deprecations: { ... }</code> config option as any other — silencing the warning does not change the removal date.</p></li><li><p><strong>6.0:</strong> both are removed, per the &quot;Detailed design&quot; section above. There is no bridge and no flag; an app that has not migrated by 6.0&#39;s release fails at build configuration time with the error message shown above, and (if it is on a fully classic, non-embroider build) can no longer resolve WarpDrive&#39;s packages as addons (see &quot;Ecosystem implications&quot; for its remaining <code>ember-auto-import</code> fallback).</p></li></ol><p>Starting the warning immediately, rather than in the final 5.x minor as RFC 0002 planned, gives every app the entire remaining 5.x release cycle to migrate instead of just its last minor — even though the removal target (6.0, not 7.0) is sooner than RFC 0002&#39;s original schedule.</p><h3 id="ecosystem-implications" tabindex="-1">Ecosystem implications <a class="header-anchor" href="#ecosystem-implications" aria-label="Permalink to “Ecosystem implications”">​</a></h3><ul><li><strong>A fully classic (non-embroider) app is not left with zero options</strong>, only without addon resolution. It can still adopt <code>ember-auto-import</code> and depend on WarpDrive as a plain npm package, the same way it would depend on any non-Ember JS library. That path gets WarpDrive&#39;s compiled output as an opaque external module: none of the build plugin&#39;s transforms run over it, so there is no deprecation stripping, no canary feature toggling, and no app-side flag imports — the app gets an unoptimized, always-development-shaped build (or has to fall back to a CDN/unpkg copy). It is a real fallback, not a supported, equivalent replacement for addon resolution.</li><li><strong>Addons that depend on WarpDrive</strong> and use the classic <code>setConfig(app, __dirname, ...)</code> form in their own <code>index.js</code> <code>included</code> hook (a pattern several community addons copied from WarpDrive&#39;s own guides before the plugin existed) break at 6.0 the same way an app would, and need the same migration.</li><li><strong>Ember Engines:</strong> unaffected beyond the general migration — engines already resolve V2 addons through the host app&#39;s embroider pipeline.</li><li><strong>Blueprints:</strong> the app blueprint&#39;s WarpDrive wiring, already updated to the plugin recipe under RFC 0002, drops the classic <code>ember-cli-build.js</code> tab entirely rather than marking it deprecated.</li><li><strong>Lint rules:</strong> none required; there is no runtime API surface change, only a build-time configuration one.</li><li><strong><code>@embroider/macros</code> and <code>@embroider/addon-shim</code></strong> both leave every WarpDrive package&#39;s <code>dependencies</code>, completing the removal RFC 0002 scheduled for WarpDrive&#39;s published output format.</li></ul><h2 id="how-we-teach-this" tabindex="-1">How we teach this <a class="header-anchor" href="#how-we-teach-this" aria-label="Permalink to “How we teach this”">​</a></h2><p>The setup guide (<code>guides/configuration/index.md</code>) drops the &quot;Ember Apps&quot; (classic) tab introduced under RFC 0002&#39;s interim period, leaving a single build-plugin recipe per bundler — there is no longer a paradigm switch to teach at all, only &quot;add this plugin to your bundler config.&quot; The legacy package setup guide (<code>guides/configuration/legacy-package-setup/</code>) is retitled to make clear it documents the 5.x migration path, not a currently-supported configuration.</p><p>The upgrade guide for 6.0 (<code>upgrading/v6/index.md</code>) gets a dedicated section listing, in order: (1) confirm the app builds through embroider (Vite or <code>compatBuild</code>) rather than a fully classic pipeline — a prerequisite independent of WarpDrive; (2) replace any <code>setConfig(app, __dirname, ...)</code> / <code>buildMacros()</code> wiring with the plugin call shown above; (3) delete the now-unused <code>@embroider/macros</code>, <code>babel-plugin-debug-macros</code>, and <code>babel.config.mjs</code> entries that existed only for WarpDrive. Because both deprecation IDs (<code>warp-drive.legacy-babel-config</code> and <code>warp-drive.v1-addon-support</code>) fire starting in the very next 5.x minor rather than only in the last one before 6.0, most apps should reach 6.0 having already completed this migration, with the entire remaining 5.x cycle to do it in, rather than discovering it as a breaking change.</p><h2 id="drawbacks" tabindex="-1">Drawbacks <a class="header-anchor" href="#drawbacks" aria-label="Permalink to “Drawbacks”">​</a></h2><ul><li><strong>Shorter overall support window than RFC 0002 originally proposed.</strong> RFC 0002 kept the classic path and the V1 shim fully supported through 6.0 and promised an indefinite babel bridge after that. This RFC removes both at 6.0 with no bridge at all — real apps that would have had until 7.0 now have only until 6.0. The immediate deprecation partly offsets this: the warning starts the moment this RFC lands rather than in 6.0&#39;s final beta, so affected apps get the whole current major&#39;s release cycle to react, not just its last minor — but the net window is still shorter than RFC 0002 promised.</li><li><strong>Immediate, ecosystem-wide deprecation noise.</strong> Every app still on the classic path or relying on V1 addon resolution starts seeing both warnings on its very next upgrade, rather than easing in via a silent-then-notice-then-deprecation ramp. This is deliberate — see Motivation — but it is a real, immediate cost for apps that have not yet started migrating.</li><li><strong>No escape hatch after 6.0.</strong> Unlike the 7.0 &quot;babel bridge remains available indefinitely&quot; promise in RFC 0002, this RFC leaves no supported way to configure WarpDrive without the bundler plugin, and no supported way to resolve WarpDrive packages without embroider&#39;s V2 addon support, once 6.0 ships.</li><li><strong>Community addons that copied the classic recipe</strong> from WarpDrive&#39;s own older guides (the &quot;Advanced Config&quot; tab) inherit this break even if they never touch WarpDrive&#39;s plugin themselves, since their own <code>included</code> hooks call the same removed <code>setConfig</code> shape.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to “Alternatives”">​</a></h2><ul><li><strong>Keep RFC 0002&#39;s original schedule</strong> (deprecate at 6.0, remove at 7.0 with an indefinite babel bridge). Rejected here because it keeps every problem in the Motivation section reachable for one more full major, and keeps <code>@embroider/macros</code> and <code>@embroider/addon-shim</code> in WarpDrive&#39;s dependency tree that much longer.</li><li><strong>Keep RFC 0002&#39;s staged ramp (silent → build notice → formal deprecation) but still remove at 6.0 instead of 7.0.</strong> Considered as a middle ground. Rejected: the ramp&#39;s only purpose is easing apps into a warning that will eventually fire regardless, and shortening the removal target to 6.0 without also starting the warning immediately would leave apps with less total notice than today&#39;s plan, not more — the ramp made sense paired with a 7.0 removal, not a 6.0 one.</li><li><strong>Drop only the classic macros config, keep the V1 shim.</strong> Rejected: the V1 shim&#39;s <code>included</code> hook is what feeds <code>app.options.emberData</code> into the classic <code>setConfig</code> path in the first place, so removing one without the other leaves a live entry point into code this RFC deletes.</li><li><strong>Drop only the V1 shim, keep classic macros config for embroider-based classic builds.</strong> Considered, since <code>compatBuild</code> apps without Vite could theoretically keep calling <code>setConfig(app, __dirname, ...)</code> while still resolving WarpDrive as a V2 addon. Rejected for consistency: RFC 0002&#39;s plugin already supports <code>compatBuild</code> pipelines (it runs ahead of babel regardless of bundler), so there is no build shape left that needs the classic signature once the plugin exists — keeping it only prolongs the two-paths maintenance cost this RFC exists to end.</li><li><strong>Ship the flag-gated automatic babel bridge from RFC 0002&#39;s unresolved question, then remove it later.</strong> Rejected: building and testing an automatic bridge (detecting the app&#39;s babel setup and injecting compatible config) is nontrivial work for a path this RFC proposes to delete outright; the effort is better spent on migration tooling (a codemod, see Unresolved questions) that gets apps off the classic path rather than papering over it.</li></ul><h2 id="unresolved-questions" tabindex="-1">Unresolved questions <a class="header-anchor" href="#unresolved-questions" aria-label="Permalink to “Unresolved questions”">​</a></h2><ul><li>Whether a codemod should ship alongside the 6.0 release (or earlier, during the deprecation period) that rewrites a classic <code>ember-cli-build.js</code> <code>setConfig</code> call and a <code>babel.config.mjs</code><code>buildMacros</code>/<code>setConfig</code> pair into the equivalent plugin call automatically, versus leaving this as a documented manual migration.</li><li>Whether the <code>warp-drive.v1-addon-support</code> deprecation can be made precise enough to fire only for apps that would actually fail at 6.0 (i.e. those without embroider V2 addon resolution), as opposed to firing for every app that hasn&#39;t explicitly opted into the plugin yet, which would over-warn compatBuild-based apps that face no V1-addon break at all.</li><li>How long the legacy package setup guide should stay published as a historical reference for teams mid-upgrade, versus being removed once 6.0 is released.</li></ul>`,40)])}var f=a(l,[[`render`,d]]);export{c as __pageData,f as default};
import{Ct as e,at as t,it as n,ot as r,st as i,t as a,tt as o,yt as s}from"./chunks/framework.Br18A9IS.js";var c=JSON.parse(`{"title":"HoloPrograms","description":"A HoloProgram answers every request a holodeck test makes from a per-test WarpDrive store while recording, so a test starts from a seeded scene instead of declaring one mock per request.","frontmatter":{"title":"HoloPrograms","description":"A HoloProgram answers every request a holodeck test makes from a per-test WarpDrive store while recording, so a test starts from a seeded scene instead of declaring one mock per request.","warp-drive-rfc":8,"emberjs-rfc":null,"emberjs-pr":null,"emberjs-branch":null,"sync-hash":null,"stage":"proposed","start-date":"2026-09-23T00:00:00.000Z","release-date":null,"release-versions":null,"teams":["data"],"prs":{"accepted":null},"project-link":null,"suite":null},"headers":[],"relativePath":"rfcs/0008-holoprograms.md","filePath":"rfcs/0008-holoprograms.md"}`),l={name:`rfcs/0008-holoprograms.md`},u={id:`holoprograms`,tabindex:`-1`};function d(a,c,l,d,f,p){let m=e(`StatusBadge`);return s(),n(`div`,null,[o(`h1`,u,[c[0]||=r(`HoloPrograms `,-1),i(m,{variant:`proposed`,text:`Proposed`}),c[1]||=r(),c[2]||=o(`a`,{class:`header-anchor`,href:`#holoprograms`,"aria-label":`Permalink to “HoloPrograms”`},`​`,-1)]),c[3]||=t(`<h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to “Summary”">​</a></h2><p>A HoloProgram is a reusable API scenario for <code>@warp-drive/holodeck</code> tests. It pairs a seed, the starting server state, with a router of route handlers that every program shares, plus optional per-route behaviors such as a delay or an error. A test opts in with <code>startProgram(this, program)</code>. While the suite records, which is what a local run does, the program answers each request from a per-test WarpDrive store built with the application&#39;s own schemas, and holodeck writes the answer to <code>.mock-cache</code> as it writes any fixture. A replayed suite, which is what CI runs, never boots a program. It reads the same fixtures it reads today.</p><p>Recording against a real API is out of scope. Safety protocols and the relay modes move to a follow-up RFC, &quot;Recording Holodeck Mocks Against a Real API&quot; (#11286), called RFC B below. This RFC reserves the names RFC B needs. The design carries forward the vision in #9616 and supersedes it as the design of record.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to “Motivation”">​</a></h2><p>Today a test states every response it needs, one mock at a time. Each mock restates data the test already set up, and each mutation mock carries the exact request body string so the fixture key matches.</p><p>The test <code>update hasMany with repeated patch</code> in <code>tests/json-api/tests/integration/cache/mutation-request-test.ts</code> shows the cost. It pushes two users and two pets into the client store. Its <code>patchUser1</code> helper builds a body string, declares a <code>PATCH</code> mock for <code>/api/user/1</code> with that string, and writes the response by hand, including the names and relationship links the push already holds. The test calls the helper twice, so it declares two responses that a server would compute from its own state. That file and <code>did-commit-notification-test.ts</code> declare seven such mocks for <code>user</code> resources.</p><p>With a program, the author writes the server once. A seed holds the users and pets. A shared <code>PATCH /api/user/:id</code> handler applies the request body to the program&#39;s store and returns the result. The test starts the program and makes its requests. It declares no mock and restates neither the response nor the body string.</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">test</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;update hasMany with repeated patch&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">assert</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  startProgram</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, usersAndPets); </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// from &#39;@warp-drive/holodeck/program&#39;</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">  // push the same users and pets, then build and send each PATCH as before, with no PATCH mock</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>The program records the request text it received, so the body key cannot drift from the request. The next test that needs &quot;user 1 after a pet transfer&quot; starts the same program.</p><h3 id="what-stays-the-same" tabindex="-1">What stays the same <a class="header-anchor" href="#what-stays-the-same" aria-label="Permalink to “What stays the same”">​</a></h3><ul><li>The mode. Local runs record and CI replays, decided at build time as today.</li><li>The fixtures. Programs write through the existing record endpoint, and replay reads the same directory layout and files. Every fixture a suite has committed reproduces byte for byte.</li><li>Per-test scoping. Program state lives on the test&#39;s holodeck entry and goes away with <code>setTestId(this, null)</code>.</li><li>The mock helpers in <code>@warp-drive/holodeck/mock</code>. Inside a program test they become overrides.</li></ul><h3 id="why-warpdrive-can-do-this" tabindex="-1">Why WarpDrive can do this <a class="header-anchor" href="#why-warpdrive-can-do-this" aria-label="Permalink to “Why WarpDrive can do this”">​</a></h3><p>A generic mock library does not know the application&#39;s data. Mirage keeps a second model layer that exists only inside Mirage, and <code>@msw/data</code> asks for collection schemas written again in a validation library. A program&#39;s store is a real WarpDrive <code>Store</code> with the application&#39;s own schemas, so DEBUG builds validate every seed and every handler write against them, and the application&#39;s request builders produce the URLs the router matches.</p><p>Issue #9627 asked what holodeck offers that MSW does not. Holodeck does not intercept <code>fetch</code>. It serves real HTTP/2 responses from its own process and commits fixtures per test, so a replayed suite does no setup work. HoloPrograms add scene-setting to the recording side only. <a href="#prior-art">Prior art</a> has the comparison.</p><h2 id="detailed-design" tabindex="-1">Detailed design <a class="header-anchor" href="#detailed-design" aria-label="Permalink to “Detailed design”">​</a></h2><h3 id="terminology" tabindex="-1">Terminology <a class="header-anchor" href="#terminology" aria-label="Permalink to “Terminology”">​</a></h3><ul><li><strong>HoloProgram</strong>, or <strong>program</strong>. The object <code>createProgram</code> returns. It names a seed, a router, a store factory, and optional behaviors. A test starts at most one.</li><li><strong>Seed</strong>. A program&#39;s starting server state, as an array of JSON:API resource objects.</li><li><strong>Program store</strong>. The <code>Store</code> that holds a program&#39;s state for one test while recording. It is never the store the application under test uses.</li><li><strong>Router</strong> and <strong>route handler</strong>. The router maps a method and URL pattern to a handler, which reads and writes the program store and returns a response.</li><li><strong>Behavior</strong>. A per-program, per-route adjustment, such as a delay or an error.</li><li><strong>Override</strong>. A mock declared with the existing helpers in a test that started a program. It answers one request in place of the program.</li><li><strong>Phase 0</strong>. The holodeck fixes in <a href="#phased-delivery">Phased delivery</a> that programs depend on. Each one also helps suites that use no program.</li></ul><p>&quot;Program&quot; already names the mock server process in holodeck. The server exports <code>launchProgram</code> and <code>endProgram</code>, and its startup banner prints <code>program:</code>. Both function names stay, because every <code>diagnostic.js</code> that launches holodeck calls them. Phase 1 changes the banner label to <code>project:</code>. Documentation calls the process &quot;the holodeck server&quot; and keeps &quot;program&quot; for HoloPrograms.</p><h3 id="where-things-execute" tabindex="-1">Where things execute <a class="header-anchor" href="#where-things-execute" aria-label="Permalink to “Where things execute”">​</a></h3><p>Programs run in the browser test bundle, next to the test. Only the browser knows whether the suite is recording, and only the application&#39;s build has its schemas and store. The holodeck server gains no per-test state and no endpoint.</p><table tabindex="0"><thead><tr><th>Piece</th><th>Runs in</th><th>Runs when</th></tr></thead><tbody><tr><td><code>createProgram</code>, <code>new Router(...)</code></td><td>Browser</td><td>Module evaluation. Both store thunks only.</td></tr><tr><td>Router <code>define</code> callback</td><td>Browser</td><td>First program-served request in the bundle, while recording</td></tr><tr><td>Store factory, seed thunk, <code>behaviors</code> callback</td><td>Browser</td><td>First program-served request of each test, while recording</td></tr><tr><td>Route handlers, <code>after</code></td><td>Browser</td><td>Each program-served request, while recording</td></tr><tr><td><code>updateProgram</code> callback</td><td>Browser</td><td>The call, while recording. Never in replay.</td></tr><tr><td>Fixture write</td><td>Holodeck server</td><td>The record endpoint, as today</td></tr><tr><td>Serving a fixture from disk</td><td>Holodeck server</td><td>Every request, in both modes, as today</td></tr></tbody></table><p>While recording, the program answers a request before it leaves the browser. Holodeck posts the answer to the record endpoint at that request&#39;s number, awaits the server&#39;s reply and fails the request if the server refused the fixture, then lets the request continue. The server serves the fixture it just wrote. The forwarded request takes the same server path in both modes. In this RFC, &quot;replay&quot; names the mode CI runs in, not that server step.</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>record run                                   replay run</span></span>
<span class="line"><span>  startProgram(this, usersAndPets)             startProgram(this, usersAndPets)</span></span>
<span class="line"><span>    stores the program on the test entry         stores the program on the test entry</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  store.request(PATCH /api/user/1)             store.request(PATCH /api/user/1)</span></span>
<span class="line"><span>    holodeck numbers it #0                       holodeck numbers it #0</span></span>
<span class="line"><span>    no override owns #0                          no override owns #0</span></span>
<span class="line"><span>    boots the program store (first time)         nothing runs</span></span>
<span class="line"><span>    router matches PATCH /api/user/:id</span></span>
<span class="line"><span>    handler updates the program store</span></span>
<span class="line"><span>    fixture #0 is written</span></span>
<span class="line"><span>    PATCH /api/user/1?__xTestId=…                PATCH /api/user/1?__xTestId=…</span></span>
<span class="line"><span>    server responds from disk                    server responds from disk</span></span>
<span class="line"><span>    after() runs, if a behavior sets one</span></span></code></pre></div><h3 id="entry-point" tabindex="-1">Entry point <a class="header-anchor" href="#entry-point" aria-label="Permalink to “Entry point”">​</a></h3><p>The API ships as a new subpath, <code>@warp-drive/holodeck/program</code>, next to <code>./mock</code>. The root entry, which resolves to the server under Node and to the client in the browser, does not change, so suites without programs see no difference.</p><h3 id="createprogram" tabindex="-1"><code>createProgram</code> <a class="header-anchor" href="#createprogram" aria-label="Permalink to “createProgram”">​</a></h3><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Store } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@warp-drive/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { ObjectValue } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@warp-drive/core/types/json/raw&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SeedIdentifier</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">type</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">id</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SeedResource</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  type</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  id</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  attributes</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ObjectValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  relationships</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">data</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SeedIdentifier</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SeedIdentifier</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[] </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">links</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ObjectValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">meta</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ObjectValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }&gt;;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  links</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ObjectValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  meta</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ObjectValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Seed</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SeedResource</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// Phase 2, for updateProgram</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SeedDocument</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">data</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SeedResource</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SeedResource</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[] </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">included</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SeedResource</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[] }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ProgramDefinition</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  name</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  store</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Store</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  seed</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Seed</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Seed</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  router</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Router</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  behaviors</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">r</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BehaviorMap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// Phase 2</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Program</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">readonly</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;"> name</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// opaque, see below</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createProgram</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">definition</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ProgramDefinition</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Program</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>A seed is JSON:API resource objects with ids and no lids. <code>seed</code> is a thunk that runs only when a test boots the program while recording, so a seed loaded with <code>import()</code> costs nothing in replay. <code>store</code> returns a fresh <code>Store</code> with the application&#39;s schemas; <code>TestStore</code> in the examples stands for the test app&#39;s store class. <code>name</code> is optional and appears only in errors and fixture metadata. <code>createProgram</code> is synchronous and registers nothing. <code>Program</code> is opaque, so nothing in a test file can reach the seed or the router through it.</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// tests/json-api/programs/users-and-pets.ts</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> usersAndPets</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createProgram</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;users and pets&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  store</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: () </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TestStore</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  seed</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./seeds/users-and-pets.json&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)).default,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  router,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><h3 id="startprogram-and-updateprogram" tabindex="-1"><code>startProgram</code> and <code>updateProgram</code> <a class="header-anchor" href="#startprogram-and-updateprogram" aria-label="Permalink to “startProgram and updateProgram”">​</a></h3><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> startProgram</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">context</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> object</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">program</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Program</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> updateProgram</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">context</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> object</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">update</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Seed</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SeedDocument</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span></code></pre></div><p><code>startProgram</code> takes a program object, not a registered name, so a typo fails in the editor and nothing has to be registered before tests run. It stores the program on the test&#39;s holodeck entry and boots nothing, in either mode. It is synchronous, so a missing <code>await</code> cannot race the first request. It throws when the context has no test id, when the test already started a program, and when the test already issued a request.</p><p><code>updateProgram</code> upserts resources into the program store for the current test. Like a mock&#39;s response function, its callback runs only while recording. It boots the program store first if needed, so seed and schema errors surface at the call rather than at the next request.</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> updateProgram</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, () </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ({ data: { type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;pet&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;3&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, attributes: { name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Muffin&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } } }));</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// every later request that returns pet:3 sees Muffin</span></span></code></pre></div><h3 id="program-lifecycle" tabindex="-1">Program lifecycle <a class="header-anchor" href="#program-lifecycle" aria-label="Permalink to “Program lifecycle”">​</a></h3><ul><li>The first program-served request of a recording test boots the program. Boot calls the store factory, awaits the seed, pushes it in one transaction, runs the router&#39;s <code>define</code> callback if no earlier test has, and then runs the <code>behaviors</code> callback, which validates its patterns against the routes. Concurrent first requests await one boot.</li><li><code>setTestId(this, null)</code> removes the entry, and the program store with it, and runs the unused-mock check that #11177 added. <a href="#override-precedence">Override precedence</a> keeps that check correct.</li></ul><h3 id="router-and-route-handlers" tabindex="-1">Router and route handlers <a class="header-anchor" href="#router-and-route-handlers" aria-label="Permalink to “Router and route handlers”">​</a></h3><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ProgramMethod</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;GET&#39;</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;POST&#39;</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;PUT&#39;</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;PATCH&#39;</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;DELETE&#39;</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;HEAD&#39;</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;QUERY&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LazyHandler</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RouteHandler</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">RouteHandler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RouteMap</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { [</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">M</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> in</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ProgramMethod</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">pattern</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">handler</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LazyHandler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> declare</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Router</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">constructor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">define</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">r</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RouteMap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RouteHandler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  request</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ProgramContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">request</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Request</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> HandlerResult</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">HandlerResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  protocols</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> never</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// this member and the next two are reserved for RFC B</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  requestProtocol</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> never</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  responseProtocol</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> never</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ProgramContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  readonly</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;"> store</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Store</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  readonly</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;"> params</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Readonly</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt;;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  readonly</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;"> query</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> URLSearchParams</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  readonly</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;"> body</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  readonly</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;"> route</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  readonly</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;"> routeRequestNumber</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// Phase 2</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  find</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">type</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">id</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SeedResource</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  findAll</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">type</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SeedResource</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  push</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">resources</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SeedResource</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Seed</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  remove</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">type</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">id</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  desiredId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// Phase 2</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  desiredPatch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Seed</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// Phase 2</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  commitDesiredPatch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// Phase 2</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> HandlerResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  status</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  statusText</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  headers</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  body</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ObjectValue</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> router</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Router</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">r</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  r.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">PATCH</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;/api/user/:id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./handlers/user&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)).</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PATCH</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// ./handlers/user.ts</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> PATCH</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RouteHandler</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  request</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.body </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">data</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SeedResource</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ctx.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">push</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">data, id: ctx.params.id });</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { body: { data: ctx.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">find</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;user&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, ctx.params.id) } };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>Each handler is imported the first time a request matches it, so replay never loads a handler module. <code>params</code> holds the pattern&#39;s named groups. <code>query</code> holds the search params without holodeck&#39;s two test params. <code>body</code> is <code>JSON.parse</code> of the request text, or <code>null</code>. <code>route</code> is the matched pattern and <code>routeRequestNumber</code> is the per-route counter behaviors use. <code>find</code>, <code>findAll</code>, <code>push</code>, and <code>remove</code> work in seed shape, without lids. <code>push</code> upserts through the cache, so attributes and relationships merge into an existing resource. <code>find</code> returns the cached resource with its <code>lid</code> stripped. <code>remove</code> unloads the record and does not touch relationships that pointed at it. Holodeck keeps its own index of what it pushed, because core has no public way to list every resource in a cache (see <a href="#serializecache"><code>serializeCache</code></a>). A handler that writes through <code>ctx.store</code> directly bypasses that index, and <code>findAll</code> does not see those records. <code>desiredId</code>, <code>desiredPatch</code>, and <code>commitDesiredPatch</code> expose the <code>id</code> and <code>patch</code> behaviors.</p><p>A handler may be sync or async. It returns one explicit <code>HandlerResult</code> shape, not a <code>Response</code> or a bare document, so holodeck never guesses which one it got. Bodies are JSON only, because the server stores JSON.</p><h3 id="what-a-program-records" tabindex="-1">What a program records <a class="header-anchor" href="#what-a-program-records" aria-label="Permalink to “What a program records”">​</a></h3><p>Each handler result becomes an ordinary fixture, the shape a mock helper records:</p><ul><li>The method, and the request URL without origin and without holodeck&#39;s two test params. Phase 0 makes the server key it the way replay looks it up.</li><li>The exact request text as the body key, or <code>null</code>. A non-string request body fails while recording, because only a string can match on replay.</li><li><code>status</code> defaults the way the mock helpers default. <code>GET</code>, <code>HEAD</code>, and <code>QUERY</code> get <code>200</code>. <code>POST</code> gets <code>201</code> with a body and <code>204</code> without. <code>PUT</code>, <code>PATCH</code>, and <code>DELETE</code> get <code>200</code> with a body and <code>204</code> without. <code>statusText</code> defaults from the status code.</li><li><code>headers</code> pass through. The server adds <code>Content-Type: application/vnd.api+json</code> when absent, as it does for every mock.</li><li>The response body is the result&#39;s <code>body</code>, or <code>{}</code> when it is <code>null</code>.</li></ul><h3 id="url-matching" tabindex="-1">URL matching <a class="header-anchor" href="#url-matching" aria-label="Permalink to “URL matching”">​</a></h3><p>Route patterns are <code>URLPattern</code> pathname patterns, native in current browsers, matched against the request&#39;s pathname. Origin is ignored, as fixtures ignore it. Queries do not take part in matching; a handler reads <code>ctx.query</code> instead. Explicit mocks keep today&#39;s text matching, where parameter order and encoding count. A program does not guess the encoded URL, because it records the URL the request carried.</p><h3 id="isolation-and-concurrency" tabindex="-1">Isolation and concurrency <a class="header-anchor" href="#isolation-and-concurrency" aria-label="Permalink to “Isolation and concurrency”">​</a></h3><p>Each test that boots a program gets its own program store, keyed by the test context object, which is how holodeck already isolates concurrent tests. Program stores never meet.</p><p>Within one test, handlers run one at a time. Without behaviors they run in the order the test issued the requests. Request numbers are assigned synchronously, so concurrent requests get distinct fixture slots. An async handler holds the queue until it settles. A request with a <code>requestDelay</code> joins the queue when its delay ends. The fixed order keeps store mutations, and therefore fixtures, stable across recordings.</p><h3 id="override-precedence" tabindex="-1">Override precedence <a class="header-anchor" href="#override-precedence" aria-label="Permalink to “Override precedence”">​</a></h3><p>A mock answers the next request with its method and URL that no earlier mock owns, and the program answers every other request. Holodeck already numbers each test&#39;s requests and counts its declared mocks, per method and URL. Four rules give a request the same owner in record and replay:</p><ol><li>Both counts key on one normalized URL. Phase 0 does this.</li><li>Request <em>n</em> belongs to an override when at least <em>n</em> + 1 mocks are declared for its method and URL. The program skips it. The handler does not run, the store does not change, and the per-route counter does not advance.</li><li>Otherwise the program owns request <em>n</em> and raises the mock count to <em>n</em> + 1, so an override declared afterwards lands on the next request.</li><li>Rules 2 and 3 run synchronously when holodeck assigns the request number, before any <code>requestDelay</code> or queueing, and in both modes. A mock declared while a delayed or async request is in flight lands after it. In replay the program does not run, but holodeck still assigns owners and advances the count, so record and replay agree.</li></ol><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">startProgram</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, usersAndPets);</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> store.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">request</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getUser</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;1&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));                                  </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// GET #0, program</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PATCH</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;api/user/1&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, () </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ({ data: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }), { body }); </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// override</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> store.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">request</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">patchUser</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;1&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, body));                          </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// PATCH #0, override</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> store.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">request</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">patchUser</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;1&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, body));                          </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// PATCH #1, program</span></span></code></pre></div><p>The override leaves the program store alone, so the second <code>PATCH</code> sees the state before the first. The unused-mock check keeps its meaning. An override no request reached still fails the test from <code>afterEach</code>, and program-served requests raise the mock count only up to the request count, so they never cause a false report. Without rule 3, a mock declared after a program-served request overwrote that request&#39;s fixture in the prototype.</p><h3 id="behaviors" tabindex="-1">Behaviors <a class="header-anchor" href="#behaviors" aria-label="Permalink to “Behaviors”">​</a></h3><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Behavior</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  requestDelay</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  responseDelay</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  error</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">status</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">statusText</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">headers</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;; </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">body</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ObjectValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  after</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">request</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Request</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">store</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Store</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  id</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  patch</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Seed</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BehaviorMap</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { [</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">M</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> in</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ProgramMethod</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">pattern</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">behavior</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Behavior</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Behavior</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span></code></pre></div><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">behaviors</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: (</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">r</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  r.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">GET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;/api/pet/:id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { responseDelay: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });               </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// every request</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  r.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">PATCH</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;/api/user/:id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, [{}, { error: { status: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">409</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }]); </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// first succeeds, second conflicts</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">},</span></span></code></pre></div><p>An object applies to every program-served request on the route. An array applies one entry per program-served request, in order, and nothing after the last. The index is <code>ctx.routeRequestNumber</code>, counted per method and pattern. It is not the fixture counter, which is per URL, and only the fixture counter reaches disk. Boot throws if a behavior names a pattern the router does not define for that method, or if an <code>error</code> status is below 400.</p><ul><li><code>requestDelay</code> waits before the handler runs, while recording. A mutation from another request that lands during the wait is visible to the handler.</li><li><code>responseDelay</code> waits after the handler, before the response.</li><li>The fixture stores <code>delay = requestDelay + responseDelay</code>, and replay waits that long before responding. The forwarded request of a recording run is served from disk too, so a recording run sleeps both delays in the browser and then waits for their sum again on the server.</li><li><code>error</code> replaces the response and skips the handler. The fixture is an ordinary fixture with that status. Phase 0 makes 5xx fixtures with a body replay.</li><li><code>after</code> runs once the forwarded request settles, inside the per-test queue. A request issued while this one is in flight sees the state before <code>after</code> runs. Replay never runs <code>after</code>.</li><li><code>id</code> is the primary key a handler should mint for a created record, read with <code>ctx.desiredId()</code>.</li><li><code>patch</code> is resources a mutation should also upsert, read with <code>ctx.desiredPatch()</code> and pushed by <code>ctx.commitDesiredPatch()</code>. A handler may ignore <code>id</code> and <code>patch</code>.</li></ul><h3 id="serializecache" tabindex="-1"><code>serializeCache</code> <a class="header-anchor" href="#serializecache" aria-label="Permalink to “serializeCache”">​</a></h3><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> serializeCache</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">store</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Store</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">options</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mintId</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">type</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">ordinal</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Seed</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span></code></pre></div><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> loadUsersAndPets</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Seed</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; {</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> store</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TestStore</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rey</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> store.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">createRecord</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;pet&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;1&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Rey&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> pixel</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> store.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">createRecord</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;pet&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Pixel&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }); </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// no id</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  store.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">createRecord</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;user&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;1&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, firstName: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Chris&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, pets: [rey, pixel] });</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> serializeCache</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(store);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>It depends on a change in core. The <code>Cache</code> interface declares <code>dump(): Promise&lt;ReadableStream&lt;unknown&gt;&gt;</code>, <code>JSONAPICache</code> does not implement it, and core has no other public way to list every resource. This RFC asks core to implement <code>JSONAPICache.dump()</code> as a stream of resource objects, which SSR can reuse. The fallback is a public iterator over resource keys on <code>store.cacheKeyManager</code>. Phase 3 waits for one of them.</p><p>Holodeck then serializes in two passes. A <code>createRecord</code> seed points at id-less records by <code>lid</code>, and minting ids alone leaves those references dangling. Pass one walks the dump in order and mints an id for each resource without one. Pass two rewrites every relationship reference from lid to id and drops all lids.</p><p>Minted ids are deterministic. The default <code>mintId</code> returns <code>\`\${type}-\${ordinal}\`</code>, where the ordinal counts id-less resources of that type in dump order, so a deterministic seed function mints the same ids every time and re-recording does not churn <code>.mock-cache</code>. An application that validates id format passes its own <code>mintId</code>. Deleted records are skipped. New records serialize as remote state, which is the intent of a <code>createRecord</code> seed.</p><h3 id="the-fixture-contract" tabindex="-1">The fixture contract <a class="header-anchor" href="#the-fixture-contract" aria-label="Permalink to “The fixture contract”">​</a></h3><p>The directory layout <code>.mock-cache/&lt;testId&gt;/&lt;METHOD&gt;::&lt;url&gt;::&lt;n&gt;/</code>, the body-hash file names, and the compressed body file do not change. New <code>meta.json</code> fields are written only when set, so re-recording a suite without programs still reproduces every committed file byte for byte. Two fields are added:</p><ul><li><code>delay</code>, in milliseconds, when nonzero (Phase 0). Replay waits before responding.</li><li><code>program</code>, the <code>name</code> of the program that recorded the fixture. Replay ignores it. It lets <code>git grep &#39;&quot;program&quot;:&quot;users and pets&quot;&#39;</code> find every fixture a program produced.</li></ul><p>A changed seed, handler, or behavior does not invalidate fixtures. Programs never run in replay, and the cache key comes from the request. Local runs record everything, so a test run locally after the change rewrites its fixtures. The risk is a test nobody ran. Its old fixtures still replay and pass in CI. The recommended practice after changing a seed or a shared handler is to run the whole suite locally with <code>pnpm test</code>, commit the <code>.mock-cache</code> diff with the change, and run <code>CI=1 pnpm test</code> to confirm replay. The <code>program</code> field tells a reviewer which fixtures to expect in the diff.</p><h3 id="error-messages" tabindex="-1">Error messages <a class="header-anchor" href="#error-messages" aria-label="Permalink to “Error messages”">​</a></h3><p>A test author sees these through the rejected request unless noted:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>Holodeck: program &quot;users and pets&quot; has no route for GET api/user/1/pets (request #0).</span></span>
<span class="line"><span>Add the route to the program&#39;s Router, or declare an override for this request with</span></span>
<span class="line"><span>GET(this, &#39;api/user/1/pets&#39;, ...).</span></span>
<span class="line"><span></span></span>
<span class="line"><span>This test started program &quot;users and pets&quot;. Programs do not run in replay, so every request</span></span>
<span class="line"><span>the program serves needs a committed fixture. Run the test locally to record it, then commit</span></span>
<span class="line"><span>.mock-cache.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Holodeck: the route handler for GET /api/user/:id declares &quot;protocols&quot;. The members protocols,</span></span>
<span class="line"><span>requestProtocol, and responseProtocol are reserved for safety protocols, which this version of</span></span>
<span class="line"><span>holodeck does not run. Remove them.</span></span></code></pre></div><p>The first is thrown while recording when no route matches, and nothing is recorded. The second is appended in replay to the missing-fixture error holodeck already raises. The third is thrown while recording, when the handler is first imported. In addition:</p><ul><li>A handler that throws fails its request with the handler&#39;s message, prefixed by <code>Holodeck: the route handler for &lt;pattern&gt; in program &lt;name&gt; threw while recording request #n</code>.</li><li>A non-string request body, a misused <code>startProgram</code>, a bad behavior at boot, and a mock declared for an already-served slot (Phase 0) each throw a message naming the cause.</li></ul><h3 id="reserved-extension-points-for-rfc-b" tabindex="-1">Reserved extension points for RFC B <a class="header-anchor" href="#reserved-extension-points-for-rfc-b" aria-label="Permalink to “Reserved extension points for RFC B”">​</a></h3><p>This RFC reserves the following and defines nothing else about them:</p><ul><li>The optional <code>protocols</code>, <code>requestProtocol</code>, and <code>responseProtocol</code> members of <code>RouteHandler</code>. They are typed <code>never</code>, so TypeScript rejects them, and a handler that declares one throws while recording. Replay never imports a handler, so the type error is the only guard CI has. A validation step that silently does nothing is worse than none, because the author believes the fixture was checked.</li><li>The name <code>SafetyProtocols.sanitize</code>, for the relay-only sanitizing function.</li><li>The server route <code>/__relay</code>. Relay will return upstream responses to the browser, which records them through the record endpoint, so that endpoint stays the only writer of fixtures.</li></ul><p>RFC B is expected to type validation on Standard Schema v1, with Valibot as the documented example and no runtime dependency. RFC B decides it.</p><h3 id="changes-from-the-vision" tabindex="-1">Changes from the vision <a class="header-anchor" href="#changes-from-the-vision" aria-label="Permalink to “Changes from the vision”">​</a></h3><p>Where this RFC departs from #9616:</p><ul><li><code>startProgram</code> takes an object, and <code>createProgram</code> is synchronous and registers nothing. The vision started programs by name.</li><li>A program has a <code>store</code> factory. The vision did not say which store a program uses.</li><li>Seeds are JSON:API resource objects, and <code>serializeCache</code> mints deterministic ids instead of UUID v4.</li><li>Handlers return one <code>HandlerResult</code> shape, and <code>RouteHandler</code> compiles.</li><li>Behaviors have named <code>requestDelay</code> and <code>responseDelay</code> fields and no <code>delay</code> shorthand. <code>error.body</code> is JSON.</li><li>Safety protocols and relay move to RFC B, with Standard Schema rather than a Valibot dependency as the direction.</li><li>Local runs record by default, since #11177. The vision replayed by default.</li></ul><h3 id="phased-delivery" tabindex="-1">Phased delivery <a class="header-anchor" href="#phased-delivery" aria-label="Permalink to “Phased delivery”">​</a></h3><p>Each phase ships on its own and ends in a named gate, as in RFC 0001.</p><p><strong>Phase 0. Holodeck prerequisites.</strong> Each fixes a defect that suites without programs hit today.</p><ul><li>(a) Recording keys the fixture URL the way replay looks it up. Today a query with an unencoded comma or brackets records under one key and replays under another, so the replay misses.</li><li>(b) 5xx fixtures with a body replay. Today replay drops the body but keeps the recorded length header, and the browser sees a stream error.</li><li>(c) <code>delay</code>, an optional field on every mock helper and on program fixtures, which replay waits for.</li><li>(d) The request count and the mock count use one normalized URL key. This reaches suites without programs in one case: a test that requests one path from two origins gets one counter instead of two. Its fixtures already share a directory, so the change renumbers what already collided.</li><li>(e) A mock declared after a request to its method and URL was served throws <code>MockError: request #n for &lt;METHOD&gt; &lt;url&gt; was already served</code> instead of recording over that slot. Such a test already fails today, because the request ran with no fixture and the late fixture lands in a slot no later request reads, so no passing test is expected to change.</li></ul><p>Gate: record, then replay under <code>CI=1</code>, a <code>GET</code> with an unencoded comma and brackets in its query, a <code>503</code> with a JSON:API errors body, and a <code>GET</code> with <code>delay: 50</code> that takes at least 50 ms in replay. A mock declared after its request fails with the new error. Re-recording every suite leaves <code>.mock-cache</code> unchanged, apart from any directory (d) renumbers.</p><p><strong>Phase 1. Programs from a JSON seed.</strong> <code>createProgram</code>, <code>startProgram</code>, <code>Router</code>, <code>RouteHandler</code>, <code>ProgramContext</code>, fixture conversion, the per-test store and queue, the reserved members, the error messages, the <code>program</code> field, and the banner label. A test that started a program may not declare mocks yet. <code>mock()</code> throws and names Phase 2. The type blocks above ship whole in Phase 1, with the members marked <code>Phase 2</code> present but inert.</p><p>Gate: a program-served request records and replays under <code>CI=1</code> without importing the handler module. Two tests recording concurrently with different programs see only their own state. An unmatched route fails with its message.</p><p><strong>Phase 2. Behaviors, <code>updateProgram</code>, and overrides.</strong> All six behavior fields, the per-route counter, queue order for <code>requestDelay</code>, <code>after</code>, <code>updateProgram</code>, and override rules 2 to 4 in place of the Phase 1 ban.</p><p>Gate: a test with a program and an override has the same counters and the same unused-mock result in record and replay. An array behavior applies per program-served request on its route. <code>error</code> skips the handler. A request issued while another is in flight sees the state before its <code>after</code>.</p><p>The demo milestone closes Phase 2. It converts the seven <code>user</code> mocks in <code>tests/json-api/tests/integration/cache/mutation-request-test.ts</code> and <code>did-commit-notification-test.ts</code> to one <code>users and pets</code> program. The bulk create needs <code>id</code> and <code>patch</code> for its server-assigned ids. Tests that exercise one server quirk, such as a sparse or normalized response, keep an override. Gate: the converted tests record locally and replay under <code>CI=1</code> from committed fixtures, and every hand-written <code>user</code> response body the program now computes is gone. The repository has 27 mock declarations under <code>tests/</code>, so this proves the design. It is not a migration.</p><p><strong>Phase 3. <code>serializeCache</code>.</strong> <code>JSONAPICache.dump()</code> in core with its own tests, then the two-pass serializer and <code>mintId</code> in holodeck.</p><p>Gate: a <code>createRecord</code> seed with id-less records and relationships to them boots a program store. Running the seed function twice gives identical seeds, and re-recording a test that uses it leaves <code>.mock-cache</code> unchanged.</p><p>Phase 0 precedes Phase 1, because program fixtures need (a) and overrides need (d) and (e). Phase 2 needs Phase 0 (b) and (c). Phase 3 needs Phase 1 and the core change, and it can run alongside Phase 2.</p><h3 id="ecosystem-implications" tabindex="-1">Ecosystem implications <a class="header-anchor" href="#ecosystem-implications" aria-label="Permalink to “Ecosystem implications”">​</a></h3><ul><li>No lint rule is required, because the types reject reserved members. No deprecations.</li><li>The context argument is any object, as for <code>setTestId</code> today, so any test framework works.</li><li>Addons, SSR, Engines, the Inspector, IDE support, and blueprints do not change.</li></ul><h2 id="how-we-teach-this" tabindex="-1">How we teach this <a class="header-anchor" href="#how-we-teach-this" aria-label="Permalink to “How we teach this”">​</a></h2><p>&quot;HoloProgram&quot; and &quot;program&quot; stay. The guide introduces a program once as &quot;a Mirage-style scenario with a real WarpDrive store behind it&quot;, then says &quot;program&quot;. &quot;Behavior&quot; names per-route adjustments, &quot;override&quot; names a mock inside a program test, and &quot;fixture&quot; names what lands in <code>.mock-cache</code>. The guide does not borrow other tools&#39; words, such as &quot;cassette&quot; or &quot;scenario&quot;, as holodeck terms. The server setup page adds one sentence saying <code>launchProgram</code> and <code>endProgram</code> start and stop the holodeck server and are unrelated to HoloPrograms.</p><p>The HoloPrograms page in the testing guide (#11169) becomes the reference when Phase 2 ships. Its &quot;Proposed, not implemented&quot; banner goes, and its safety-protocol and VCR sections move to RFC B&#39;s page. The page teaches five things:</p><ul><li>When to use one. A mock answers one request. A program answers every request a test makes, from a store that behaves like the API. Use one when a test needs a world rather than a payload.</li><li>Programs run only while recording. Holodeck writes the answers to <code>.mock-cache</code> like any fixture, and replay serves them. Commit fixtures as for a mock.</li><li>Write the router once and share it. Import each handler lazily. Give each program a seed, a JSON file of resource objects with ids. Add behaviors for what a handler should not know, such as a slow endpoint.</li><li>Override one request with the mock helpers. A <code>PATCH(this, ...)</code> in a program test answers the next matching request, and that request leaves the program&#39;s store untouched.</li><li>Re-record after changing a program. Run the suite locally with <code>pnpm test</code>, commit the <code>.mock-cache</code> diff, and run <code>CI=1 pnpm test</code>.</li></ul><p>These pages exist only on #11169 today, so this section assumes it merges first:</p><ul><li>The testing index gains the program step in its record and replay diagram, and its HoloPrograms entry stops calling the page a design.</li><li>Writing mocks gains &quot;Override a program for one request&quot;.</li><li>Recording and replaying gains &quot;Fixtures a program wrote&quot;, with the <code>program</code> field and the re-record practice.</li><li>Troubleshooting gains the three program errors, indexed by their text.</li><li>Each export of <code>@warp-drive/holodeck/program</code> gets TSDoc with an example, and the package README links the guide page.</li><li>Phase 3 adds a &quot;Build a seed with <code>createRecord</code>&quot; section to the HoloPrograms page, covering <code>serializeCache</code> and <code>mintId</code>.</li></ul><p>An agent skill, <code>warp-drive-packages/memory-alpha/skills/holodeck/use-holoprograms-in-tests.md</code>, gets a row in the skills index. It covers starting a program, writing a handler, overriding a request, and re-recording after a change.</p><h2 id="drawbacks" tabindex="-1">Drawbacks <a class="header-anchor" href="#drawbacks" aria-label="Permalink to “Drawbacks”">​</a></h2><ul><li>Invalidation is manual. After a seed or handler change, fixtures of tests nobody ran locally stay stale and CI replays them. The <code>program</code> field reduces this but does not remove it.</li><li>Program code never runs in CI. Handlers, seeds, behaviors, <code>after</code>, and the reserved-member check run only while recording, so a handler that throws or a seed that fails DEBUG validation passes CI for as long as its stale fixtures exist. Only a local run catches it.</li><li>Recorded delays add to CI time. Replay waits for every fixture&#39;s <code>delay</code>, so a suite&#39;s replay grows by the sum of the delays its behaviors recorded.</li><li>Phase 0 (d) and (e) reach suites without programs. (d) renumbers fixtures a test recorded from two origins, and (e) turns a late mock into an error.</li><li>Bundle cost in record mode. Program objects and the router module are in the test bundle. Handlers and JSON seeds load through <code>import()</code>, so replay never fetches them, but the build still compiles them.</li><li>One extra round trip per program-served request while recording. Replay has none.</li><li>Two counters to reason about. Fixtures count per method and URL, while array behaviors count per method and pattern. An author of an array behavior has to know which one applies.</li><li>Overrides keep today&#39;s object-body defect. A program records the request text, but an override in a program test still has to pass its body as the exact string.</li><li>Windows paths. A query string puts <code>?</code> in a fixture directory name, which Windows cannot check out. JSON:API URLs often carry <code>include</code>, so programs produce more of them.</li><li>Dead fixtures. Nothing prunes or reports orphaned fixture directories (issue #11170). A program that changes which requests a screen makes leaves more behind.</li><li>Drift from the real API. Shared handlers imitate a server, and until RFC B nothing checks them against the real API. DEBUG cache validation checks resource shape, not endpoint behavior.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to “Alternatives”">​</a></h2><h3 id="one-rfc-instead-of-two" tabindex="-1">One RFC instead of two <a class="header-anchor" href="#one-rfc-instead-of-two" aria-label="Permalink to “One RFC instead of two”">​</a></h3><p>The vision bundles programs, safety protocols, and relay. Programs work with hand-written seeds and no real API, and static relay needs no program. Relay brings credentials, network egress, sanitized production data in git, and a dependency decision, each contestable on its own, and the vision left safety protocols unspecified. One RFC would hold programs until that design exists. RFC B depends on this one only for dynamic relay.</p><h3 id="server-side-programs" tabindex="-1">Server-side programs <a class="header-anchor" href="#server-side-programs" aria-label="Permalink to “Server-side programs”">​</a></h3><p>Programs could live in the server process, with state per test id, start and update endpoints, and modules loaded by path. Rejected. A <code>Store</code> does not load in plain Node without the application&#39;s build and macro configuration, and an Ember app&#39;s store service imports <code>@ember/*</code>, which does not load in Node at all. The server worker receives its options by structured clone, so <code>updateProgram</code> callbacks and <code>after</code> closures could not reach it. Isolation would rest on test id hygiene in one shared process instead of object identity. Its one advantage, fetching real APIs from Node, matters only for relay, and RFC B can add <code>/__relay</code> without moving programs.</p><h3 id="a-registered-name-api" tabindex="-1">A registered-name API <a class="header-anchor" href="#a-registered-name-api" aria-label="Permalink to “A registered-name API”">​</a></h3><p><code>startProgram(this, &#39;the-big-goodbye_chapter-13&#39;)</code>, as in the vision. It needs a registry, a module that fills it before tests run, a slug rule, and lazy loading by name, and a typo fails at run time instead of in the editor. The <code>name</code> field keeps a readable label without the registry.</p><h3 id="per-test-mocks-only-or-fixture-factories" tabindex="-1">Per-test mocks only, or fixture factories <a class="header-anchor" href="#per-test-mocks-only-or-fixture-factories" aria-label="Permalink to “Per-test mocks only, or fixture factories”">​</a></h3><p>Both keep one mock per request. Factories cut typing, but the test still restates every response and body string, and mock order still has to match request order.</p><h3 id="prior-art" tabindex="-1">Prior art <a class="header-anchor" href="#prior-art" aria-label="Permalink to “Prior art”">​</a></h3><p>Mirage&#39;s route handlers and ember-cli-mirage scenarios are the closest ancestor (<a href="https://miragejs.com/docs/main-concepts/route-handlers/" target="_blank" rel="noreferrer">route handlers</a>, <a href="https://www.ember-cli-mirage.com/versions/v0.4.x/seeding-your-database/" target="_blank" rel="noreferrer">seeding</a>). This RFC borrows the split between shared handlers and per-situation data, but it uses the real store instead of Mirage&#39;s second model layer (<a href="https://miragejs.com/docs/main-concepts/models/" target="_blank" rel="noreferrer">models</a>) and never patches the page&#39;s <code>fetch</code>, whose Pretender polyfill corrupts binary responses (<a href="https://github.com/miragejs/ember-cli-mirage/issues/1915" target="_blank" rel="noreferrer">ember-cli-mirage#1915</a>). Mirage&#39;s last npm release is 0.1.48, from 2023-10-30.</p><p>MSW&#39;s initial handlers plus runtime overrides map onto shared handlers plus in-test overrides (<a href="https://mswjs.io/docs/best-practices/network-behavior-overrides/" target="_blank" rel="noreferrer">network behavior overrides</a>), <code>once</code> maps onto array behaviors, and its unhandled-request policy (<a href="https://mswjs.io/docs/api/setup-server/listen/" target="_blank" rel="noreferrer">listen</a>) is why an unmatched route fails loudly here. <code>@msw/data</code> typing collections on Standard Schema is precedent for RFC B. MSW keeps recording out of its core, and its author warns that &quot;a recording of your traffic is not a specification for your API&quot; (<a href="https://mswjs.io/blog/introducing-source/" target="_blank" rel="noreferrer">Introducing Source</a>). Holodeck is record and replay first, and that is the answer to #9627. Holodeck does not intercept <code>fetch</code>, it commits fixtures per test so tests replay concurrently with no setup cost, and programs build responses from the application&#39;s own schemas. A WarpDrive-aware MSW plugin would give up committed per-test fixtures.</p><p>Polly.js is the closest analogue to record and replay with fixtures. Its <code>order</code> matcher is holodeck&#39;s per-URL counter, <code>.times(n)</code> and <code>intercept</code> map onto behaviors and overrides (<a href="https://raw.githubusercontent.com/Netflix/pollyjs/master/docs/configuration.md" target="_blank" rel="noreferrer">configuration</a>), and its <code>beforePersist</code> hook is where RFC B&#39;s sanitizing will sit. This RFC avoids its HAR files and default header matching, which make recordings sensitive to noise. A local holodeck run is the Rails VCR gem&#39;s <code>:all</code> mode and a CI run is <code>:none</code> (<a href="https://raw.githubusercontent.com/vcr/vcr/master/features/record_modes/all.feature" target="_blank" rel="noreferrer">record modes</a>). <code>re_record_interval</code> is not borrowed, because time-based re-recording churns committed fixtures. WireMock models state as named scenario states (<a href="https://wiremock.org/docs/stateful-behaviour/" target="_blank" rel="noreferrer">stateful behaviour</a>); this RFC models state as data in a store, because named states multiply with every combination a test needs. Playwright&#39;s <code>routeFromHAR</code> serves the first match for every identical request, and recording state-changing POSTs was declined (<a href="https://github.com/microsoft/playwright/issues/32272" target="_blank" rel="noreferrer">#32272</a>, <a href="https://github.com/microsoft/playwright/issues/28167" target="_blank" rel="noreferrer">#28167</a>). Holodeck&#39;s per-URL counter avoids the first problem, and programs address the second by computing mutation responses from a store.</p><h2 id="unresolved-questions" tabindex="-1">Unresolved questions <a class="header-anchor" href="#unresolved-questions" aria-label="Permalink to “Unresolved questions”">​</a></h2><p>Open items, to settle during the Exploring stage with the Data team. The last item lists what this RFC defers to RFC B.</p><ol><li>Fixture invalidation. This RFC relies on the manual practice in <a href="#the-fixture-contract">The fixture contract</a>. Automatic detection, such as a program hash in <code>meta.json</code> checked in replay, stays open. Hashing handler source in the browser is not reliable.</li><li>Deterministic ids. Whether the default <code>mintId</code> should look like a UUID.</li><li><code>QUERY</code>. The router accepts it, but the server&#39;s CORS list omits it and no mock helper exists.</li><li>Other cache formats, and the cache layouts in #11154. Seeds and <code>updateProgram</code> are defined for JSON:API resource objects only.</li><li>Delays while recording. A recording run spends every delay twice. The options are in the decision note under <a href="#behaviors">Behaviors</a>.</li><li>Keying the request count by normalized URL (Phase 0 d). Picked here. Tests with two origins and one path renumber on re-record.</li><li>Batching program fixtures to save the extra round trip per request. Likely not worth an endpoint.</li><li>For core: whether <code>JSONAPICache.dump()</code> also emits documents, which seeds do not need.</li><li>Matching on origin, which this RFC ignores because fixtures ignore it.</li><li>Streaming or non-JSON handler bodies, which the server cannot store today.</li><li>A per-program or per-test <code>RECORD</code>. Programs follow the suite&#39;s mode only.</li><li>A mode that records only missing fixtures, like VCR&#39;s <code>:once</code>, so local runs stop rewriting fixtures they did not need. Independent of programs.</li><li><code>launchServer</code> and <code>endServer</code> aliases for the server functions.</li><li>Querying the program store with AQL (#11087).</li><li>A Node-runnable core, which relay or a seed CLI would need. RFC 0002&#39;s build plugin does not cover it, and core reads <code>window</code> without a guard in two places.</li><li>Deferred to RFC B: the design of safety protocols, suppressing transforms outside relay, where a static-relay protocol comes from, Valibot versus Standard Schema, relay configuration and credentials, dynamic relay scope, mutation handling, and generated program output.</li></ol>`,121)])}var f=a(l,[[`render`,d]]);export{c as __pageData,f as default};
/**
 * @module
 * @summary Build-time flags, such as `JSON_API_CACHE_VALIDATION_ERRORS`, that gate unreleased WarpDrive features for
 * canary builds.
 */

export * from '@warp-drive/build-config/canary-features';

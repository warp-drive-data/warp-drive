/**
 * @module
 * @summary The `macros` function, which returns the Babel plugins that strip WarpDrive deprecation, canary-feature,
 * debug-logging and env code at build time.
 */

export { macros } from '@warp-drive/build-config/babel-macros';

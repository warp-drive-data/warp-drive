/**
 * @module
 * @summary Deprecation flags, such as `DEPRECATE_NON_STRICT_TYPES`, that keep deprecated WarpDrive behavior active
 * until the build config drops it by `compatWith` version or by flag.
 */

export * from '@warp-drive/build-config/deprecations';

/**
 * @module
 * @summary Experimental `WorkerFetch` request handler that sends requests from the main thread to a `DataWorker` and
 * settles them with its responses.
 */

export { WorkerFetch } from './data-worker/fetch';

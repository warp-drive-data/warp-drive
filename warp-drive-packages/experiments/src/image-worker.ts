/**
 * {@include ./image-worker.md}
 *
 * @summary Experimental worker-side image loader that fetches and caches images for main-thread `ImageFetch` clients.
 * @module
 */
export { ImageWorker } from './image-worker/worker';

/**
 * {@include ./image-worker.md}
 *
 * @summary Experimental main-thread client for loading images through an `ImageWorker` running in a SharedWorker.
 * @module
 */
export { ImageFetch } from './image-worker/fetch';

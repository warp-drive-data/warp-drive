/**
 * @module
 * @summary Experimental `DocumentStorage`, a persistent store for request documents and their resources backed by the
 * Origin Private File System and synced across tabs.
 */

export { DocumentStorage } from './document-storage/index';

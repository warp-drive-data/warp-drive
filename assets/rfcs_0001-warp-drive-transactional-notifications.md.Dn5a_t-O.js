import{Ct as e,at as t,it as n,ot as r,st as i,t as a,tt as o,yt as s}from"./chunks/framework.CI0evN9J.js";var c=JSON.parse(`{"title":"Transactional Notification Delivery and Reactive Request State","description":"","frontmatter":{"title":"Transactional Notification Delivery and Reactive Request State","warp-drive-rfc":1,"emberjs-rfc":null,"emberjs-pr":null,"emberjs-branch":null,"sync-hash":null,"stage":"proposed","start-date":"2026-09-02T00:00:00.000Z","release-date":null,"release-versions":null,"teams":["data"],"prs":{"accepted":null},"project-link":null,"suite":null},"headers":[],"relativePath":"rfcs/0001-warp-drive-transactional-notifications.md","filePath":"rfcs/0001-warp-drive-transactional-notifications.md"}`),l={name:`rfcs/0001-warp-drive-transactional-notifications.md`},u={id:`transactional-notification-delivery-and-reactive-request-state`,tabindex:`-1`};function d(a,c,l,d,f,p){let m=e(`StatusBadge`);return s(),n(`div`,null,[o(`h1`,u,[c[0]||=r(`Transactional Notification Delivery and Reactive Request State `,-1),i(m,{variant:`proposed`,text:`Proposed`}),c[1]||=r(),c[2]||=o(`a`,{class:`header-anchor`,href:`#transactional-notification-delivery-and-reactive-request-state`,"aria-label":`Permalink to “Transactional Notification Delivery and Reactive Request State”`},`​`,-1)]),c[3]||=t(`<h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to “Summary”">​</a></h2><p>WarpDrive currently delivers cache-change notifications under five distinct timing policies that grew independently: immediate per-call delivery, transaction-deferred delivery (relationships only), request-window buffering (<code>_enableAsyncFlush</code>), a synchronous-render escape hatch (<code>willSyncFlushWatchers</code>), and consumer-forced flushes. This RFC replaces all five with a single rule — <strong>every write to store-managed state happens inside a transaction, and all notifications from a transaction are delivered in one synchronous batch when it closes</strong> — and rebuilds the request-state layer (<code>RequestStateService</code>) as transactionally-written, signals-backed data so that record data and request status can never be observed out of sync.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to “Motivation”">​</a></h2><h3 id="the-problem-five-flush-policies-zero-mental-models" tabindex="-1">The problem: five flush policies, zero mental models <a class="header-anchor" href="#the-problem-five-flush-policies-zero-mental-models" aria-label="Permalink to “The problem: five flush policies, zero mental models”">​</a></h3><p>The <code>NotificationManager</code> is how WarpDrive tells reactive layers (records, record arrays, documents, request states) that cache state changed. When a notification is <em>delivered</em> determines what a renderer or an interleaved promise continuation observes. Today that timing depends on which of five policies a given write happens to hit:</p><ol><li><strong>Immediate</strong>: a bare <code>notify()</code> (attributes, state, added/removed, …) flushes the entire buffer synchronously before returning — including mid-<code>cache.put</code>, while a payload is half-applied.</li><li><strong>Transaction-deferred</strong>: relationship notifications from the graph defer to the notify phase of the store&#39;s internal transaction (<code>store._schedule(&#39;notify&#39;)</code>), so they are never dispatched mid-graph-sync.</li><li><strong>Request windows</strong>: request/push processing sets <code>_enableAsyncFlush</code>, buffers everything, and relies on an explicit <code>notifications._flush()</code> later — hand-rolled in five places with five slightly different shapes.</li><li><strong>Sync-render bypass</strong>: when the configured signals implementation reports <code>willSyncFlushWatchers()</code>, buffering is skipped so a synchronous render cannot observe stale state.</li><li><strong>Forced flushes</strong>: internal consumers (legacy relationship support, the request cache via <code>_onNextFlush</code>) flush or sequence against the buffer directly because none of the above gives them a reliable &quot;the world is consistent now&quot; point.</li></ol><p>These are all partial answers to one question — <em>when is the current unit of work done?</em> — using three incompatible definitions of &quot;unit of work.&quot;</p><h3 id="the-concrete-failures-this-causes-today" tabindex="-1">The concrete failures this causes today <a class="header-anchor" href="#the-concrete-failures-this-causes-today" aria-label="Permalink to “The concrete failures this causes today”">​</a></h3><p><strong>Torn synchronous reads.</strong> Between a response&#39;s <code>cache.put</code> and its finalize-time flush there are several microtask hops. Any code that runs in those gaps sees an incoherent mix: <code>peekRecord</code> returns a record that <code>peekAll</code> does not yet contain; a record field read before the request returns its stale memoized value while a never-read field on the same record computes fresh from the new cache.</p><p><strong>Cross-request mingling.</strong> The notification buffer is shared. When two responses are processed in adjacent microtasks, both buffer into the same map and the first finalize flush delivers them <strong>mingled</strong> — response B&#39;s record data publishes before B&#39;s own request state transitions, which is exactly the tear the buffering was built to prevent.</p><p><strong>Data / request-state skew.</strong> Request states transition on promise hops (a <code>.then</code> inside the request cache) and are stitched back to the notification flush via a one-shot <code>_onNextFlush</code> callback. The coupling is fragile and only covers the blocking-request path; everywhere else (bare <code>store.push</code>, non-blocking priorities, the sync-render bypass) data and request state publish at different instants.</p><p><strong>Lost notifications on teardown.</strong> A notification buffered during a transaction is silently dropped if the same transaction tears down its subscriber (e.g. <code>deleteRecord</code> on a new record unloads it, unsubscribing the legacy <code>RecordState</code> before the flush). Consumers work around this by depending on <em>immediate</em> delivery, which is what blocks unifying the policies. (This was surfaced concretely while removing the duplicate relationship buffer in <a href="https://github.com/warp-drive-data/warp-drive/pull/10992" target="_blank" rel="noreferrer">warp-drive#10992</a>.)</p><h3 id="the-expected-outcome" tabindex="-1">The expected outcome <a class="header-anchor" href="#the-expected-outcome" aria-label="Permalink to “The expected outcome”">​</a></h3><ul><li>One documented delivery contract instead of five implicit ones.</li><li>Synchronous APIs (<code>peekRecord</code>, <code>peekAll</code>, relationship access) can never observe a torn world: the buffer is provably empty at every yield point.</li><li>Record data and request status for one response always publish in the same synchronous instant.</li><li>Per-response batch isolation under concurrent requests.</li><li>Deletion of the accidental machinery: <code>_enableAsyncFlush</code>, all explicit <code>_flush()</code> call sites, <code>_onNextFlush</code>, the relationships-only carve-out, and the <code>willSyncFlushWatchers</code> bypass.</li></ul><h2 id="detailed-design" tabindex="-1">Detailed design <a class="header-anchor" href="#detailed-design" aria-label="Permalink to “Detailed design”">​</a></h2><h3 id="terminology" tabindex="-1">Terminology <a class="header-anchor" href="#terminology" aria-label="Permalink to “Terminology”">​</a></h3><ul><li><strong>Transaction</strong>: the synchronous unit of work in which store-managed state is written. An upgrade of the existing internal <code>store._run</code> / <code>store._join</code> mechanism; not a database-style transaction (no rollback).</li><li><strong>Publication</strong>: delivery of all notifications buffered during a transaction, in one synchronous batch at its close.</li></ul><h3 id="the-invariant" tabindex="-1">The invariant <a class="header-anchor" href="#the-invariant" aria-label="Permalink to “The invariant”">​</a></h3><blockquote><p>Every write to store-managed state (cache content, graph edges, request lifecycle) happens inside a transaction. All notifications emitted during a transaction are delivered in one synchronous batch when the outermost transaction closes. The notification buffer is empty at every yield point.</p></blockquote><p>Because delivery is always synchronous within the frame that performed the writes, code outside the store can only ever execute in one of two states: <em>before</em> a transaction (old cache, old memoization) or <em>after</em> one (new cache, all signals invalidated) — both coherent. The &quot;cache updated but notifications pending&quot; window that torn reads and interleaving bugs live in becomes unrepresentable outside store internals.</p><h3 id="_1-transactions-phase-queues" tabindex="-1">1. Transactions: phase queues <a class="header-anchor" href="#_1-transactions-phase-queues" aria-label="Permalink to “1. Transactions: phase queues”">​</a></h3><p>The existing internal mechanism (<code>_run</code>/<code>_join</code>/<code>_schedule</code>) already defines phases; this RFC upgrades its single-slot callbacks to queues and adds one phase:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>mutate → coalesce → sync → notify → cleanup</span></span></code></pre></div><ul><li><code>mutate</code> is the transaction body; <code>coalesce</code> and <code>sync</code> are the graph&#39;s existing remote/local settling phases, unchanged in role.</li><li><code>notify</code> drains the notification buffer <strong>until empty</strong>. A mutation performed by a subscriber during delivery joins the open transaction and lands in the same drain (with a dev-mode depth guard against subscriber ping-pong, replacing today&#39;s single-slot assertion).</li><li><code>cleanup</code> (new): record teardown, unsubscription, and identifier disconnect run here — strictly after delivery. This structurally fixes the lost-notification-on-teardown class: the world is told before things die.</li><li>Nested transactions join the outermost one; only its close runs phases.</li></ul><p>The internal shape of the transaction object is perf-sensitive (it wraps every write in hot paths) and is left as an explicit unresolved question below; the phase <em>semantics</em> above are the normative part.</p><h3 id="_2-notificationmanager-single-buffer-single-trigger-two-pass-delivery" tabindex="-1">2. NotificationManager: single buffer, single trigger, two-pass delivery <a class="header-anchor" href="#_2-notificationmanager-single-buffer-single-trigger-two-pass-delivery" aria-label="Permalink to “2. NotificationManager: single buffer, single trigger, two-pass delivery”">​</a></h3><ul><li>The per-identifier, per-namespace, channel-aware buffer (as of warp-drive#10560 / #10992) is kept. The <strong>only</strong> flush trigger is the notify phase. A <code>notify()</code> call with no open transaction opens a degenerate one (precedent: <code>graph.push</code> already self-wraps in <code>_run</code> today).</li><li>Delivery is <strong>two-pass</strong>: internal subscribers first (reactive records, record arrays, documents, request states — everything whose callback is signal invalidation), then external subscribers. After pass one, every memoized value is coherent, so even a synchronous read from inside a user&#39;s subscription callback sees a consistent world.</li><li>Deleted: the immediate-flush path, <code>_enableAsyncFlush</code> coupling, <code>_onNextFlush</code>, the relationships-only deferral carve-out, and the <code>willSyncFlushWatchers()</code> check — nothing buffers across a yield, so there is nothing a synchronous watcher flush could miss.</li></ul><p>The delivery contract, documented as public API:</p><blockquote><p>A notification emitted while you are subscribed is delivered to you exactly once, at the close of the emitting transaction, before any same-transaction teardown, always against a fully-settled cache.</p></blockquote><h3 id="_3-request-state-transactionally-written-signals-backed" tabindex="-1">3. Request state: transactionally-written, signals-backed <a class="header-anchor" href="#_3-request-state-transactionally-written-signals-backed" aria-label="Permalink to “3. Request state: transactionally-written, signals-backed”">​</a></h3><p>The <code>RequestStateService</code> is replaced by lazily-created, signal-backed state records — per <code>RequestKey</code>, and per <code>ResourceKey</code> for mutations:</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RequestState</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  status</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;pending&#39;</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;fulfilled&#39;</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;rejected&#39;</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;aborted&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  isMutation</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  request</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ImmutableRequestInfo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  response</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ResponseInfo</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  error</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Error</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Transitions are plain writes performed <strong>inside the owning transaction</strong>: <code>pending</code> at dispatch; <code>fulfilled</code>/<code>rejected</code> in the same transaction as <code>put</code>/<code>didCommit</code>/<code>commitWasRejected</code>. The tear between record data and <code>isSaving</code>/<code>isLoading</code> becomes unrepresentable because both are published by the same flush. Reactive consumers do not subscribe at all — they read the state and autotrack.</p><p>The exact public API shape for consuming these (and for the imperative subscription escape hatch that replaces <code>subscribeForRecord</code>) is an unresolved question below. The sunset path is not: <code>getRequestStateService</code> and <code>subscribeForRecord</code> remain as a compatibility shim — an internal pass-two subscriber replaying transitions to old callbacks — behind a deprecation (id <code>warp-drive:deprecate-request-state-subscriptions</code>, <code>until: 6.0</code>, gated by a build-config flag following the existing <code>ENABLE_LEGACY_*</code> pattern).</p><h3 id="_4-who-owns-the-transaction-async-paths" tabindex="-1">4. Who owns the transaction (async paths) <a class="header-anchor" href="#_4-who-owns-the-transaction-async-paths" aria-label="Permalink to “4. Who owns the transaction (async paths)”">​</a></h3><p>Three cooperating layers:</p><table tabindex="0"><thead><tr><th>Layer</th><th>Role</th></tr></thead><tbody><tr><td><strong>CacheManager</strong> (floor)</td><td>Every cache write method (<code>put</code>, <code>didCommit</code>, <code>commitWasRejected</code>, <code>patch</code>, <code>mutate</code>, <code>upsert</code>, <code>setIsDeleted</code>, …) joins-or-opens a transaction. This guards against direct calls: a custom handler invoking <code>cache.put</code> bare still gets a single coherent publication.</td></tr><tr><td><strong>CacheHandler and the legacy network handler</strong> (primary)</td><td>Each response-application frame opens one transaction spanning cache application, reactive document setup, <code>lifetimes.didRequest</code>, and the request-state transition. One publication per response — which also provides per-response isolation that today&#39;s shared finalize flush cannot.</td></tr><tr><td><strong>RequestManager</strong> (sentinel)</td><td>It cannot own the transaction — the handler chain spans real async I/O and transactions are synchronous. Instead it writes the <code>pending</code> transition at dispatch, and at settle asserts (dev-mode) that a terminal transition was written — opening a fallback micro-transaction when a request bypassed the CacheHandler — and that the buffer is empty.</td></tr></tbody></table><p>The current finalize-time flush inside <code>store.request()</code> becomes a dev-mode assertion that the buffer is already empty: the permanent proof that the invariant holds.</p><h3 id="_5-synchronous-apis" tabindex="-1">5. Synchronous APIs <a class="header-anchor" href="#_5-synchronous-apis" aria-label="Permalink to “5. Synchronous APIs”">​</a></h3><p><code>store.push</code>, <code>createRecord</code>, <code>deleteRecord</code>, <code>unloadRecord</code>, <code>unloadAll</code>, and graph mutations already run inside <code>_join</code>; they inherit the model with no call-site changes. The bolt-ons are deleted: <code>_push</code> loses its <code>asyncFlush</code> parameter, <code>unloadAll</code>&#39;s hand-rolled flag/flush/pause-resume becomes transaction + cleanup-phase work, and the forced flushes in legacy relationship support are removed.</p><p>A public batching primitive falls out for free and may be exposed once the internals stabilize:</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">store.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">batch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  store.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">push</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(payloadA);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  store.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">push</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(payloadB);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  record.name </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Chris&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}); </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// one publication</span></span></code></pre></div><h3 id="_6-migration-sequence" tabindex="-1">6. Migration sequence <a class="header-anchor" href="#_6-migration-sequence" aria-label="Permalink to “6. Migration sequence”">​</a></h3><p>Each phase is independently shippable and behavior-gated by WarpDrive&#39;s existing test matrix (<code>main-test-app</code>, graph, json-api, legacy, ember framework suites):</p><ol><li><strong>Instrumentation</strong>: tag buffered notifications with a request id; demonstrate today&#39;s cross-request mingling.</li><li><strong>Phase queues + <code>cleanup</code></strong>; move teardown into it. Gate: the notify-then-unload scenario (the failure mode found in #10992) passes under full deferral.</li><li><strong>CacheManager floor</strong>; <code>notify()</code> self-wraps; delete the relationships-only carve-out. Gate: <code>peekAll</code>/<code>peekRecord</code> agreement; bare-<code>cache.put</code> custom-handler test.</li><li><strong>Request state v2</strong> + compat shim + deprecation. Gate: request-state suites; an <code>isSaving</code>-vs-data correlation test.</li><li><strong>Handler-owned transactions</strong>; delete <code>_enableAsyncFlush</code> and all explicit flushes; finalize flush becomes an assertion. Gate: a two-concurrent-responses isolation test; the assertion stays silent across all suites.</li><li><strong>Two-pass delivery</strong>; remove <code>willSyncFlushWatchers</code> from the NotificationManager; optionally expose <code>store.batch</code>.</li></ol><p>Ordering constraints: (2) must precede (3) — the cleanup phase is what makes full deferral sound; (4) must precede (5) — otherwise deleting the request window reopens the data/request-state tear.</p><h3 id="ecosystem-implications" tabindex="-1">Ecosystem implications <a class="header-anchor" href="#ecosystem-implications" aria-label="Permalink to “Ecosystem implications”">​</a></h3><ul><li><strong>Deprecations</strong>: <code>getRequestStateService</code> / <code>subscribeForRecord</code> (see §3); the <code>asyncFlush</code> parameter of <code>store._push</code> (intimate API).</li><li><strong>Addon ecosystem</strong>: custom <code>Cache</code> implementations and custom request handlers are the affected extension points; both get <em>stronger</em> guarantees with no interface change (the CacheManager floor wraps them). Addons that subscribe to notifications and depend on mid-mutation delivery timing will observe changes; the delivery contract in §2 becomes the documented guarantee.</li><li><strong>Server-side rendering</strong>: publication is always synchronous within the mutating frame, which is strictly friendlier to SSR than microtask-timed or scheduler-timed delivery.</li><li><strong>Debuggability / Ember Inspector</strong>: <code>LOG_NOTIFICATIONS</code> gains a stable story — every notification logs a buffer event and a publication event with a transaction id, replacing today&#39;s regime-dependent logs.</li><li>Lint rules, blueprints, Engines, IDE support: no changes.</li></ul><h2 id="how-we-teach-this" tabindex="-1">How we teach this <a class="header-anchor" href="#how-we-teach-this" aria-label="Permalink to “How we teach this”">​</a></h2><p>The concept maps to vocabulary developers already have from databases and other reactive systems: <em>writes happen in transactions; observers see transaction boundaries, never intermediate states</em>. Teaching materials need one new sentence more than they have today, and several fewer caveats:</p><ul><li>Guides: the &quot;reactivity&quot; section of the WarpDrive docs gains a short &quot;when do observers see changes?&quot; passage stating the delivery contract. Existing guidance about awaiting settled state is unchanged.</li><li>API docs: <code>NotificationManager.subscribe</code> documents the contract in §2; the new request-state read APIs are documented alongside <code>getRequestState</code> usage they replace.</li><li>Existing users are reached through the deprecation guide entry for <code>warp-drive:deprecate-request-state-subscriptions</code>, which shows the mechanical migration from <code>subscribeForRecord</code> callbacks to reading signals-backed request state.</li></ul><p>No reorganization of the guides is required; this removes special cases rather than adding concepts.</p><h2 id="drawbacks" tabindex="-1">Drawbacks <a class="header-anchor" href="#drawbacks" aria-label="Permalink to “Drawbacks”">​</a></h2><ul><li><strong>Notification timing is observable.</strong> Ad-hoc attribute notifications move from mid-mutation to transaction close. Application tests that assert intermediate states, and addons that rely on immediate delivery, will notice. The phase-by-phase landing plan exists precisely to surface these in WarpDrive&#39;s own suites first.</li><li><strong>Churn in intimate APIs.</strong> Several semi-public internals (<code>getRequestStateService</code>, <code>_enableAsyncFlush</code>, forced <code>_flush()</code>) are load bearing in the wild despite their markings; the compatibility shim and deprecation window carry real maintenance cost until 6.0.</li><li><strong>A transaction wrapper on every write is hot-path code.</strong> If the internal shape is wrong, this trades a correctness problem for a performance one (see unresolved questions).</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to “Alternatives”">​</a></h2><ul><li><strong>Status quo</strong>: keep five policies and continue patching consumers (the relationships-only carve-out shipped in warp-drive#10992 is exactly such a patch, and this RFC deletes it). The accidental complexity keeps compounding: #10560 had to make the now-removed duplicate buffer channel-aware precisely because the policies could not be unified.</li><li><strong>Microtask-scheduled flushing</strong> (deliver on the next microtask rather than at transaction close): simpler to implement, but reintroduces the torn-read window for synchronous APIs, breaks synchronous renderers, and makes SSR timing fragile. Rejected.</li><li><strong>Per-namespace policies</strong> (formalize &quot;relationships defer, everything else is immediate&quot;): preserves today&#39;s observable timing exactly, but hard-codes the very incoherence this RFC removes and leaves the request-window machinery intact. This is the shipped interim state, not the destination.</li><li><strong>Prior art</strong>: MobX (<code>runInAction</code>/transactions with reactions at action close), Vue (<code>nextTick</code>-batched effects), Solid (<code>batch()</code>), and Svelte 5 (microtask-batched effects) all converge on &quot;writes batch; observers see batch boundaries.&quot; The transaction-close (rather than microtask) timing here is what preserves WarpDrive&#39;s promise-resolution ordering guarantees.</li></ul><h2 id="unresolved-questions" tabindex="-1">Unresolved questions <a class="header-anchor" href="#unresolved-questions" aria-label="Permalink to “Unresolved questions”">​</a></h2><ol><li><strong>Public API shape for reactive request subscriptions.</strong> The signals-backed request state (§3) needs a consumer-facing read API — a <code>store.requestStateFor(keyOrRecord)</code> accessor, properties on <code>ReactiveDocument</code>, both, or something else — and a decision on whether any imperative subscription API survives beyond the deprecation shim. Strawman sketches exist but the shape should be settled during the Exploring stage with the Data team.</li><li><strong>The internal shape of the transaction.</strong> Phase queues, the join fast-path, and buffer representation sit on the hottest write paths in the library. Options include a pooled transaction object, a monomorphic phase array, or retaining the current <code>_cbs</code>-style record with queues only where re-entrancy is possible. This needs benchmarks (notification-heavy push/mutation workloads) before the shape is fixed.</li><li><strong>Entanglement with the Render Aware Scheduler interface (<a href="https://github.com/emberjs/rfcs/pull/957" target="_blank" rel="noreferrer">emberjs/rfcs#957</a>).</strong> That proposal makes promise interleaving safer by giving the framework a render-aware notion of when work flushes relative to the browser&#39;s event loop. If it lands, some guarantees this RFC achieves by <em>synchronous</em> publication (notably the sync-render bypass deletion, and how external pass-two subscribers are scheduled) could instead be expressed as scheduler lanes — potentially allowing external delivery to be deferred off the critical path without reopening the torn-read window. The two proposals should be kept explicitly compatible: this RFC&#39;s invariant is the stronger (synchronous) guarantee, and adopting #957 would relax only the <em>external</em> delivery pass, never the internal one.</li><li><strong><code>store.batch</code> as public API</strong>: name, whether it is sugar over the internal transaction or a distinct concept, and whether it may be opened during an active flush.</li></ol>`,58)])}var f=a(l,[[`render`,d]]);export{c as __pageData,f as default};
import{Ct as e,at as t,it as n,ot as r,st as i,t as a,tt as o,yt as s}from"./chunks/framework.CI0evN9J.js";var c=JSON.parse(`{"title":"A Browser Devtools Extension","description":"","frontmatter":{"title":"A Browser Devtools Extension","warp-drive-rfc":3,"emberjs-rfc":null,"emberjs-pr":null,"emberjs-branch":null,"sync-hash":null,"stage":"proposed","start-date":"2026-09-23T00:00:00.000Z","release-date":null,"release-versions":null,"teams":["data","framework"],"prs":{"accepted":null},"project-link":null,"suite":null},"headers":[],"relativePath":"rfcs/0003-warp-drive-devtools-extension.md","filePath":"rfcs/0003-warp-drive-devtools-extension.md"}`),l={name:`rfcs/0003-warp-drive-devtools-extension.md`},u={id:`a-browser-devtools-extension`,tabindex:`-1`};function d(a,c,l,d,f,p){let m=e(`StatusBadge`);return s(),n(`div`,null,[o(`h1`,u,[c[0]||=r(`A Browser Devtools Extension `,-1),i(m,{variant:`proposed`,text:`Proposed`}),c[1]||=r(),c[2]||=o(`a`,{class:`header-anchor`,href:`#a-browser-devtools-extension`,"aria-label":`Permalink to “A Browser Devtools Extension”`},`​`,-1)]),c[3]||=t(`<h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to “Summary”">​</a></h2><p>WarpDrive gains a dedicated browser devtools extension (Chrome and Firefox, Manifest V3) built with Ember and WarpDrive itself. A tiny, production-strippable hook shipped inside <code>@warp-drive/core</code> lets the extension attach to any WarpDrive <code>Store</code> on a page with zero app-side setup, the same pattern React DevTools and Redux DevTools use. The extension gives visibility and control that <code>@ember-data/debug</code>&#39;s ember-inspector integration cannot: a live request inspector with per-request actions (invalidate, reload, background-reload, force a pending/error state), a cache explorer over the full schema-driven cache (not just <code>Model</code> records), a schema/relationship-graph explorer, on-page highlighting of <code>&lt;Request&gt;</code>-rendered DOM with hover actions, and runtime toggles for WarpDrive&#39;s existing debug-log flags. It ships in three phases behind one architecture and one versioned protocol, described in full below.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to “Motivation”">​</a></h2><p>WarpDrive apps today have exactly one inspection tool: <code>@ember-data/debug</code>&#39;s ember-inspector <code>DataAdapter</code> integration. It has structural limits that this RFC&#39;s target audience — anyone debugging a real <code>Store</code> — runs into immediately:</p><ul><li>It only understands <code>@ember-data/model</code>&#39;s <code>Model</code> class (<code>store.modelFor(type).attributes</code>, <code>store.peekAll(type)</code>). Apps on <code>SchemaRecord</code>/reactive resources, which is the direction WarpDrive&#39;s own architecture has moved, are invisible to it.</li><li>It exposes per-type record lists and attributes — nothing about <code>RequestManager</code>, in-flight or completed requests, <code>Cache</code> documents, or the relationship graph as a whole.</li><li>It has no concept of &quot;what triggered this fetch&quot; or &quot;is this data still in use anywhere,&quot; both of which come up constantly when chasing down redundant requests or stale-cache bugs.</li><li>It is a plugin <em>inside</em> ember-inspector&#39;s own protocol and panel, so it cannot add its own panel layout, an on-page overlay, or interactive graph visualization — ember-inspector&#39;s UI shell is not built for that.</li></ul><p>Issue <a href="https://github.com/warp-drive-data/warp-drive/issues/10407" target="_blank" rel="noreferrer">#10407</a> already collects this wishlist from real usage: tracing requests, clean/dirty record state, cache exploration, invalidating requests, highlighting DOM inside request/await boundaries, and visual schema and relationship-graph exploration. Issue <a href="https://github.com/warp-drive-data/warp-drive/issues/9518" target="_blank" rel="noreferrer">#9518</a> (&quot;cache v3 exploration&quot;) independently flags that the <code>Cache</code> interface has no capability for asking &quot;is this request / resource active,&quot; which several of the features below need — this RFC treats that as a dependency to coordinate on, not something to route around.</p><p>Prior art in adjacent ecosystems validates the shape: React DevTools and Redux DevTools both work via a page-injected global hook plus a three-hop extension bridge; Apollo Client Devtools and TanStack Query Devtools both ship a request/query list with per-item state and a normalized cache explorer. None of them do on-page DOM highlighting tied to data-fetching boundaries — that piece is closer to what React DevTools does for the component tree, applied instead to WarpDrive&#39;s <code>&lt;Request&gt;</code> boundaries.</p><p>The expected outcome: a WarpDrive app is inspectable and controllable from a standalone extension the moment the extension is installed, with no app-side integration step, no <code>Model</code>-only limitation, and a UI shell expressive enough for a relationship graph and an on-page overlay.</p><h2 id="detailed-design" tabindex="-1">Detailed design <a class="header-anchor" href="#detailed-design" aria-label="Permalink to “Detailed design”">​</a></h2><h3 id="why-a-browser-extension-and-not-an-embedded-devtools-component" tabindex="-1">Why a browser extension, and not an embedded devtools component <a class="header-anchor" href="#why-a-browser-extension-and-not-an-embedded-devtools-component" aria-label="Permalink to “Why a browser extension, and not an embedded devtools component”">​</a></h3><p>TanStack Query and Apollo both primarily ship an <em>in-app</em> embedded devtools component (a floating panel the app itself renders in development). That approach cannot do on-page DOM highlighting of <code>&lt;Request&gt;</code> boundaries from outside the app&#39;s own render tree, and it requires every app to remember to mount it. A browser extension can overlay any page, needs no app-side mounting, and is the natural home for a relationship-graph visualizer that wants real screen space. This RFC does not preclude an embedded companion later — the panel UI described below is itself built from ordinary WarpDrive-consuming Ember code and nothing stops it from being reused inside an app-embedded shell — but it is out of scope here.</p><h3 id="architecture-hook-bridge-panel" tabindex="-1">Architecture: hook, bridge, panel <a class="header-anchor" href="#architecture-hook-bridge-panel" aria-label="Permalink to “Architecture: hook, bridge, panel”">​</a></h3><p>Three pieces, following the same shape React DevTools and Redux DevTools use:</p><p><strong>1. The hook (<code>@warp-drive/core/devtools-hook</code>).</strong> When a <code>Store</code> is constructed, and only when the devtools hook is enabled for the build (see &quot;Production safety&quot; below), the <code>Store</code> registers itself on a page-global registry:</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// conceptually, inside Store&#39;s constructor path</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">DEVTOOLS_HOOK_ENABLED</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  installDevtoolsHook</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// idempotent; installs globalThis.__WARP_DRIVE_DEVTOOLS_GLOBAL_HOOK__ once</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  globalThis.__WARP_DRIVE_DEVTOOLS_GLOBAL_HOOK__.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">registerStore</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The hook is a page-global singleton (keyed like WarpDrive&#39;s other page-globals, e.g. the existing <code>RuntimeConfig</code> singleton under a <code>Symbol.for(...)</code>/well-known global name) holding a registry of <code>{ storeId, store, protocolVersion, capabilities }</code> — a page can have more than one <code>Store</code> (multiple Ember apps, micro-frontends, iframes), so the registry is a map, not a singleton store reference. It exposes the minimal surface the bridge needs: enumerate stores, subscribe to a store&#39;s request/cache/notification events, and dispatch an action (invalidate, reload, etc.) to a specific store. The hook itself does not know about <code>chrome.*</code> APIs at all — it only talks <code>postMessage</code>/<code>CustomEvent</code> on <code>window</code>, so it has zero browser-extension-specific code and could in principle be driven by something other than this extension (a test harness, a future embedded panel).</p><p><strong>2. The bridge.</strong> Three standard hops, because a devtools panel cannot reach into the inspected page directly and a content script cannot reach the devtools panel directly:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>devtools panel  &lt;—chrome.runtime.connect (Port)—&gt;  background service worker</span></span>
<span class="line"><span>                                                          |</span></span>
<span class="line"><span>                                                    chrome.tabs.sendMessage /</span></span>
<span class="line"><span>                                                    chrome.scripting relay</span></span>
<span class="line"><span>                                                          |</span></span>
<span class="line"><span>content script  &lt;——— window.postMessage ———&gt;  page (the hook)</span></span></code></pre></div><p>The content script is injected at <code>document_start</code> in the <code>MAIN</code> world (Manifest V3&#39;s <code>world: &quot;MAIN&quot;</code> execution context, available in both Chrome and Firefox 128+) purely to relay <code>postMessage</code> traffic between the page&#39;s <code>window</code> and the extension&#39;s isolated messaging channels — it runs no WarpDrive-aware logic of its own. Firefox support uses <a href="https://github.com/mozilla/webextension-polyfill" target="_blank" rel="noreferrer"><code>webextension-polyfill</code></a> so the background script and panel code are written once against the promise-based API and run unmodified on both browsers; the manifest itself is close enough between MV3 targets that it needs only a <code>browser_specific_settings</code> block for Firefox, not a fork.</p><p><strong>3. The panel.</strong> An Ember app, using WarpDrive&#39;s own framework bindings, rendered inside the devtools panel page each browser provides (<code>chrome.devtools.panels.create</code> / <code>devtools_page</code> for Firefox). It receives bridge messages as they arrive and models them as ordinary WarpDrive requests and cache data — i.e. the extension&#39;s own data layer is a <code>Store</code> (or several, one per inspected <code>Store</code>), fed by a custom <code>RequestManager</code> handler that turns bridge events into request/response documents. This is both a dogfooding choice (per your ask that the extension itself be built with Ember + WarpDrive) and a practical one: the panel gets caching, request-state (pending/error/success), and reactivity for free instead of reinventing them.</p><h3 id="protocol-versioning-and-multi-store-handling" tabindex="-1">Protocol versioning and multi-store handling <a class="header-anchor" href="#protocol-versioning-and-multi-store-handling" aria-label="Permalink to “Protocol versioning and multi-store handling”">​</a></h3><p>The extension version and the inspected app&#39;s WarpDrive version are independent and can be arbitrarily far apart — someone can install the latest extension against an app pinned to an older WarpDrive release. The hook reports a <code>protocolVersion</code> and a <code>capabilities</code> list on connect; the panel negotiates against what it knows and degrades gracefully (a feature backed by a capability the connected hook doesn&#39;t report renders as disabled with a &quot;requires WarpDrive ≥ X&quot; note, rather than breaking). This mirrors how React DevTools&#39; frontend tolerates a range of backend versions.</p><p>Because a page can host more than one <code>Store</code>, the panel&#39;s top-level UI is a store picker (defaulting to the first registered store when there&#39;s only one) whose selection scopes every other panel to that store&#39;s <code>storeId</code>.</p><h3 id="production-safety" tabindex="-1">Production safety <a class="header-anchor" href="#production-safety" aria-label="Permalink to “Production safety”">​</a></h3><p>The hook must never ship in a production bundle by default. It is gated by a new build-config flag (in the same family as <code>includeDataAdapterInProduction</code>), e.g. <code>includeDevtoolsHookInProduction</code>, defaulting to <code>false</code>; in development and test builds the hook is included by default (no app-side setup needed — this is the &quot;always-on in dev&quot; choice this RFC makes deliberately, matching React/Redux DevTools&#39; zero-config experience), with an explicit opt-out (<code>setConfig({ devtoolsHook: false })</code>) for apps that want it off even in dev. The hook only ever <em>reads</em> store state and <em>replays</em> actions the app&#39;s own code could already trigger (re-issuing a request, aborting one) — it never evaluates arbitrary code sent from the panel, so enabling it does not add a code-execution surface. No data ever leaves the browser: the bridge is local <code>chrome.runtime</code>/<code>postMessage</code> traffic only, never a network call.</p><h3 id="phase-1-—-requests-cache-and-log-toggles" tabindex="-1">Phase 1 — requests, cache, and log toggles <a class="header-anchor" href="#phase-1-—-requests-cache-and-log-toggles" aria-label="Permalink to “Phase 1 — requests, cache, and log toggles”">​</a></h3><p><strong>Request inspector.</strong> <code>RequestManager.request()</code> and its handler chain (<code>manager.ts</code>) are the existing seam. A devtools-only handler is registered first in the chain (ahead of the app&#39;s own handlers, alongside where <code>useCache</code> inserts the <code>CacheHandler</code>) whenever the hook is active. It observes, per request: the minted <code>RequestKey</code>/<code>.lid</code>, method and URL, the <code>GodContext</code> given to handlers, dedupe/cache-hit outcome (the same <code>&#39;ISSUED&#39; | &#39;DEDUPED&#39; | &#39;CACHE-HIT&#39;</code> classification <code>CacheHandler</code> already computes for <code>LOG_REQUESTS</code>), timing, and final <code>StructuredDataDocument</code> or <code>StructuredErrorDocument</code>. Today that classification only reaches a developer as unstructured <code>console.log</code> text (<code>store/-private/debug/utils.ts</code>&#39;s <code>log()</code>); this RFC proposes those call sites also emit through a small structured event (a <code>DevtoolsEmitter</code> the log helper and the new handler both use), so there is one source of truth feeding both the console and the extension instead of the extension scraping console output.</p><p>Per-request actions in the panel:</p><ul><li><strong>Invalidate / Reload</strong>: re-issue the same request descriptor with <code>{ cacheOptions: { reload: true } }</code>.</li><li><strong>Background reload</strong>: re-issue with <code>{ cacheOptions: { backgroundReload: true } }</code>.</li><li><strong>Abort</strong>: call <code>.abort()</code> on the live <code>Future</code>, for requests still in flight.</li><li><strong>Force pending / force error</strong>: these have no existing equivalent — nothing in <code>Cache</code> or <code>RequestManager</code> today lets a caller substitute a synthetic outcome for a real network response. This RFC proposes a <code>DevtoolsOverrideHandler</code>, inserted only when the hook is active, that intercepts a request matching a <code>RequestKey.lid</code> the panel has armed and resolves or rejects it with a synthetic <code>StructuredDataDocument</code>/<code>StructuredErrorDocument</code> instead of calling <code>next()</code>. It is a normal handler using the normal handler-chain contract — no new primitive on <code>Cache</code> or <code>RequestManager</code> is needed for this piece.</li></ul><p><strong>Cache explorer.</strong> <code>store.schema.resourceTypes()</code> enumerates every known type; <code>Cache.dump()</code> (the same serialization <code>Cache</code> already supports for fork/SSR use cases) gives a snapshot of cache contents without requiring a new export path. Full eager subscription to every resource identifier does not scale to real app cache sizes, so the explorer tree only subscribes (via <code>NotificationManager.subscribe</code>) to identifiers currently expanded/visible in the panel, and relies on a lightweight global mutation counter (a per-type &quot;something changed&quot; tick) to know when a collapsed subtree&#39;s summary (count, last-modified) needs refreshing without holding a live subscription open for it.</p><p><strong>Debug-log toggles.</strong> The panel becomes a UI over the runtime logging flags that already exist (<code>setWarpDriveLogging</code>/<code>getWarpDriveRuntimeConfig</code>, <code>warp-drive-packages/core/src/types/runtime.ts</code>) instead of requiring the console. Toggling <code>LOG_REQUESTS</code>, <code>LOG_CACHE</code>, etc. from the panel calls the same <code>setLogging</code> the console global already calls — no new runtime API, just a UI on top of one that exists.</p><h3 id="phase-2-—-schema-explorer-and-on-page-highlighting" tabindex="-1">Phase 2 — schema explorer and on-page highlighting <a class="header-anchor" href="#phase-2-—-schema-explorer-and-on-page-highlighting" aria-label="Permalink to “Phase 2 — schema explorer and on-page highlighting”">​</a></h3><p><strong>Schema explorer.</strong> <code>store.schema.resourceTypes()</code> plus <code>schema.fields(type)</code> / <code>schema.resource(type)</code> give every field and relationship for every type with no private access (<code>SchemaService</code> is already a fully public, enumerable API). The panel renders this as both a searchable field table per type and a force-directed graph of <code>belongsTo</code>/<code>hasMany</code> relationships between types, using the graph purely as a navigation aid (click a node to jump to that type&#39;s field table) rather than trying to make the graph itself the primary data surface.</p><p><strong>On-page highlighting and hover actions.</strong> This is the one feature that needs a small change outside the devtools packages themselves: <code>&lt;Request&gt;</code> (from the Ember/React/Vue/Svelte bindings) needs to tag its rendered boundary with a stable attribute identifying which <code>RequestKey</code> it is currently rendering — a <code>data-warp-drive-request</code> attribute carrying the request&#39;s <code>.lid</code>, applied only when the devtools hook is active (so it costs nothing in production and nothing in dev/test unless the extension is actually attached). The content script&#39;s overlay then queries for elements with that attribute, draws a highlight box on hover with the request&#39;s identifier, and offers the same invalidate/reload/background-reload/abort actions as the request inspector, routed through the same bridge. This makes the DOM the entry point into the request inspector, not a separate feature with its own data path.</p><h3 id="phase-3-—-active-resource-tracking-and-request-tracing" tabindex="-1">Phase 3 — active-resource tracking and request tracing <a class="header-anchor" href="#phase-3-—-active-resource-tracking-and-request-tracing" aria-label="Permalink to “Phase 3 — active-resource tracking and request tracing”">​</a></h3><p><strong>Active vs. inactive resources.</strong> No existing capability answers &quot;is this resource or request currently in use anywhere&quot; — issue #9518 calls out exactly this gap as a Cache v3 concern (a <code>capability</code> for &quot;asking if a given request is active / asking for a list of active requests&quot;). Rather than inventing a competing mechanism, this RFC proposes an interim approximation built on what exists today — <code>NotificationManager.subscribe</code>&#39;s live subscriber count for a given identifier, treated as a proxy for &quot;something is currently reading this&quot; — and explicitly frames the durable answer as depending on whatever capability Cache v3 lands. The &quot;eject&quot; quick action (distinct from unloading a record outright) is deferred to land alongside that capability rather than being approximated the same way, since acting on a wrong &quot;unused&quot; signal is destructive in a way a stale display value is not.</p><p><strong>Tracing a request back to source.</strong> <code>RequestManager.request()</code> gains an opt-in capture of <code>new Error().stack</code> at call time, active only when the devtools hook is armed for it (stack capture has a real perf cost, so it is never on by default even in dev). The panel displays the captured stack as-is; clicking through relies on the browser&#39;s own existing source-mapped stack rendering rather than this RFC building custom editor-integration — Chrome and Firefox already resolve dev-build source maps and make stack frames clickable in their native panels, and the extension&#39;s stack display can link into that same behavior instead of re-solving it.</p><h3 id="package-layout" tabindex="-1">Package layout <a class="header-anchor" href="#package-layout" aria-label="Permalink to “Package layout”">​</a></h3><ul><li><code>@warp-drive/core/devtools-hook</code> — the page-global hook and its <code>postMessage</code> protocol. Framework-agnostic, minimal, lives in core so every framework binding gets it identically.</li><li><code>@warp-drive/devtools-protocol</code> — shared TypeScript types for hook ⇄ bridge ⇄ panel messages, versioned independently so the extension and a given WarpDrive release can each depend on the version they actually implement.</li><li><code>warp-drive-packages/devtools-extension</code> — the shipped product: MV3 manifest, background script, content script, and the Ember-based panel app. Lives alongside the other WarpDrive-branded packages rather than under <code>packages/</code> (the legacy <code>@ember-data/*</code> line), since this is new surface, not a shim.</li><li>A demo/e2e app for the extension&#39;s own test suite fits the existing <code>tests/</code> convention (e.g. <code>tests/devtools-extension</code>), exercising real request/cache/schema scenarios the panel needs to render correctly.</li></ul><h2 id="how-we-teach-this" tabindex="-1">How we teach this <a class="header-anchor" href="#how-we-teach-this" aria-label="Permalink to “How we teach this”">​</a></h2><p>Teach it as &quot;the WarpDrive devtools extension&quot; — install once from the Chrome Web Store or Firefox Add-ons, works against any WarpDrive app on a development build with no setup. The guides gain a new page describing what each panel shows and how the quick actions map to existing <code>Store</code>/<code>RequestManager</code> concepts (so &quot;invalidate&quot; is taught as &quot;the same as calling your request again with <code>reload: true</code>,&quot; not as new vocabulary). Existing debug-log documentation gains a note that the same toggles are reachable from the extension&#39;s UI. <code>@ember-data/debug</code>&#39;s docs get a note that it remains the ember-inspector integration for <code>Model</code>-based apps, while the new extension is the general-purpose tool going forward, including for <code>SchemaRecord</code>-based apps.</p><h2 id="drawbacks" tabindex="-1">Drawbacks <a class="header-anchor" href="#drawbacks" aria-label="Permalink to “Drawbacks”">​</a></h2><ul><li><strong>New shipped product.</strong> A browser extension is a maintenance commitment beyond a library release: two extension-store listings, their review processes, and a protocol that has to stay compatible across independent WarpDrive and extension release cadences.</li><li><strong>Bundle-size and surface cost of the hook.</strong> Even minimal, the hook is code that ships in every development and test build by default. It is fully stripped in production by default, but it is a new tripwire to keep honest as the codebase changes.</li><li><strong>Two inspection tools.</strong> Apps still on <code>@ember-data/debug</code>&#39;s ember-inspector integration and the new extension can disagree or overlap in scope for a while; the docs note above is meant to reduce confusion but won&#39;t eliminate it immediately.</li><li><strong>Some features are approximations until Cache v3 lands.</strong> Active-resource tracking in Phase 3 is explicitly a stopgap; it can give a wrong answer for &quot;in use&quot; in cases a real capability would not, which is part of why the destructive &quot;eject&quot; action is deferred rather than shipped against the same approximation.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to “Alternatives”">​</a></h2><ul><li><strong>Extend <code>@ember-data/debug</code> instead of building a new extension.</strong> Rejected: it is architecturally tied to <code>Model</code> and to ember-inspector&#39;s own panel shell, which cannot host a relationship-graph visualizer or an on-page overlay; extending it would mean rewriting most of it inside someone else&#39;s protocol.</li><li><strong>Ship only an in-app embedded devtools panel</strong> (TanStack Query / Apollo style), no browser extension. Rejected as the <em>only</em> delivery mechanism because it can&#39;t overlay the page for DOM highlighting and requires every app to mount it; not rejected as a future addition — the panel UI this RFC describes is ordinary WarpDrive-consuming code and nothing here prevents reusing it inside an app-embedded shell later.</li><li><strong>Opt-in devtools addon apps install themselves</strong>, instead of an always-on DEBUG hook. Rejected for the initial design: it reintroduces a setup step for every app, undermining the &quot;install the extension, it just works&quot; experience this RFC targets, in exchange for a bundle savings that the production-stripped, dev/test-only hook already achieves without the setup cost.</li><li><strong>Chrome-only, Firefox later.</strong> Considered and rejected for this RFC: <code>webextension-polyfill</code> plus MV3&#39;s now-shared <code>world: &quot;MAIN&quot;</code> content-script execution model make both browsers reachable from one implementation, so deferring Firefox would trade a maintained cross-browser design for a marginal short-term simplification.</li></ul><h2 id="unresolved-questions" tabindex="-1">Unresolved questions <a class="header-anchor" href="#unresolved-questions" aria-label="Permalink to “Unresolved questions”">​</a></h2><ul><li>Exact shape of the <code>DevtoolsEmitter</code> that request/cache logging should be refactored to use — whether it subsumes today&#39;s <code>log()</code>/<code>logGroup()</code> console helpers or wraps them, and whether it becomes a small public API of its own (useful beyond this extension, e.g. for app-level telemetry) or stays private to the devtools hook.</li><li>Whether &quot;active resource&quot; tracking should be reachable at all before Cache v3&#39;s real capability lands, given the risk that its approximation is read as more authoritative than it is — an alternative is shipping Phase 3&#39;s tracking feature disabled with a &quot;coming soon, pending Cache v3&quot; state until the real capability exists.</li><li>Whether the devtools protocol should be exposed as a documented, semver&#39;d package (<code>@warp-drive/devtools-protocol</code>) that a third party could target for a different frontend (e.g. a standalone Electron inspector), or kept as an internal implementation detail between the hook and this specific extension until there&#39;s a concrete second consumer.</li><li>Where <code>includeDevtoolsHookInProduction</code>-style configuration should sit relative to the framework-agnostic build plugin described in <a href="/rfcs/0002-warp-drive-build-plugin">RFC 2</a> — ideally this RFC&#39;s flag is just another <code>WarpDriveConfig</code> option handled by the same mechanism, but that RFC was still <code>proposed</code> at the time of writing.</li></ul>`,47)])}var f=a(l,[[`render`,d]]);export{c as __pageData,f as default};
import{at as e,it as t,t as n,yt as r}from"./chunks/framework.CI0evN9J.js";var i=JSON.parse(`{"title":"Writing and Implementing RFCs","description":"","frontmatter":{},"headers":[],"relativePath":"skills/contributors/writing-and-implementing-rfcs.md","filePath":"skills/contributors/writing-and-implementing-rfcs.md"}`),a={name:`skills/contributors/writing-and-implementing-rfcs.md`};function o(n,i,a,o,s,c){return r(),t(`div`,null,[...i[0]||=[e(`<h1 id="writing-and-implementing-rfcs" tabindex="-1">Writing and Implementing RFCs <a class="header-anchor" href="#writing-and-implementing-rfcs" aria-label="Permalink to “Writing and Implementing RFCs”">​</a></h1><p>Use this skill when a task requires an RFC — a new public API, a behavior change, or a deprecation — or when implementing one that has already been accepted.</p><h2 id="when-you-need-an-rfc" tabindex="-1">When you need an RFC <a class="header-anchor" href="#when-you-need-an-rfc" aria-label="Permalink to “When you need an RFC”">​</a></h2><p>Not every change needs one. A bug fix, an internal refactor, or an addition that doesn&#39;t change public API or observable behavior does not. If the change adds, changes, or deprecates public API or behavior, it needs an RFC before implementation begins — see <a href="/guides/contributing/rfc-process">The RFC Process</a> for the full discussion-and-consensus workflow leading up to drafting.</p><h2 id="drafting" tabindex="-1">Drafting <a class="header-anchor" href="#drafting" aria-label="Permalink to “Drafting”">​</a></h2><p>WarpDrive-specific RFCs live in <a href="/rfcs/"><code>rfcs/</code></a> in this repository, which is the <strong>source of truth</strong> — not <code>emberjs/rfcs</code>. Numbering is local to this repo, 1-indexed, independent of any <code>emberjs/rfcs</code> number:</p><ol><li>Copy <code>rfcs/0000-template.md</code> to <code>rfcs/000N-your-title.md</code>, where <code>N</code> is the next unused number (check the existing files in <code>rfcs/</code> — don&#39;t reuse or skip numbers).</li><li>Fill in the template&#39;s frontmatter and body. The sidebar nav is generated automatically from <code>warp-drive-rfc</code>/<code>title</code>/<code>stage</code>/<code>start-date</code>, ordered by RFC number — there&#39;s no separate list to update. Leave <code>emberjs-rfc</code>, <code>emberjs-pr</code>, <code>emberjs-branch</code>, and <code>sync-hash</code> blank — the sync bot fills these in once the RFC is first mirrored upstream; hand-editing them just gets overwritten and can desync the two copies. Don&#39;t start <code>title</code> with &quot;WarpDrive&quot; — the sync bot adds that prefix automatically for the <code>emberjs/rfcs</code> copy and its PR title, so a local title that already has it would end up doubled there.</li><li>Open a PR labeled <code>:label: rfc</code> (see <a href="/guides/contributing/submitting-prs#pull-request-labeling">Pull Request Labeling</a> for the PR mechanics). That label also triggers a docs-site PR preview so reviewers can read the rendered RFC, not just the raw markdown diff.</li><li>Iterate on the PR like any other design discussion. Once there is team consensus to move forward, merging the PR is what publishes the RFC — see the next section for what that triggers.</li></ol><h2 id="how-the-emberjs-rfcs-sync-works" tabindex="-1">How the <code>emberjs/rfcs</code> sync works <a class="header-anchor" href="#how-the-emberjs-rfcs-sync-works" aria-label="Permalink to “How the emberjs/rfcs sync works”">​</a></h2><p>WarpDrive still follows Ember&#39;s RFC process end to end (Proposed → Exploring → FCP → Accepted → Ready for Release → Released → Recommended, per <a href="https://github.com/emberjs/rfcs#stages" target="_blank" rel="noreferrer">emberjs/rfcs&#39; own stages</a>) — those stages are tracked and voted on in <code>emberjs/rfcs</code>, not here. What changes is <em>where the text lives and who edits it first</em>: this repo, not <code>emberjs/rfcs</code>, is authoritative for the content.</p><p>A dedicated bot account (see <code>scripts/rfc-sync/README.md</code>) maintains its own fork of <code>emberjs/rfcs</code> and does the mirroring, entirely through PRs on both sides — it never has direct write access to <code>emberjs/rfcs</code> itself, and never merges anything:</p><ul><li><strong>Outbound</strong> (on merge to <code>main</code> here): a new RFC (no <code>emberjs-rfc</code> set yet) gets a brand-new PR opened against <code>emberjs/rfcs</code> from the bot&#39;s fork; an already-published RFC gets a new commit pushed to the same fork branch that already backs its open <code>emberjs/rfcs</code> PR. Either way, the commit&#39;s author is set to whoever actually wrote the change in this repo — the bot only ever appears as committer, never author, so credit for the words stays with the person who wrote them.</li><li><strong>Inbound</strong>: the bot polls its own fork branches for commits it didn&#39;t make itself — e.g. an Ember reviewer applying a suggested edit directly on the PR (this requires &quot;allow edits from maintainers&quot;, which the bot sets when opening the PR). When it finds one, it opens a PR back into <code>warp-drive-data/warp-drive</code> with that change, again crediting the real author.</li><li>Nothing is ever auto-merged on either side. Every sync lands as a PR for a human to review.</li></ul><p>If you&#39;re picking up an RFC that predates the bot (its <code>emberjs-branch</code> frontmatter field is blank), the bot can&#39;t sync it until a maintainer points it at the right upstream fork branch, or lets it open a fresh PR — ask in <code>#dev-ember-data</code> if you hit this.</p><h2 id="implementing-an-accepted-rfc" tabindex="-1">Implementing an accepted RFC <a class="header-anchor" href="#implementing-an-accepted-rfc" aria-label="Permalink to “Implementing an accepted RFC”">​</a></h2><ul><li>Reference the RFC number in your implementation PR&#39;s description (e.g. &quot;Implements <code>rfcs/0003-...</code>&quot;), so reviewers and future readers can find the design discussion.</li><li>Land the implementation behind the same phased/deprecation approach the RFC describes, if it describes one — don&#39;t skip straight to the end state an RFC called out as a later phase.</li><li>Ship the documentation with the implementation. An accepted RFC is the first item in the <a href="/guides/contributing/writing-documentation/#cross-documentation-checklist">Cross-Documentation Checklist</a> for a new public API or a deprecation; the TSDoc, guide, and upgrade page it lists come next. Follow <a href="./write-documentation">Write Documentation</a> for those.</li><li>Once landed, <code>stage</code> in the RFC&#39;s frontmatter (both here and, via the sync bot, upstream) advances the same way <code>emberjs/rfcs</code> advancement PRs do today — this repo does not add a separate advancement mechanism.</li></ul>`,14)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};
import{Ct as e,at as t,it as n,ot as r,st as i,t as a,tt as o,yt as s}from"./chunks/framework.CI0evN9J.js";var c=JSON.parse(`{"title":"Migrating Between Versions Using The Two Store Approach","description":"","frontmatter":{},"headers":[],"relativePath":"upgrading/v5/two-store-migration.md","filePath":"upgrading/v5/two-store-migration.md"}`),l={name:`upgrading/v5/two-store-migration.md`};function u(a,c,l,u,d,f){let p=e(`SinceBadge`);return s(),n(`div`,null,[c[0]||=o(`h1`,{id:`migrating-between-versions-using-the-two-store-approach`,tabindex:`-1`},[r(`Migrating Between Versions Using The Two Store Approach `),o(`a`,{class:`header-anchor`,href:`#migrating-between-versions-using-the-two-store-approach`,"aria-label":`Permalink to “Migrating Between Versions Using The Two Store Approach”`},`​`)],-1),i(p,{version:`5.0.0`}),c[1]||=t(` \xA0 authored 2023-06-10<p>Sometimes apps build over features, private APIs 🙈, or 3rd party libraries that later become deprecated and removed.</p><p>Most deprecations are relatively easy to resolve, but sometimes APIs change that had been central to the way an app was built. A good example of a difficult to resolve change is the switch away from per-record caching internally, which should have been transparent to apps but left users of a popular 3rd party library (ModelFragments) stuck on the 4.6 release.</p><p>An example of such a shift in public APIs is the move away from Ember&#39;s ArrayLike interface for collections of records and away from promise-proxy wrappers surrounding async relationships. While lots of new patterns exist to replace those APIs, users that made heavy use of these features have a harder time migrating from version 4 to version 5, and thus stop being able to adopt new features in newer parts of their application until a substantial cleanup has occurred.</p><p>This is where the two-store-approach can be useful. In the two-store approach, an application configures a second store service under a different service name (for instance <code>v2-store</code>) enabling some portions of the app to use modern features without immediately being required to upgrade all of the existing application code to match.</p><p>Any application can configure more than one store using whatever version of WarpDrive is installed, but to maximize this approach it is useful to be able to have the second store also use a more recent version of the library.</p><p>This is why we publish <strong>&quot;mirror&quot;</strong> versions of every package.</p><h2 id="mirror-versions" tabindex="-1">Mirror Versions <a class="header-anchor" href="#mirror-versions" aria-label="Permalink to “Mirror Versions”">​</a></h2><p>Every package we publish has a <strong>&quot;mirror&quot;</strong> package equivalent.</p><p>For packages that have an org name (e.g. <code>@ember-data/store</code> or <code>@warp-drive/ember</code>) we add <code>-mirror</code> to the org name. E.g. <code>@ember-data/store</code> has the mirror <code>@ember-data-mirror/store</code>. For packages without an org we add <code>-mirror</code> to the name e.g. <code>ember-data</code> has the mirror <code>ember-data-mirror</code>.</p><p>Mirror packages are configured to work with other mirror packages as a wholly distinct package ecosystem.</p><p>For instance, the mirror of <code>@ember-data/model</code> is <code>@ember-data-mirror/model</code> and instead of having a peer-dependency on <code>@ember-data/store</code> it has a peer-dependency on <code>@ember-data-mirror/store</code>.</p><p>When importing code, you import from the mirror path e.g. <code>import Model, { attr } from &#39;@ember-data-mirror/model&#39;;</code>.</p><p>For convenience, some types, constants and symbols that are safe to interop between versions will do so if you have both mirror and non-mirror versions available.</p><p>Mirror packages are available for versions <code>^4.13.0 | &gt;=5.3.8</code>.</p><h2 id="typescript" tabindex="-1">TypeScript <a class="header-anchor" href="#typescript" aria-label="Permalink to “TypeScript”">​</a></h2><p>All mirror packages are also versions which ship their own types. To use those types, configure your <code>tsconfig.json</code> to be able to find them <a href="/guides/the-manual/typescript/configuration#using-native-types">as shown here</a>, adding the appropriate <code>-mirror</code> appendage to each package name (PS: you can have distinct types for both your older and your newer version!).</p><h2 id="caveat-emptor" tabindex="-1">Caveat Emptor <a class="header-anchor" href="#caveat-emptor" aria-label="Permalink to “Caveat Emptor”">​</a></h2><p>Utilizing the two-store approach has a few tradeoffs:</p><ol><li>If you want compatible type signatures for use by your components, you should upgrade your app to make use of native types first, likely using the <a href="/guides/the-manual/typescript/installation#using-types-packages">types-package approach</a>.</li></ol><p>In fact, it is likely that if you want TypeScript for the v2 store that you are <em>forced</em> to remove the <code>@types/ember*</code> packages from your project entirely and upgrade to using native types for ember-source and ember-data. This is because the native types for WarpDrive depend on ember&#39;s native types.</p><ol start="2"><li><p>You should not use both <code>ember-data</code> and <code>ember-data-mirror</code> or there will be an unresolveable race condition for which one is the <code>store</code> service. Instead one of the stores, usually the mirror, MUST import and fully configure the store. The <a href="./#step-4-configure-the-store">4.x → 5.x guide</a> does this with <code>useLegacyStore</code>, which reproduces the store that <code>ember-data</code> configured &quot;by default&quot; historically. Its options let you drop pieces of that default you no longer want, such as adapters and serializers, or Model itself.</p></li><li><p>Due to (#2) above, your build MUST call <code>setConfig</code> from the mirror package so the mirror gets its own build config. See <a href="./#step-2-configure-the-build">Configure The Build</a> in the 4.x → 5.x guide.</p></li><li><p>Record instances created by one store may not be used by another store, this primarily means they cannot be set as values of relationships. The records (and data) of each store is a wholly distinct context. You may find <a href="https://github.com/customerio/ember-provide-consume-context" target="_blank" rel="noreferrer">ember-provide-consume-context</a> useful for helping to manage this. Migrating &quot;leaf first&quot; or well-encapsulated parts of your app will generally lead to the pit-of-success.</p></li></ol>`,22)])}var d=a(l,[[`render`,u]]);export{c as __pageData,d as default};
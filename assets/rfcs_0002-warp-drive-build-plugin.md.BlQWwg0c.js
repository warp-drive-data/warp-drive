import{Ct as e,at as t,it as n,ot as r,st as i,t as a,tt as o,yt as s}from"./chunks/framework.CI0evN9J.js";var c=JSON.parse(`{"title":"A Framework-Agnostic Build Plugin for WarpDrive","description":"","frontmatter":{"title":"A Framework-Agnostic Build Plugin for WarpDrive","warp-drive-rfc":2,"emberjs-rfc":null,"emberjs-pr":null,"emberjs-branch":null,"sync-hash":null,"stage":"proposed","start-date":"2026-09-02T00:00:00.000Z","release-date":null,"release-versions":null,"teams":["data"],"prs":{"accepted":null},"project-link":null,"suite":null},"headers":[],"relativePath":"rfcs/0002-warp-drive-build-plugin.md","filePath":"rfcs/0002-warp-drive-build-plugin.md"}`),l={name:`rfcs/0002-warp-drive-build-plugin.md`},u={id:`a-framework-agnostic-build-plugin-for-warpdrive`,tabindex:`-1`};function d(a,c,l,d,f,p){let m=e(`StatusBadge`);return s(),n(`div`,null,[o(`h1`,u,[c[0]||=r(`A Framework-Agnostic Build Plugin for WarpDrive `,-1),i(m,{variant:`proposed`,text:`Proposed`}),c[1]||=r(),c[2]||=o(`a`,{class:`header-anchor`,href:`#a-framework-agnostic-build-plugin-for-warpdrive`,"aria-label":`Permalink to “A Framework-Agnostic Build Plugin for WarpDrive”`},`​`,-1)]),c[3]||=t(`<h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to “Summary”">​</a></h2><p>WarpDrive (EmberData) gains a first-class bundler plugin, imported from <code>@warp-drive/core/build-plugin</code>, that replaces its current babel-based build configuration. One plugin works in Vite, Rollup, Rolldown, Webpack, Rspack, and esbuild, serving Ember, React, Vue, Svelte, and Angular apps. It removes <code>@embroider/macros</code> and babel from WarpDrive&#39;s app-facing build path, reduces setup to one line of bundler config, and guarantees a single shared configuration no matter how many copies of WarpDrive packages exist in an app&#39;s dependency tree. The existing <code>setConfig</code> options are accepted unchanged; the babel path is deprecated on a staged timeline and removed in a future major.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to “Motivation”">​</a></h2><p>WarpDrive instruments its source with build-time flags: deprecation stripping keyed on <code>compatWith</code>, canary feature flags, dev-only assertions, and toggleable debug logging. Today, applying an app&#39;s configuration to those flags requires a babel pipeline: <code>setConfig()</code> feeds <code>@embroider/macros</code>, and the app&#39;s babel config must include the embroider macros plugins, <code>babel-plugin-debug-macros</code>, and (for app-side flag use) WarpDrive&#39;s own transform set. Four problems motivate replacing this:</p><p><strong>1. Supporting more frameworks.</strong> WarpDrive now ships bindings for React, Vue, and Svelte alongside Ember. Those ecosystems do not have babel in their default toolchains — Vite uses esbuild/oxc, Angular uses its own compiler. Today a React app must disable its native transforms and adopt babel <em>solely to configure WarpDrive</em>. A bundler plugin meets every framework where it already is.</p><p><strong>2. Simplifying configuration overall.</strong> The current Ember setup threads one config object through several coupled pieces: <code>buildMacros({ configure: ... })</code>, <code>setConfig()</code>, <code>...macros()</code>, <code>...Macros.babelMacros</code>, and a <code>babel-plugin-debug-macros</code> entry — each a chance to wire something in the wrong order or miss a piece entirely (the widely-copied &quot;Simple Config&quot; recipe silently omits the transforms that compile app-side flag imports). The replacement is a single plugin call carrying the same options object.</p><p><strong>3. Avoiding a forced babel pass.</strong> Requiring babel is a real cost even where babel exists: every file WarpDrive ships must flow through the app&#39;s babel pipeline in <code>node_modules</code>, and apps must maintain marker-based include lists to make that happen. The plugin transforms only the files that need it, using a native-speed parser, with no babel dependency.</p><p><strong>4. Avoiding collisions with <code>@embroider/macros</code>.</strong> When an app&#39;s dependency tree accidentally contains more than one copy of <code>@embroider/macros</code>, each copy holds its own config store — under <code>buildMacros()</code> the copies never coordinate — so WarpDrive&#39;s config can silently fail to reach the copy compiling its files, producing wrong or missing stripping with no error. The plugin owns its own coordination (detailed below) and, by consuming WarpDrive&#39;s macros itself before babel runs, removes WarpDrive from embroider&#39;s blast radius entirely — while leaving other addons&#39; embroider usage untouched.</p><p>The expected outcome: every supported framework configures WarpDrive with one plugin line; Ember apps keep their existing options unchanged; the config-collision failure mode becomes impossible or loud; and <code>@embroider/macros</code> plus babel leave WarpDrive&#39;s dependency graph on a published timeline.</p><h2 id="detailed-design" tabindex="-1">Detailed design <a class="header-anchor" href="#detailed-design" aria-label="Permalink to “Detailed design”">​</a></h2><h3 id="the-plugin" tabindex="-1">The plugin <a class="header-anchor" href="#the-plugin" aria-label="Permalink to “The plugin”">​</a></h3><p>The implementation lives in <code>@warp-drive/build-config</code> (the package that already owns <code>setConfig</code> and the flag definitions) and is re-exported as <code>@warp-drive/core/build-plugin</code>. It is <strong>one plugin</strong>, built on <a href="https://unplugin.unjs.io" target="_blank" rel="noreferrer">unplugin</a>: a single instance whose per-bundler adapters are property accesses, so there is one import path for every bundler and no per-framework plugin packages.</p><div class="language-js"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { warpDrive } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@warp-drive/core/build-plugin&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">warpDrive.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vite</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(options);     </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// Vite (Ember via embroider, React, Vue, Svelte, SolidStart, Astro, Nuxt)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">warpDrive.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">webpack</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(options);  </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// Webpack (Next.js, Angular&#39;s legacy builder)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">warpDrive.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">esbuild</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(options);  </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// esbuild (Angular&#39;s current builder, via custom-esbuild)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">warpDrive.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rollup</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(options);   </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// also .rolldown(), .rspack(), .rsbuild()</span></span></code></pre></div><p>The options are the same <code>WarpDriveConfig</code> object <code>setConfig</code> accepts today — <code>compatWith</code>, <code>deprecations</code>, <code>features</code>, <code>debug</code>, <code>polyfillUUID</code>, <code>includeDataAdapterInProduction</code> — with identical semantics and identical environment-variable handling (<code>EMBER_ENV</code>, <code>NODE_ENV</code>, <code>IS_TESTING</code>, <code>WARP_DRIVE_FEATURE_OVERRIDE</code>, and friends). No option is renamed. The plugin and <code>setConfig</code> share one implementation of config resolution, so they cannot drift.</p><h3 id="using-the-new-api" tabindex="-1">Using the new API <a class="header-anchor" href="#using-the-new-api" aria-label="Permalink to “Using the new API”">​</a></h3><p><strong>Ember (embroider + Vite):</strong></p><div class="language-js"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// vite.config.mjs</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { ember, extensions } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@embroider/vite&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { warpDrive } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@warp-drive/core/build-plugin&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  plugins: [</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    ...</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ember</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    warpDrive.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vite</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ compatWith: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;5.7&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>WarpDrive-related entries in <code>babel.config.mjs</code> are no longer needed (see Migration). Babel remains for Ember&#39;s own needs — decorators, templates — untouched.</p><p><strong>Ember (classic ember-cli): no change.</strong> <code>setConfig(app, __dirname, config)</code> in <code>ember-cli-build.js</code> remains the entire user surface, in 5.x via today&#39;s pipeline and after the transition via a WarpDrive-provided babel bridge that the addon wires up automatically.</p><p><strong>React (plain Vite)</strong> — shown to make the framework-agnostic claim concrete:</p><div class="language-js"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// vite.config.mjs</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> react </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@vitejs/plugin-react&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { warpDrive } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@warp-drive/core/build-plugin&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  plugins: [</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">react</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), warpDrive.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vite</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ compatWith: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;5.7&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// No babel config. No esbuild:false workaround. WarpDrive was the only reason either existed.</span></span></code></pre></div><p><strong>Using flags in your own app code</strong> works with zero additional configuration. The same booleans WarpDrive&#39;s source uses are available to apps, compiled by the same plugin:</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { DEBUG } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@warp-drive/core/build-config/env&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { assert } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@warp-drive/core/build-config/macros&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">DEBUG</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">  // stripped from production builds</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">assert</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;expected a store&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isStore</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(candidate)); </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// stripped from production builds</span></span></code></pre></div><p>The plugin recognizes these by their import specifiers — which are WarpDrive-owned module names — so it cannot affect any other import in app code.</p><h3 id="what-the-plugin-does" tabindex="-1">What the plugin does <a class="header-anchor" href="#what-the-plugin-does" aria-label="Permalink to “What the plugin does”">​</a></h3><p>Three transforms, applied only to files that import the relevant modules (a cheap string-marker filter, evaluated natively by the bundler where supported, skips everything else):</p><ol><li><strong>Published WarpDrive packages.</strong> WarpDrive&#39;s published code carries its flags as <code>@embroider/macros</code> expressions (<code>macroCondition(getGlobalConfig().WarpDrive...)</code>). The plugin evaluates these against the app&#39;s config and prunes dead branches — the same stripping the embroider babel plugin performs today, from the same config values. This works against already-published versions: no library upgrade is required to adopt the plugin. Scoping is by the owning package&#39;s <code>package.json</code> name (<code>@warp-drive/*</code>, <code>@ember-data/*</code>, <code>ember-data</code>), so embroider macros in any other package are never touched.</li><li><strong>Flag imports in app code</strong> (the example above), replaced with constant values; dead branches are removed in production by the plugin or the app&#39;s minifier.</li><li><strong><code>deprecate</code>/<code>warn</code> from <code>@ember/debug</code></strong> inside WarpDrive&#39;s published files — the job <code>babel-plugin-debug-macros</code> does today. In Ember apps these are left untouched so <code>registerDeprecationHandler</code> and <code>expectDeprecation</code> keep working; in non-Ember apps they are backed by a console shim in development and stripped in production.</li></ol><p>Runtime-toggleable debug logging is preserved exactly: in dev and test builds, logging branches remain and are gated at runtime, so <code>setWarpDriveLogging({ LOG_REQUESTS: true })</code> in the console keeps working without a rebuild; in production builds unconfigured logging compiles to zero bytes.</p><p>In a later major (see the schedule under &quot;Deprecating the old API&quot;), WarpDrive&#39;s published output stops carrying <code>@embroider/macros</code> expressions at all, switching to plain flag imports with working runtime defaults — at which point a build with no plugin configured still runs correctly (as an unoptimized development-flavored build that logs a one-time warning), and <code>@embroider/macros</code> leaves WarpDrive&#39;s dependencies.</p><h3 id="one-config-no-matter-how-many-copies" tabindex="-1">One config, no matter how many copies <a class="header-anchor" href="#one-config-no-matter-how-many-copies" aria-label="Permalink to “One config, no matter how many copies”">​</a></h3><p>A real dependency tree can contain several copies of the plugin&#39;s own package and many copies of WarpDrive libraries, across bundler worker threads and processes. The design guarantees they all apply one configuration:</p><ul><li><p><strong>Within a process</strong>, every copy converges on a single registry stored on <code>globalThis</code> under <code>Symbol.for(&#39;warp-drive.build-store&#39;)</code> — a key that is identical across all copies and versions. (Embroider&#39;s equivalent handshake is keyed by object identity, which independently-created instances never share; that is the root of today&#39;s duplicate-copy failure.) The registry holds only plain JSON data with an explicit protocol version, so version-skewed copies interoperate or fail loudly, never silently.</p></li><li><p><strong>Registering the same config twice is the normal case</strong> (that is how copies converge). Registering a <em>different</em> config is a build error that names both sources and the first differing keys:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>[WarpDrive::build] Conflicting WarpDrive build configs for app &#39;/srv/app&#39;.</span></span>
<span class="line"><span>  First set from ember-cli-build.js via setConfig() with compatWith: &#39;4.12&#39;;</span></span>
<span class="line"><span>  then from vite.config.mjs via warpDrive.vite() with compatWith: &#39;5.6&#39;.</span></span>
<span class="line"><span>WarpDrive config must be identical everywhere it is declared.</span></span>
<span class="line"><span>Differing keys: compatWith, deprecations.DEPRECATE_TRACKING_PACKAGE.</span></span></code></pre></div></li><li><p><strong>Across threads and processes</strong>, which share no <code>globalThis</code>, the guarantee is determinism: the resolved config is a pure function of the plugin options and environment variables, so every worker that evaluates the same bundler config derives the same result. Where config travels as data (loader options), it carries a hash that the receiving side verifies, turning environment drift into a diagnosable error instead of divergent output.</p></li><li><p><strong>Library copies need no coordination at build time</strong> — they are inert files, each transformed with the same config regardless of which physical copy it is.</p></li></ul><h3 id="coexistence-with-embroider-macros" tabindex="-1">Coexistence with <code>@embroider/macros</code> <a class="header-anchor" href="#coexistence-with-embroider-macros" aria-label="Permalink to “Coexistence with @embroider/macros”">​</a></h3><p>During migration, an app may have both the plugin and an embroider babel pass wired. This is safe in both orders:</p><ul><li>The plugin runs ahead of babel (<code>enforce: &#39;pre&#39;</code>). After it transforms a WarpDrive file, no <code>@embroider/macros</code> imports remain in it. The embroider babel plugin scopes all of its work to references of those imports, so it provably no-ops on the plugin&#39;s output — and continues to process every <em>other</em> package&#39;s macros exactly as before.</li><li>If a misconfigured pipeline runs embroider first, <code>setConfig</code> (which now also feeds the plugin&#39;s registry, and continues to feed embroider&#39;s) ensures embroider inlines the same values the plugin would have; the plugin then finds nothing left to do.</li></ul><p>This ordering property is also the migration mechanism: <strong>adopting the plugin is an insertion, not a swap.</strong> Adding the plugin immediately takes over WarpDrive&#39;s files; removing the old babel entries becomes optional cleanup rather than a coordinated step.</p><h3 id="migrating" tabindex="-1">Migrating <a class="header-anchor" href="#migrating" aria-label="Permalink to “Migrating”">​</a></h3><p><strong>Before</strong> (Ember, embroider + Vite):</p><div class="language-js"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// babel.config.mjs</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { buildMacros } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@embroider/macros/babel&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { setConfig } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@warp-drive/core/build-config&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { macros } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@warp-drive/core/build-config/babel-macros&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Macros</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> buildMacros</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  configure</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: (</span><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">config</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    setConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(config, { compatWith: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;5.7&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  plugins: [</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    ...</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">macros</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;babel-plugin-debug-macros&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">/* ... */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;ember-data-macros&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">    ...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Macros.babelMacros,</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">    // ...decorators, templates, etc.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p><strong>After:</strong></p><div class="language-js"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// vite.config.mjs — one added line</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { ember, extensions } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@embroider/vite&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { warpDrive } </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@warp-drive/core/build-plugin&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  plugins: [</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ember</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), warpDrive.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vite</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ compatWith: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;5.7&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// babel.config.mjs — WarpDrive entries deleted; only decorators/templates remain.</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// If other addons in the app use @embroider/macros, keep buildMacros for them;</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// it will no longer process WarpDrive&#39;s files either way.</span></span></code></pre></div><p>The options object moves verbatim from <code>setConfig</code> to the plugin call. Apps that keep both temporarily get identical output (same config, either order) or a loud conflict error if the two ever disagree — never silent divergence.</p><p>Classic ember-cli apps migrate by doing nothing: <code>setConfig(app, __dirname, config)</code> is unchanged.</p><h3 id="deprecating-the-old-api" tabindex="-1">Deprecating the old API <a class="header-anchor" href="#deprecating-the-old-api" aria-label="Permalink to “Deprecating the old API”">​</a></h3><p>The babel path — <code>babelPlugin()</code>, <code>buildMacros()</code> + <code>setConfig()</code> wiring, <code>macros()</code>, and the <code>babel-plugin-debug-macros</code> entry — is deprecated on this schedule (the 3-arg classic <code>setConfig(app, __dirname, config)</code> form is <em>not</em> deprecated; it becomes classic Ember&#39;s way of passing options to the plugin):</p><ol><li><p><strong>Next 5.x minor:</strong> plugin ships; docs recommend it everywhere except classic ember-cli. The babel path is fully supported and prints nothing.</p></li><li><p><strong>Following 5.x minor:</strong> the babel path prints a one-time build notice (info level, not a deprecation) pointing at the migration guide.</p></li><li><p><strong>Next major (6.0):</strong> the babel path issues a formal build-time deprecation:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>DEPRECATION [warp-drive.legacy-babel-config]: Configuring WarpDrive through babel</span></span>
<span class="line"><span>(babelPlugin(), buildMacros() + setConfig(), macros(), or babel-plugin-debug-macros)</span></span>
<span class="line"><span>is deprecated. Add the WarpDrive build plugin to your bundler config instead — it</span></span>
<span class="line"><span>replaces all of these entries and accepts the same options:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  // vite.config.mjs</span></span>
<span class="line"><span>  import { warpDrive } from &#39;@warp-drive/core/build-plugin&#39;;</span></span>
<span class="line"><span>  plugins: [...ember(), warpDrive.vite({ compatWith: &#39;5.7&#39; })]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Then remove the WarpDrive entries from your babel config.</span></span>
<span class="line"><span>Migration guide: https://docs.warp-drive.io/guides/build-plugin-migration</span></span>
<span class="line"><span>[deprecation id: warp-drive.legacy-babel-config, since: 6.0, until: 7.0]</span></span></code></pre></div></li><li><p><strong>Following major (7.0):</strong> the babel path is removed. A babel <em>bridge</em> plugin (wrapping the same transform core, no embroider involved) remains available indefinitely for pipelines that genuinely only have babel.</p></li></ol><p>Also at 6.0, WarpDrive&#39;s published output switches to the plain-flag format and <code>@embroider/macros</code> is removed from every WarpDrive package&#39;s dependencies — ending the duplicate-copy hazard for Ember apps at the root.</p><h3 id="ecosystem-implications" tabindex="-1">Ecosystem implications <a class="header-anchor" href="#ecosystem-implications" aria-label="Permalink to “Ecosystem implications”">​</a></h3><ul><li><strong>Addons</strong> consuming WarpDrive flags in their own code get compiled by the app&#39;s plugin the same way app code does; addons that use <code>@embroider/macros</code> for their own purposes are unaffected.</li><li><strong>Ember Inspector / debuggability:</strong> unchanged; <code>includeDataAdapterInProduction</code> and the runtime logging toggles behave identically.</li><li><strong>Engines / SSR / FastBoot:</strong> the plugin is build-time only; output semantics match the current pipeline.</li><li><strong>Blueprints:</strong> the app blueprint&#39;s WarpDrive/EmberData wiring updates to the plugin recipe.</li><li><strong>Lint rules:</strong> none required.</li><li><strong>IDE support:</strong> flag imports are real modules with real types; nothing changes.</li></ul><h2 id="how-we-teach-this" tabindex="-1">How we teach this <a class="header-anchor" href="#how-we-teach-this" aria-label="Permalink to “How we teach this”">​</a></h2><p>Teach it as &quot;the WarpDrive build plugin&quot; — a continuation of the existing concept that WarpDrive has build configuration, relocated from babel to the bundler. The guides&#39; setup page reduces from three paradigm-dependent recipes to one line per bundler, with classic ember-cli documented as &quot;no change.&quot; The <code>WarpDriveConfig</code> options reference is already written and applies as-is.</p><p>For existing users, the migration guide is the before/after shown above plus one rule of thumb: <em>put the plugin ahead of babel; delete the babel entries when convenient.</em> For new users, the plugin recipe is strictly simpler than what it replaces, and non-Ember framework docs no longer need to explain babel at all.</p><h2 id="drawbacks" tabindex="-1">Drawbacks <a class="header-anchor" href="#drawbacks" aria-label="Permalink to “Drawbacks”">​</a></h2><ul><li><strong>Install weight:</strong> the plugin adds <code>unplugin</code> and <code>oxc-parser</code> (a native-binary parser with wasm fallback) to <code>@warp-drive/build-config</code>&#39;s dependencies, which every consumer installs transitively. These are node-only, never bundled, and deduped, but they are real bytes and CI surface.</li><li><strong>Two supported paths during the transition</strong> (plugin and babel) means dual documentation and dual testing until 7.0.</li><li><strong>Weak hosts have caveats:</strong> esbuild&#39;s plugin model limits coexistence with other transform plugins (relevant to Angular&#39;s builder), and Turbopack supports only a loader-shaped bridge with user-maintained file globs. Both degrade to correct-but- unoptimized behavior rather than breakage, but the support tiers must be documented honestly.</li><li><strong>Reimplementation risk:</strong> the plugin evaluates the macro expressions WarpDrive publishes, a job embroider&#39;s babel plugin does today. The expression grammar is closed and small (WarpDrive&#39;s own publish step is its only author), and it is locked down by golden tests against real published artifacts, but it is code WarpDrive now owns.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to “Alternatives”">​</a></h2><ul><li><strong>Stay on <code>@embroider/macros</code> + babel.</strong> Rejected: it makes babel a hard requirement in ecosystems that have moved off it, and the duplicate-copy config hazard is structural.</li><li><strong>Ship a babel plugin instead of a bundler plugin.</strong> Simpler to build, but fails the primary motivation — non-babel toolchains — and keeps WarpDrive&#39;s files flowing through app babel pipelines.</li><li><strong>Per-framework plugin packages</strong> (<code>@warp-drive/vite-plugin</code>, etc.). Rejected: unplugin provides all per-bundler adapters from one implementation; separate packages would multiply the version-skew and duplicate-copy surface this RFC works to eliminate.</li><li><strong>A new standalone package for the plugin.</strong> Rejected in favor of housing it in <code>@warp-drive/build-config</code> (re-exported from <code>@warp-drive/core</code>): the plugin lives beside the config code it shares, and consumers need no new dependency.</li><li><strong>Do nothing for non-Ember frameworks</strong> and document babel workarounds. Rejected: the workarounds (disabling native TS/JSX transforms to insert babel) are the worst part of the current non-Ember experience.</li></ul><h2 id="unresolved-questions" tabindex="-1">Unresolved questions <a class="header-anchor" href="#unresolved-questions" aria-label="Permalink to “Unresolved questions”">​</a></h2><ul><li>The classic ember-cli story at 6.0 relies on the addon automatically injecting the babel bridge into the app&#39;s babel options; this is gated behind a flag for the full 6.0 beta cycle, with a documented manual fallback if it proves unreliable across ember-cli-babel versions and engines setups.</li><li>Default flag values for builds that never ran the plugin (post-6.0): lenient test-friendly defaults keep runtime log toggling alive but relax a duplicate-copy runtime guard; strict defaults invert the trade.</li><li>Whether <code>@warp-drive/core/build-plugin</code> should also be exposed under the <code>ember-data</code> package name for apps that consume WarpDrive exclusively through it.</li></ul>`,58)])}var f=a(l,[[`render`,d]]);export{c as __pageData,f as default};
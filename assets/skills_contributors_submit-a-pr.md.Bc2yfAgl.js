import{at as e,it as t,t as n,yt as r}from"./chunks/framework.CI0evN9J.js";var i=JSON.parse(`{"title":"Submit a PR","description":"","frontmatter":{},"headers":[],"relativePath":"skills/contributors/submit-a-pr.md","filePath":"skills/contributors/submit-a-pr.md"}`),a={name:`skills/contributors/submit-a-pr.md`};function o(n,i,a,o,s,c){return r(),t(`div`,null,[...i[0]||=[e(`<h1 id="submit-a-pr" tabindex="-1">Submit a PR <a class="header-anchor" href="#submit-a-pr" aria-label="Permalink to “Submit a PR”">​</a></h1><p>Use this skill when a change is ready to leave your worktree and become a pull request against WarpDrive. It encodes <a href="/guides/contributing/submitting-prs">Submitting PRs</a> plus the label checks CI runs on every PR, so a PR opened this way carries everything those checks look for.</p><h2 id="steps" tabindex="-1">Steps <a class="header-anchor" href="#steps" aria-label="Permalink to “Steps”">​</a></h2><ol><li><p>Target <code>main</code>. Every PR opens against <code>main</code>, even a fix that must also reach a published release. For those, land the <code>main</code> PR first, then cherry-pick the merged commit into a second PR against the release branch (<code>beta</code>, <code>release</code>, <code>lts-4-12</code>, and so on). Do not open the release-branch PR first.</p></li><li><p>Ship tests with the change. A bug fix carries a test that fails without the fix and passes with it. Test deprecation and assertion messages with <code>assert.expectDeprecation()</code> and <code>assert.expectAssertion()</code>; each test app&#39;s <code>test-helper.ts</code> installs them on QUnit&#39;s <code>assert</code> via <code>configureAsserts</code> from <code>@ember-data/unpublished-test-infra/test-support/asserts/index</code>. CI runs every test app in both development and production builds, and production strips assertions, deprecations, and warnings. Wrap any expectation about those in <code>if (DEBUG)</code>, with <code>DEBUG</code> imported from <code>@warp-drive/core/build-config/env</code>. <code>testInDebug</code> is the older form of the same guard and survives only in <code>tests/dont-write-new-tests-here</code>; do not add tests there.</p></li><li><p>Push the branch and let CI verify the change, per <a href="./use-ci-as-the-source-of-truth">Use CI as the Source of Truth</a>. Keep the PR a draft until those checks are green. The one case with no CI loop to read is a first contribution: a maintainer has to approve the workflow run before any check executes, so the PR shows nothing until they do.</p></li><li><p>Update every guide, API doc, and example the change affects, in the same PR. <a href="./write-documentation">Write Documentation</a> covers how to produce each kind of page.</p></li><li><p>Title the PR in Conventional Commits form, <code>type(scope): subject</code>, in the imperative and without a trailing period. The title becomes the squash commit and the changelog line, so it must say what changed for a reader who never opens the PR. The title and body are subject to <a href="./keep-commits-human-authored">Keep Commits Human-Authored</a>, so carry no agent byline.</p></li><li><p>Get a changelog label and a target label onto the PR. CI on <code>main</code> blocks a PR until it carries both; the exact lists live in <code>.github/workflows/enforce-pr-labels-canary.yml</code>, and the changelog mapping in the root <code>package.json</code> under <code>changelog.labels</code>. Which path you take depends on your access to the repository.</p><p><strong>If you are a maintainer</strong>, apply both labels yourself when you open the PR.</p><p><strong>If you are not</strong>, you cannot apply them at all. Labeling needs write or triage access on the repository, which opening a PR does not grant, so the control is absent from your own PR. Name the two labels you expect in the PR body instead, so a maintainer can apply them without re-reading the diff. Both label checks stay red until one does, and that is the expected state of your PR rather than something to fix. Pushing another commit will not clear them. The workflow triggers only on <code>labeled</code>, <code>unlabeled</code>, <code>opened</code>, and <code>reopened</code>, so nothing re-evaluates the PR until a maintainer labels it.</p><p>Pick exactly one changelog label:</p><table tabindex="0"><thead><tr><th>Label</th><th>Use for</th></tr></thead><tbody><tr><td><code>:label: breaking</code></td><td>a breaking change</td></tr><tr><td><code>:label: feat</code></td><td>a new public feature or behavior</td></tr><tr><td><code>:label: bug</code></td><td>a fix for a reported issue</td></tr><tr><td><code>:label: perf</code></td><td>a meaningful performance improvement</td></tr><tr><td><code>:label: cleanup</code></td><td>removal of a deprecated feature, or a deprecation that became an assertion</td></tr><tr><td><code>:label: deprecation</code></td><td>a new deprecation</td></tr><tr><td><code>:label: doc</code></td><td>a fix or improvement to guides or API docs</td></tr><tr><td><code>:label: test</code></td><td>new tests, or a refactor of existing tests</td></tr><tr><td><code>:label: chore</code></td><td>internal refactoring with no public API change worth calling out</td></tr><tr><td><code>:label: rfc</code></td><td>a new RFC, or a change to one; see <a href="./writing-and-implementing-rfcs">Writing and Implementing RFCs</a></td></tr><tr><td><code>:label: dependencies</code></td><td>a dependency bump on <code>main</code>; also satisfies the target check on its own</td></tr></tbody></table><p>Then pick target labels. <code>:dart: canary</code> means no backport. Otherwise add one <code>:dart:</code> label per release channel that needs the change: <code>:dart: beta</code>, <code>:dart: release</code>, <code>:dart: lts</code>, <code>:dart: lts-prev</code>. Maintainers search these while releasing and remove each one once its backport PR is open.</p><p>Never add a <code>backport-*</code> label to a <code>main</code> PR; CI bans them there. <code>:label: doc</code>, <code>:label: feat</code>, and <code>:label: rfc</code> also trigger a live docs preview, linked in a PR comment.</p></li><li><p>For the backport PR itself, cherry-pick onto the release branch and open the PR against that branch. CI adds the matching <code>backport-beta</code>, <code>backport-release</code>, <code>backport-lts</code>, or <code>backport-lts-prev</code> label. For an older non-LTS release branch no job does, so a maintainer applies <code>backport-old-release</code> by hand under the same access rule as step 6. Those PRs need a changelog label too, and CI bans the <code>:dart:</code> labels on them.</p></li><li><p>Discuss first when the change adds or alters public API. Open the conversation with the <a href="https://emberjs.com/team/" target="_blank" rel="noreferrer">team</a> before the implementation goes deep. A change to public API or observable behavior needs an RFC before implementation starts, and <a href="./writing-and-implementing-rfcs">Writing and Implementing RFCs</a> carries that workflow.</p></li></ol><h2 id="example" tabindex="-1">Example <a class="header-anchor" href="#example" aria-label="Permalink to “Example”">​</a></h2><p>A one-line docs fix opens against <code>main</code> titled <code>docs(upgrading): fix the LegacyMode link</code> with the labels <code>:label: doc</code> and <code>:dart: canary</code>. With only <code>:label: doc</code>, the <code>enforce-target-label</code> check fails and stays failed until someone adds <code>:dart: canary</code>. That is exactly how <a href="https://github.com/warp-drive-data/warp-drive/pull/11146" target="_blank" rel="noreferrer">#11146</a> opened.</p>`,6)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};
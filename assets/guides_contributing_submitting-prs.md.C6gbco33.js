import{at as e,it as t,t as n,yt as r}from"./chunks/framework.CI0evN9J.js";var i=JSON.parse(`{"title":"Submitting PRs","description":"","frontmatter":{"title":"Submitting PRs"},"headers":[],"relativePath":"guides/contributing/submitting-prs.md","filePath":"guides/contributing/submitting-prs.md"}`),a={name:`guides/contributing/submitting-prs.md`};function o(n,i,a,o,s,c){return r(),t(`div`,null,[...i[0]||=[e(`<h1 id="submitting-work" tabindex="-1">Submitting Work <a class="header-anchor" href="#submitting-work" aria-label="Permalink to “Submitting Work”">​</a></h1><p>Before implementing a feature or a fix, it is usually best to discuss the proposed changes with <a href="https://emberjs.com/team/" target="_blank" rel="noreferrer">team members</a>. Some fixes might require new public API or changes to existing public APIs. If this is the case, it is even more important to discuss the issue&#39;s problem space and the proposed changes before diving too deep into the implementation.</p><h2 id="making-a-pr" tabindex="-1">Making a PR <a class="header-anchor" href="#making-a-pr" aria-label="Permalink to “Making a PR”">​</a></h2><p>Submissions should be made as PRs against the <code>main</code> branch.</p><p>If a bugfix for an existing release is needed, that work should be cherry-picked to secondary PRs targeting the appropriate release branches after being accepted to the main branch.</p><h3 id="writing-tests" tabindex="-1">Writing Tests <a class="header-anchor" href="#writing-tests" aria-label="Permalink to “Writing Tests”">​</a></h3><p>All PRs should have accompanying tests. For bug-fixes, this should include tests that demonstrate the issue being fixed and test that the solution works.</p><ul><li>We do write tests for our deprecations and assertion messages, using the <code>assert.expectAssertion()</code> and <code>assert.expectDeprecation()</code> helpers.</li><li>Because we run tests in both development and <code>production</code> environments, assertions, deprecations and warnings may be stripped out. To avoid tests of debug behaviors failing for your PR in production environments, use the <code>testInDebug</code> function instead of <code>qunit</code> <code>test</code> to skip them in production when appropriate. <ul><li>alternatively wrap specific assertions in <code>if (DEBUG)</code> or <code>if (PRODUCTION)</code></li></ul></li><li>Update the documentation, examples, and guides when affected by your contribution</li></ul><h3 id="running-tests" tabindex="-1">Running Tests <a class="header-anchor" href="#running-tests" aria-label="Permalink to “Running Tests”">​</a></h3><ul><li>PRs will automatically run an extensive set of test scenarios for your work. In some cases a contributor may need to approve the workflow run if this is your first contribution.</li><li><code>WarpDrive</code> is a collection of packages and comes with multiple test apps scoped to specific situations or parts of the codebase we want to test. These test applications can be found in <code>&lt;project&gt;/tests</code>. These should look like familiar ember app/addon tests, and to run them from within a specific test app use <code>pnpm test</code> or <code>pnpm test --serve</code>. For additional test commands see the list of commands in the respective <code>package.json</code> files.</li></ul><h3 id="pull-request-titles" tabindex="-1">Pull Request Titles <a class="header-anchor" href="#pull-request-titles" aria-label="Permalink to “Pull Request Titles”">​</a></h3><p>PRs should be meaningfully titled to give context into the change for the changelog.</p><h3 id="pull-request-labeling" tabindex="-1">Pull Request Labeling <a class="header-anchor" href="#pull-request-labeling" aria-label="Permalink to “Pull Request Labeling”">​</a></h3><p>All PRs should be labeled. PR labeling for changelog and backporting is enforced in CI, but labels may only be applied by project maintainers. PRs from non-maintainers will be labeled by maintainers prior to a PR being accepted and merged.</p><h4 id="changelog-labels" tabindex="-1">Changelog Labels <a class="header-anchor" href="#changelog-labels" aria-label="Permalink to “Changelog Labels”">​</a></h4><p>Labels used for the changelog include any labels listed in the changelog config in the <a href="https://github.com/warp-drive-data/warp-drive/blob/main/package.json" target="_blank" rel="noreferrer">root package.json</a>.</p><p>These labels are prefixed with <code>changelog:</code> and currently the options are:</p><ul><li><code>:label: breaking</code> which should be used to signify a breaking change</li><li><code>:label: feat</code> which should be used to signify an addition of a new public feature or behavior</li><li><code>:label: bug</code> which should be used to signify a fix for a reported issue</li><li><code>:label: perf</code> which should be used to signify that the commit will improve performance characteristics in a meaningful way</li><li><code>:label: cleanup</code> which should be used to signify removal of deprecated features or that a deprecation has become an assertion.</li><li><code>:label: deprecation</code> which should be used to signify addition of a new deprecation</li><li><code>:label: doc</code> which should be used to signify a fix or improvement to documentation generated for api.emberjs.com</li><li><code>:label: test</code> which should be used to signify addition of new tests or refactoring of existing tests</li><li><code>:label: chore</code> which should be used to signify refactoring of internal code that should not have an affect on public APIs or behaviors but which we may want to call out for potentially unintended consequences.</li><li><code>:label: dependencies</code> which should be used when bumping dependencies on <code>main</code>. Bumps on other branches should use other labels as this implies a more substantive change.</li><li><code>:label: rfc</code> which should be used for PRs that draft, update, or advance a WarpDrive RFC in <a href="/rfcs/"><code>rfcs/</code></a>. This label also triggers a docs-site PR preview — see <a href="./rfc-process">The RFC Process</a>.</li></ul><h4 id="backporting-labels" tabindex="-1">Backporting Labels <a class="header-anchor" href="#backporting-labels" aria-label="Permalink to “Backporting Labels”">​</a></h4><p>We use one set of labels to indicate that a PR needs to be backported and where it needs to be backported to, and a second set of labels to indicate that a PR <strong>is</strong> the backport PR.</p><p>To indicate that a PR should be backported, the following labels, all prefixed with <code>target:</code> are available:</p><ul><li><code>:dart: canary</code> indicates that a PR will not require backporting.</li><li><code>:dart: beta</code> indicates the PR requires being backported to the current beta release.</li><li><code>:dart: release</code> indicates the PR requires being backported to the current active release.</li><li><code>:dart: lts</code> indicates that a PR requires being backported to the most current LTS release.</li><li><code>:dart: lts-prev</code> indicates that a PR requires being backported to the second-most recent LTS release.</li></ul><p>Note: a PR should add the individual label for <em>every</em> backport target required. We use this while releasing to search for any commits still requiring backport to include, and will eventually automate opening backport PRs via a bot when these labels are present. We remove the <code>target:</code> label from merged PRs only once the backport PR has been opened.</p><p>To indicate that a PR <strong>is</strong> the backport PR, the following labels, all prefixed with <code>backport-</code> are available:</p><ul><li><code>backport-beta</code> for PRs to the beta branch</li><li><code>backport-release</code> for PRs to the current active release branch</li><li><code>backport-old-release</code> for PRs to previous release branches that are not LTS branches</li><li><code>backport-lts</code> for PRs targetting the current active LTS branch</li><li><code>backport-lts-prev</code> for PRs targetting the second most current LTS branch</li></ul><p>Note, we automatically add this label to any PR opened to a beta/release/lts branch, but for non-current non-lts backports it will need to be added manually.</p><h4 id="project-labels" tabindex="-1">Project Labels <a class="header-anchor" href="#project-labels" aria-label="Permalink to “Project Labels”">​</a></h4><p>Labels used for tracking work in <a href="https://github.com/warp-drive-data/warp-drive/projects" target="_blank" rel="noreferrer">various projects</a> are not enforced, but PRs and issues should be labeled for any applicable projects and added to those projects when reviewed.</p>`,28)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};
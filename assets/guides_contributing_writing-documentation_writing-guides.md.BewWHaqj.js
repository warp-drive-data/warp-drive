import{at as e,it as t,t as n,yt as r}from"./chunks/framework.CI0evN9J.js";var i=JSON.parse(`{"title":"Writing Guides","description":"How to write a tutorial, concept, or reference guide for the docs site, where the files live, and what frontmatter and tags shape the Markdown that coding agents read.","frontmatter":{"title":"Writing Guides","description":"How to write a tutorial, concept, or reference guide for the docs site, where the files live, and what frontmatter and tags shape the Markdown that coding agents read."},"headers":[],"relativePath":"guides/contributing/writing-documentation/writing-guides.md","filePath":"guides/contributing/writing-documentation/writing-guides.md"}`),a={name:`guides/contributing/writing-documentation/writing-guides.md`};function o(n,i,a,o,s,c){return r(),t(`div`,null,[...i[0]||=[e(`<h1 id="writing-guides" tabindex="-1">Writing Guides <a class="header-anchor" href="#writing-guides" aria-label="Permalink to “Writing Guides”">​</a></h1><p>How to write and maintain the <a href="/guides/">Guides</a>, the markdown pages under <code>guides/</code>, and the two sections that share their tooling but not their rules: <a href="/upgrading/">Upgrading</a> and <a href="/blog/">Blog</a>.</p><h2 id="audience" tabindex="-1">Audience <a class="header-anchor" href="#audience" aria-label="Permalink to “Audience”">​</a></h2><p>Of the <a href="./#know-your-audience">audiences</a> <strong><em>Warp</em>Drive</strong>&#39;s documentation serves, guides are written for the ones still learning: technical evaluators deciding whether it fits, hobbyists trying it out, and new engineers picking up a codebase that already uses it. Assume no knowledge of the project&#39;s history and none of its concepts until the page introduces them. Existing users arrive too, usually from a search or an API docs link, so cross-link the concepts a page depends on rather than re-explaining them.</p><h2 id="guide-types" tabindex="-1">Guide Types <a class="header-anchor" href="#guide-types" aria-label="Permalink to “Guide Types”">​</a></h2><p>Upgrade and migration walkthroughs are not guides; they live in the repo-root <code>upgrading/</code> directory and follow <a href="#upgrading-and-blog-pages">Upgrading and Blog Pages</a> below.</p><p>Guides fall into three types. Keep a single page to a single type; a page that seems to need two is usually two pages.</p><h3 id="tutorial" tabindex="-1">Tutorial <a class="header-anchor" href="#tutorial" aria-label="Permalink to “Tutorial”">​</a></h3><p>A tutorial walks the reader through building something specific or completing a task. It must state its prerequisites up front, show complete code at each step, and end with the result the reader should see. Use it when the reader needs to do something, such as setting up a project or wiring up their first request. Of the guide audiences, tutorials narrow to hobbyists and new engineers, who know their own stack but little about <strong><em>Warp</em>Drive</strong>, so every step has to be runnable as written. Concept and reference guides keep the full guide audience. Tutorials live in their own top-level <a href="/guides/tutorials/">Tutorials</a> section (<code>guides/tutorials/</code>), above The Manual, the section holding the concept and reference guides, so a reader can find them without knowing which concept they cover.</p><h3 id="concept" tabindex="-1">Concept <a class="header-anchor" href="#concept" aria-label="Permalink to “Concept”">​</a></h3><p>A concept guide explains what something is, why it exists, and how it fits with the rest of <strong><em>Warp</em>Drive</strong>. It must define the concept clearly, explain the problem it solves, and cross-link the related concepts and APIs it mentions. Use it when the reader needs to understand something before they can make good decisions about using it, such as how the cache or reactivity works.</p><h3 id="reference" tabindex="-1">Reference <a class="header-anchor" href="#reference" aria-label="Permalink to “Reference”">​</a></h3><p>A reference guide organizes the facts about one area so a reader can look something up. It owns the prose-level facts: which options exist, what values they accept, which rules apply and when. It leaves signatures and per-member details to the generated <a href="/api/">API Docs</a> and links to them rather than restating them. Use it when the reader already knows what they want and needs the details.</p><h2 id="where-files-live" tabindex="-1">Where Files Live <a class="header-anchor" href="#where-files-live" aria-label="Permalink to “Where Files Live”">​</a></h2><p>Guides are markdown files under <code>guides/</code>. Add a new page&#39;s slug to the <code>items</code> list in its directory&#39;s <code>_meta.json</code> so it sorts where you intend; the keys, <code>draft</code> behavior, sidebar, and preview are covered in <a href="./#how-the-docs-site-is-built">How the Docs Site Is Built</a>.</p><h2 id="frontmatter-and-agent-only-content" tabindex="-1">Frontmatter and Agent-Only Content <a class="header-anchor" href="#frontmatter-and-agent-only-content" aria-label="Permalink to “Frontmatter and Agent-Only Content”">​</a></h2><p>Every page is also published as plain Markdown for coding agents and indexed in <code>llms.txt</code> (see <a href="https://warp-drive.io/llm-docs" target="_blank" rel="noreferrer">LLM Optimized Documentation</a>). Two things in the source affect what those agents get:</p><ul><li><strong><code>description</code> in the frontmatter.</strong> The <code>llms.txt</code> entry for a page is its title alone unless the frontmatter sets <code>description</code>, in which case the entry reads <code>- [Title](url): description</code>. An agent choosing which of 900 pages to fetch has only that line to go on, so give every new page a one-sentence <code>description</code> that says what a reader can do after reading it, and add one to any page you touch that lacks it.</li><li><strong><code>&lt;llm-only&gt;</code> and <code>&lt;llm-exclude&gt;</code> tags.</strong> Content wrapped in <code>&lt;llm-only&gt;</code> appears only in the Markdown outputs, never on the website; <code>&lt;llm-exclude&gt;</code> is the reverse. Use <code>&lt;llm-only&gt;</code> for an instruction that only makes sense to an agent (&quot;always pair this with the schema from the previous section&quot;) and <code>&lt;llm-exclude&gt;</code> for a screenshot walkthrough or a &quot;click here&quot; aside. Both are rare; a page that reads well to a human usually reads well to an agent.</li></ul><p>Callouts (<code>:::tip</code>) and <code>:::tabs</code> reach agents in their raw form, so write the tab labels and callout titles as if they will be read as plain text.</p><h2 id="upgrading-and-blog-pages" tabindex="-1">Upgrading and Blog Pages <a class="header-anchor" href="#upgrading-and-blog-pages" aria-label="Permalink to “Upgrading and Blog Pages”">​</a></h2><p><a href="/upgrading/">Upgrading</a> and <a href="/blog/">Blog</a> are different from the rest of the guides: they hold point-in-time content whose URLs are a permanent contract with readers, not just the current best explanation of a concept. Follow these rules when adding to either section.</p><p>These pages are for existing users. An upgrade guide may presume knowledge of the concepts being replaced but never of the ones replacing them. A blog post should say near the top who it is for. LLMs and coding agents also land on these pages, years later and out of order, and will present whatever they find as current unless the page&#39;s version and date say otherwise; that is the reason for the dating rule below.</p><h3 id="urls-are-never-renamed-or-unpublished" tabindex="-1">URLs are never renamed or unpublished <a class="header-anchor" href="#urls-are-never-renamed-or-unpublished" aria-label="Permalink to “URLs are never renamed or unpublished”">​</a></h3><p>Once a page under <code>upgrading/</code> or <code>blog/</code> is published, its path doesn&#39;t change and the page is never deleted, even after its content is out of date.</p><p>If a page&#39;s content is fully superseded (a newer major-version guide replaces it, a post is factually wrong, etc.), don&#39;t delete or move it. Instead:</p><ol><li><p>Replace its body with a short pointer to the replacement, wrapped in a <code>:::danger</code> callout. Keep the frontmatter <code>title</code> and the <code>&lt;SinceBadge&gt;</code> line so the page still says what it was:</p><div class="language-md"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">md</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:::danger </span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;">**We&#39;ve moved!**</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">This guide has [</span><span style="--shiki-light:#032F62;--shiki-light-text-decoration:underline;--shiki-dark:#DBEDFF;--shiki-dark-text-decoration:underline;">moved</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">](</span><span style="--shiki-light:#24292E;--shiki-light-text-decoration:underline;--shiki-dark:#E1E4E8;--shiki-dark-text-decoration:underline;">/upgrading/v6/index.md</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:::</span></span></code></pre></div></li><li><p>Add <code>draft: true</code> to its frontmatter. This hides the page from the sidebar and nav while leaving it published at its original URL (see <a href="./#sidebar-and-meta-json">Sidebar and <code>_meta.json</code></a>).</p></li></ol><h3 id="every-page-is-dated-and-versioned" tabindex="-1">Every page is dated and versioned <a class="header-anchor" href="#every-page-is-dated-and-versioned" aria-label="Permalink to “Every page is dated and versioned”">​</a></h3><p>Record the <strong><em>Warp</em>Drive</strong>* version and the date a page was written for near the top of the page, as a <code>&lt;SinceBadge&gt;</code> followed by the date (<code>YYYY-MM-DD</code>) it was authored or last meaningfully revised:</p><div class="language-md"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">md</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;SinceBadge version=&quot;5.0.0&quot; /&gt; &amp;nbsp; authored 2023-06-10</span></span></code></pre></div><p>This lets a reader who lands on an old search result or bookmark know immediately whether the page still applies to the version they&#39;re using.</p><h3 id="organize-by-major-version" tabindex="-1">Organize by major version <a class="header-anchor" href="#organize-by-major-version" aria-label="Permalink to “Organize by major version”">​</a></h3><p>Each section is sub-divided by major version (<code>upgrading/v5/</code>, <code>blog/v5/</code>, <code>upgrading/v6/</code>, ...). Within a major version&#39;s directory:</p><ul><li><code>upgrading/&lt;major&gt;/</code> holds that version&#39;s upgrade guide plus any deprecation or feature-specific migration guides written against it.</li><li><code>blog/&lt;major&gt;/</code> holds posts published while that major version was current.</li></ul><p>Add new major-version directories to the section&#39;s root <code>_meta.json</code> <code>items</code> list so they sort in release order rather than alphabetically.</p>`,34)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};
/**
 * A selection of pre-built request handlers for handling common
 * request scenarios.
 *
 * @summary Re-exports prebuilt request handlers such as `Gate`, `AutoCompress`, and `MetaDocHandler`, plus header and
 * URL-length helpers, from `@warp-drive/utilities/handlers`.
 * @module
 */
export * from '@warp-drive/utilities/handlers';

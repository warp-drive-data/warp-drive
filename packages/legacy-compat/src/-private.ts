/**
 * Utilities - often temporary - for maintaining backwards compatibility with
 * older parts of EmberData.
 *
 * @summary Legacy re-export of private compat internals from `@warp-drive/legacy/compat/-private`, including
 * `Snapshot`, `SnapshotRecordArray`, `FetchManager`, and `upgradeStore`.
  @module
*/
export * from '@warp-drive/legacy/compat/-private';

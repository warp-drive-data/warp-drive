/**
 * @summary Legacy entry re-exporting the base `Adapter` class and `BuildURLMixin` from `@warp-drive/legacy/adapter` for
 * apps still using the adapter/serializer request path.
 * @module
 * @mergeModuleWith <project>
 */
export { Adapter as default, BuildURLMixin } from '@warp-drive/legacy/adapter';

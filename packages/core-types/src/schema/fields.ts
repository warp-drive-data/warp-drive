/**
 * Legacy alias of {@link @warp-drive/core!types/schema/fields | @warp-drive/core/types/schema/fields}.
 * This entry re-exports that module unchanged so existing
 * `@warp-drive/core-types/schema/fields` imports keep working; new code should import
 * from `@warp-drive/core/types/schema/fields` directly.
 *
 * @summary Legacy alias re-exporting `@warp-drive/core/types/schema/fields`, the field and resource schema types; new
 * code should import from there.
 * @module
 */
export * from '@warp-drive/core/types/schema/fields';

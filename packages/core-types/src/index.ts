/**
 * @summary Legacy package that re-exports the types from `@warp-drive/core/types` so existing `@warp-drive/core-types`
 * imports keep working.
 * @module
 * @mergeModuleWith <project>
 */
export type * from '@warp-drive/core/types';

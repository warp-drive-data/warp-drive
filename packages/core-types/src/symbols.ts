/**
 * Legacy alias of {@link @warp-drive/core!types/symbols | @warp-drive/core/types/symbols}.
 * This entry re-exports that module unchanged so existing
 * `@warp-drive/core-types/symbols` imports keep working; new code should import
 * from `@warp-drive/core/types/symbols` directly.
 *
 * @summary Legacy alias re-exporting `@warp-drive/core/types/symbols`, the symbols that brand typed records with their
 * store, type, and request signature; new code should import from there.
 * @module
 */
export * from '@warp-drive/core/types/symbols';

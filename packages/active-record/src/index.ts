/**
 * @summary Empty root entry of the legacy `@ember-data/active-record` package; its request builders are exported from
 * `@ember-data/active-record/request`.
 * @module
 * @mergeModuleWith <project>
 */
export {};

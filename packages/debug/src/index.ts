/**
 * @summary Exports the `DataAdapter` that lets the Ember Inspector list and watch the records loaded in the store.
 * @module
 * @mergeModuleWith <project>
 */
export { default } from './data-adapter';
